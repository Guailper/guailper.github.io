<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>二进制数补码</title>
    <url>/2021/11/23/%E4%BA%8C%E8%BF%9B%E5%88%B6%E6%95%B0%E8%A1%A5%E7%A0%81/</url>
    <content><![CDATA[<!--mod 为模运算，M 为一个二进制数的模（有一个n位二进制数，则M = 2^n）-->

<h2 id="1-1-作用"><a href="#1-1-作用" class="headerlink" title="1.1 作用"></a>1.1 作用</h2><p>计算机中，使用二进制的补码形式表示带符号整数</p>
<h2 id="1-2原码与补码之间的转换"><a href="#1-2原码与补码之间的转换" class="headerlink" title="1.2原码与补码之间的转换"></a>1.2原码与补码之间的转换</h2><p>一个二进制数如何转换为它的补码形式</p>
<h3 id="1-2-1-正数原码与补码"><a href="#1-2-1-正数原码与补码" class="headerlink" title="1.2.1 正数原码与补码"></a>1.2.1 正数原码与补码</h3><p><strong>原码→补码：</strong></p>
<p>X &#x3D; <strong>0</strong> 000 0011</p>
<p>这是一个正数的原码表示，表示 3。</p>
<p>这里我们看一下模运算求正数补码的公式：</p>
<p>[X]  &#x3D; X + M ( mod M)</p>
<p>转换为补码后，[X] &#x3D; <strong>0</strong> 000 0011。可以看出正数的原码与补码一样。</p>
<p>所以，一个正数的补码表示的<strong>符号位为 0</strong>。以后求正数的补码可以直接将其原码写上。</p>
<p><strong>补码→原码：</strong></p>
<p>由上可知，正数补码与原码相同，即补码即原码</p>
<h3 id="1-2-2-负数原码与补码"><a href="#1-2-2-负数原码与补码" class="headerlink" title="1.2.2 负数原码与补码"></a>1.2.2 负数原码与补码</h3><p><strong>原码→补码：</strong></p>
<p>X &#x3D; <strong>1</strong> 000 0011</p>
<p>这是一个负数的原码表示，表示 -3。</p>
<p>这里我们看一下模运算求负数补码的公式：</p>
<p>[X]  &#x3D; M + X ( mod M)</p>
<p>转换为补码后，[X] &#x3D; <strong>1</strong> 111 1101。</p>
<p>对比一下 X 与 [X]:</p>
<p>  X &#x3D; <strong>1</strong> 000 0011</p>
<p>[X] &#x3D; <strong>1</strong> 111 1101</p>
<p>我们可以看到原码与补码的变化。</p>
<p>原码的 <strong>符号位为1不变，数值部分取反加1</strong> 后，得到补码。</p>
<p><strong>补码→原码：</strong></p>
<p>由上可知，原码的 <strong>符号位为1不变，数值部分取反加1</strong> 后，得到补码。</p>
<p>那么，将思路逆转一下，将补码 <strong>符号位为1不变，数值部分减一取反</strong> 后，得到原码。</p>
<p>除此之外，还有其他方法吗？❤❤❤</p>
<p>对比一下 [X] 与 X:</p>
<p>[X] &#x3D; <strong>1</strong> 111 1101</p>
<p>  X &#x3D; <strong>1</strong> 000 0011</p>
<p>是不是似曾相识呢？</p>
<p>没错，补码的 <strong>符号位为1不变，数值部分取反加一</strong> 后，得到原码。</p>
]]></content>
      <tags>
        <tag>二进制</tag>
      </tags>
  </entry>
  <entry>
    <title>视频剪辑基础</title>
    <url>/2026/04/07/film-editing/</url>
    <content><![CDATA[<h1 id="达芬奇第一课"><a href="#达芬奇第一课" class="headerlink" title="达芬奇第一课"></a>达芬奇第一课</h1><h2 id="1-视频剪辑基础知识"><a href="#1-视频剪辑基础知识" class="headerlink" title="1. 视频剪辑基础知识"></a>1. 视频剪辑基础知识</h2><h3 id="1-1-数字视频信号"><a href="#1-1-数字视频信号" class="headerlink" title="1.1 数字视频信号"></a>1.1 数字视频信号</h3><p>标清：720p以下</p>
<p>高清：720p-1080p，宽高比为16:9</p>
<p>超高清：4k分辨率（3840*2160px）或者8k分辨率（7680*4320px）</p>
<p>帧速率（单位fps）：每秒显示的图片数量，大于10fps可以在视觉上造成平滑的动画。</p>
<h3 id="1-2-场与场序"><a href="#1-2-场与场序" class="headerlink" title="1.2 场与场序"></a>1.2 场与场序</h3><p>PAL制：每帧625行扫描</p>
<p>每秒25帧</p>
<p>NTSC制：每帧525行扫描</p>
<p>每秒30帧</p>
<p>分离场：解决交错视频场的最佳方案（在对素材进行如变速、缩放、旋转等时有很好效果，未进行场分离会使画面有严重抖动、毛刺效果）</p>
<h3 id="1-3-时间代码"><a href="#1-3-时间代码" class="headerlink" title="1.3 时间代码"></a>1.3 时间代码</h3><p>国际标准称为SMPTE时间代码，简称时码，表示为h : m : s : f</p>
<h3 id="1-4-帧长比"><a href="#1-4-帧长比" class="headerlink" title="1.4 帧长比"></a>1.4 帧长比</h3><p>帧的长度和宽度的比例。</p>
<h3 id="1-5-像素长宽比"><a href="#1-5-像素长宽比" class="headerlink" title="1.5 像素长宽比"></a>1.5 像素长宽比</h3><p>像素的长度和宽度的比例。PAL制视频采用矩形像素，一帧图像由720*576像素组成。其像素长宽比为1.067.方形像素的像素长宽比为1，剪辑软件可调整像素长宽比。</p>
<h3 id="1-6-颜色模式"><a href="#1-6-颜色模式" class="headerlink" title="1.6 颜色模式"></a>1.6 颜色模式</h3><p>颜色模式即翻译颜色的方法。视频领域常用RGB模式、Lab模式、HSB模式、YUV模式。</p>
<p>PAL制采用YUV模式，NSTC制采用HSB模式</p>
<h3 id="1-7-颜色深度"><a href="#1-7-颜色深度" class="headerlink" title="1.7 颜色深度"></a>1.7 颜色深度</h3><p>颜色深度：每个像素可显示出的颜色数。8位颜色是256色；16位颜色为中彩色；24位颜色为真彩色（百万色）；32位颜色为百万+（24位颜色+8位透明信息（Alpha通道））</p>
<h3 id="1-8-视频格式"><a href="#1-8-视频格式" class="headerlink" title="1.8 视频格式"></a>1.8 视频格式</h3><p>(1)AVI格式</p>
<p>音频交错格式。图像质量好，可跨平台，体积庞大，压缩标准多。</p>
<p>(2)DV-AVI格式</p>
<p>(3)MPEG格式</p>
<p>运动图像专家组格式。如VCD、SVCD、DVD。压缩标准：MPEG-1、MPEG-2、MPEG-4。</p>
<p>(4)MOV格式</p>
<p>Apple开发的。默认播放器QuickTime。</p>
<p>较高压缩比率，较完美视频清晰度。跨平台。</p>
<p>(5)RM格式</p>
<p>RealPlayer播放器。压缩速率可随网络传输速率制定。</p>
<p>(6)RMVB格式</p>
<p>(7)WMV格式</p>
<p>英文全称为Windows Media Video，也是微软推出的一种采用独立<a href="https://baike.baidu.com/item/%E7%BC%96%E7%A0%81">编码</a>方式并且可以直接在网上实时观看视频节目的文件压缩格式。WMV格式的主要优点包括：本地或网络回放、可扩充的媒体类型、可伸缩的媒体类型、多语言支持、环境独立性、丰富的流间关系以及扩展性等。</p>
<p>(8)DivX格式</p>
<p>(9)ASF格式</p>
<h2 id="2-媒体面板"><a href="#2-媒体面板" class="headerlink" title="2. 媒体面板"></a>2. 媒体面板</h2><p><img src="https://i.loli.net/2021/11/05/8LSGygHQBNcPoT5.png" alt="GIEB_}T)2(7%T@XM8TB8IBO.png"></p>
<p>这个面板主要是将素材添加到达芬奇中，对素材进行分类。</p>
<p>场景剪切检测</p>
<p>对素材进行描述</p>
<h2 id="3-快编面板"><a href="#3-快编面板" class="headerlink" title="3. 快编面板"></a>3. 快编面板</h2><p><img src="https://i.loli.net/2021/11/05/9jePwHaRYIyK3k7.png" alt="快编.png"></p>
<p>磁带逻辑</p>
<p>磁带监看(母带监看)</p>
<p>每个素材一个分界</p>
<p>自动调色（快捷键C）</p>
<h2 id="4-剪辑面板"><a href="#4-剪辑面板" class="headerlink" title="4. 剪辑面板"></a>4. 剪辑面板</h2><p><img src="https://i.loli.net/2021/11/05/qFKiMBPybj3vODU.png" alt="剪辑.png"></p>
<p>Aroll：视频的主体</p>
<p>Broll：辅助镜头</p>
<p>智能媒体夹：可快速筛选素材</p>
<p>双时间线：在不破坏素材连续性的情况下，方便选择素材。（Aroll、Broll）</p>
<p>I、O 分别设置入点出点。</p>
<p>J、K、L键分别为后退、暂停、前进。</p>
<h2 id="5-交付面板"><a href="#5-交付面板" class="headerlink" title="5. 交付面板"></a>5. 交付面板</h2><p><img src="https://i.loli.net/2021/11/05/9jePwHaRYIyK3k7.png" alt="快编.png"></p>
<p>1.选中目标序列(即时间线)</p>
<p>2.选中渲染格式(可选择预设类型，也可以自己设置导出格式)</p>
<p>​	这一步注意选择的是渲染单个片段还是多个片段</p>
<p>3.添加到渲染队列</p>
<p>4.开始渲染</p>
<span id="more"></span>
]]></content>
      <tags>
        <tag>视频剪辑</tag>
      </tags>
  </entry>
  <entry>
    <title>Git学习</title>
    <url>/2026/04/07/Git%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="工作流程"><a href="#工作流程" class="headerlink" title="工作流程"></a>工作流程</h1><p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260413214134979.png?token=AR3LVDCDJILRVE57MOBMKADJ3TZMW" alt="image-20260413214126265"></p>
<ul>
<li>工作区（Working Directory）</li>
</ul>
<p>实际修改文件的地方，即本地工作目录</p>
<ul>
<li>暂存区</li>
</ul>
<p>文件修改完成后，暂时存放的地方</p>
<p>指令：git add</p>
<ul>
<li>本地仓库</li>
</ul>
<p>存放暂存区中打包好的文件，并附上标签信息</p>
<p>指令：git commit</p>
<ul>
<li>远程仓库</li>
</ul>
<p>接收本地仓库中发送过来的文件包，方便别人查看</p>
<p>指令：git push</p>
<ul>
<li>贮藏区</li>
</ul>
<p>存储未完成但又不想提交的文件</p>
<p>指令：git stash</p>
<h1 id="初次配置"><a href="#初次配置" class="headerlink" title="初次配置"></a>初次配置</h1><p>全局基础参数配置：用户名和邮箱（github或gitee中使用的名字和邮箱）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;你的名字&quot;</span><br><span class="line">git config --global user.email &quot;你的邮箱&quot;</span><br></pre></td></tr></table></figure>

<h1 id="仓库相关"><a href="#仓库相关" class="headerlink" title="仓库相关"></a>仓库相关</h1><h2 id="初始化仓库"><a href="#初始化仓库" class="headerlink" title="初始化仓库"></a>初始化仓库</h2><p>Git 使用 <strong>git init</strong> 命令来初始化一个 Git 仓库，Git 的很多命令都需要在 Git 的仓库中运行，所以 <strong>git init</strong> 是使用 Git 的第一个命令。</p>
<p>在执行完成 <strong>git init</strong> 命令后，Git 仓库会生成一个 .git 目录，该目录包含了资源的所有元数据，其他的项目目录保持不变。</p>
<h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><p>当前目录作为仓库：git init</p>
<p>指定目录作为仓库：git init {路径}</p>
<p>[^1]: <strong>注：</strong> 在 Linux 系统中，commit 信息使用单引号 **’**，Windows 系统，commit 信息使用双引号 **”**。所以在 git bash 中 <strong>git commit -m ‘提交说明’</strong> 这样是可以的，在 Windows 命令行中就要使用双引号 **git commit -m “提交说明”**。</p>
<h2 id="关联远程仓库"><a href="#关联远程仓库" class="headerlink" title="关联远程仓库"></a>关联远程仓库</h2><p>使用git remote来关联远程仓库。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1. 查看当前关联的远程仓库</span></span><br><span class="line">git remote -v</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">2. 关联远程仓库（把链接换成你的）</span></span><br><span class="line">git remote add origin https://github.com/xxx/xxx.git</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">示例：git remote add origin https://github.com/user_name/respository_name.git(仓库名区分大小写)</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">3. 修改远程仓库地址</span></span><br><span class="line">git remote set-url origin https://新地址.git</span><br></pre></td></tr></table></figure>

<h2 id="仓库克隆"><a href="#仓库克隆" class="headerlink" title="仓库克隆"></a>仓库克隆</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1. 第一次克隆整个项目，完整仓库地址可以在github仓库页面获取到</span></span><br><span class="line">git clone https://远程仓库地址.git</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">2. 拉取远程分支最新代码</span></span><br><span class="line">git pull origin 分支名（如main）</span><br></pre></td></tr></table></figure>

<h2 id="文件上传"><a href="#文件上传" class="headerlink" title="文件上传"></a>文件上传</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1. 查看文件修改状态</span></span><br><span class="line">git status</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">2. 添加文件到暂存区</span></span><br><span class="line">git add 文件名        # 添加单个文件</span><br><span class="line">git add .            # 添加所有文件（最常用）</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">3. 提交到本地仓库（必须写备注）</span></span><br><span class="line">git commit -m &quot;本次提交说明&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">4. 推送到远程仓库，首次push分支需要使用git push origin 分支名(如main、master)</span></span><br><span class="line">git push origin main    # 第一次推送，主流分支名 main/master</span><br><span class="line">git push				# 非第一次推送</span><br></pre></td></tr></table></figure>

<h1 id="分支相关"><a href="#分支相关" class="headerlink" title="分支相关"></a>分支相关</h1><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1. 查看所有分支，前面带 * 的是你当前所在的分支</span></span><br><span class="line">git branch</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">2. 创建新分支（开发新功能），基于你当前所在的分支，复制一份代码，创建新分支。注意：创建分支后需要手动切换到新分支</span></span><br><span class="line">git branch 分支名</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">3. 切换到分支</span></span><br><span class="line">git checkout 分支名</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或新版命令</span></span><br><span class="line">git switch 分支名</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">4. 创建并直接切换分支（最常用）</span></span><br><span class="line">git checkout -b 分支名</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">5. 把分支代码合并到主分支</span></span><br><span class="line">git checkout main    # 先切回主分支</span><br><span class="line">git merge 分支名     # 合并你的分支</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">6. 删除本地分支</span></span><br><span class="line">git branch -d 分支名</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">7. 推送分支到远程</span></span><br><span class="line">git push origin 分支名</span><br></pre></td></tr></table></figure>



<h1 id="创建repository"><a href="#创建repository" class="headerlink" title="创建repository"></a>创建repository</h1><h2 id="在桌面进入git-bash："><a href="#在桌面进入git-bash：" class="headerlink" title="在桌面进入git bash："></a>在桌面进入git bash：</h2><p>账号确定：git config –global user.name “账号名字”</p>
<p>邮箱确定(账号注册时的邮箱)：git config –global user.email “邮箱”</p>
<p>生成本地密码：ssh-keygen -t rsa -C “邮箱备注”</p>
<p>生成密码后，在C盘找到 .ssh 文件（文件内包含两个文件：id_rsa和id_rsa.pub）</p>
<p>打开id_rsa.pub文件，复制其中的密钥。</p>
<p>打开github，进入seeting，找到 SSH and GPG keys, 点击 new SSH key，将密钥粘贴到Key中（Title建议写用户名）</p>
<p>检测是否连接成功：shh -T <a href="mailto:&#103;&#105;&#116;&#64;&#x67;&#105;&#116;&#x68;&#x75;&#x62;&#46;&#x63;&#111;&#109;">&#103;&#105;&#116;&#64;&#x67;&#105;&#116;&#x68;&#x75;&#x62;&#46;&#x63;&#111;&#109;</a>(注意看出现的信息是否连接成功)</p>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>栈的定义及实现(C语言)</title>
    <url>/2021/11/01/%E6%A0%88/</url>
    <content><![CDATA[<h1 id="栈的定义及实现-C语言"><a href="#栈的定义及实现-C语言" class="headerlink" title="栈的定义及实现(C语言)"></a>栈的定义及实现(C语言)</h1><h3 id="1-1-栈的定义和特点"><a href="#1-1-栈的定义和特点" class="headerlink" title="1.1 栈的定义和特点"></a>1.1 栈的定义和特点</h3><p>栈：只能在表尾插入或删除元素的线性表</p>
<p>栈顶(top)：线性表的表尾</p>
<p>栈底(bottom)：线性表的表头</p>
<p>操作原则：后进先出原则(LIFO)</p>
<h3 id="1-2-栈的操作"><a href="#1-2-栈的操作" class="headerlink" title="1.2 栈的操作"></a>1.2 栈的操作</h3><p>创建栈 Init_Stack()</p>
<p>添加元素 push()</p>
<p>删除元素 pop()</p>
<p>获取栈顶元素 top()</p>
<p>获取栈实际长度 size()</p>
<h3 id="1-3-顺序栈的实现"><a href="#1-3-顺序栈的实现" class="headerlink" title="1.3 顺序栈的实现"></a>1.3 顺序栈的实现</h3><h5 id="1-3-1-定义一个栈结构体"><a href="#1-3-1-定义一个栈结构体" class="headerlink" title="1.3.1 定义一个栈结构体"></a>1.3.1 定义一个栈结构体</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">stack</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> *elem;</span><br><span class="line">    <span class="type">int</span> length;</span><br><span class="line">&#125;<span class="built_in">stack</span>;</span><br></pre></td></tr></table></figure>

<h5 id="1-3-2-初始化"><a href="#1-3-2-初始化" class="headerlink" title="1.3.2 初始化"></a>1.3.2 初始化</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">Init_Stack</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    sq.elem = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="type">int</span>) * stack_size);</span><br><span class="line">    sq.length = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;初始化成功&quot;</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="1-3-3-将元素压入栈"><a href="#1-3-3-将元素压入栈" class="headerlink" title="1.3.3 将元素压入栈"></a>1.3.3 将元素压入栈</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">push</span><span class="params">(<span class="built_in">stack</span> &amp;sq,<span class="type">int</span> elem)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(sq.length &gt;= stack_size) <span class="keyword">return</span>;</span><br><span class="line">    sq.elem[sq.length] = elem;</span><br><span class="line">    sq.length++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="1-3-4-将元素弹出栈"><a href="#1-3-4-将元素弹出栈" class="headerlink" title="1.3.4 将元素弹出栈"></a>1.3.4 将元素弹出栈</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">pop</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(sq.length &lt;= <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    sq.elem[sq.length<span class="number">-1</span>] = <span class="number">0</span>;</span><br><span class="line">    sq.length--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="1-3-5-获取栈顶元素"><a href="#1-3-5-获取栈顶元素" class="headerlink" title="1.3.5 获取栈顶元素"></a>1.3.5 获取栈顶元素</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>  <span class="title function_">top</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> sq.elem[sq.length<span class="number">-1</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="1-3-6-获取栈实际长度"><a href="#1-3-6-获取栈实际长度" class="headerlink" title="1.3.6 获取栈实际长度"></a>1.3.6 获取栈实际长度</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">size</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> sq.length;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-4-链栈的实现"><a href="#1-4-链栈的实现" class="headerlink" title="1.4 链栈的实现"></a>1.4 链栈的实现</h3><p>需要另外定义一个全局的整形变量 length 来存放栈的长度。</p>
<h5 id="1-4-1-定义一个栈结构体"><a href="#1-4-1-定义一个栈结构体" class="headerlink" title="1.4.1 定义一个栈结构体"></a>1.4.1 定义一个栈结构体</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> elem;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Stack</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;*<span class="built_in">stack</span>,*node;</span><br></pre></td></tr></table></figure>

<h5 id="1-4-2-初始化"><a href="#1-4-2-初始化" class="headerlink" title="1.4.2 初始化"></a>1.4.2 初始化</h5><p>需要为头结点分配空间</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">Init_Stack</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    sq = (<span class="built_in">stack</span>)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Stack));</span><br><span class="line">    sq-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="1-4-3-将元素压入栈"><a href="#1-4-3-将元素压入栈" class="headerlink" title="1.4.3 将元素压入栈"></a>1.4.3 将元素压入栈</h5><p>需要为新节点分配空间</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">push</span><span class="params">(<span class="built_in">stack</span> &amp;sq,<span class="type">int</span> e)</span>&#123;</span><br><span class="line">    node p = (node)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Stack));</span><br><span class="line">    p-&gt;elem = e;</span><br><span class="line">    p-&gt;next = sq-&gt;next;</span><br><span class="line">    sq-&gt;next = p;</span><br><span class="line">    length++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="1-4-4-将元素弹出栈"><a href="#1-4-4-将元素弹出栈" class="headerlink" title="1.4.4 将元素弹出栈"></a>1.4.4 将元素弹出栈</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">pop</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(sq-&gt;next == <span class="literal">NULL</span>) <span class="keyword">return</span>;</span><br><span class="line">    node p = sq-&gt;next;</span><br><span class="line">   sq-&gt;next = sq-&gt;next-&gt;next;</span><br><span class="line">   delete p;</span><br><span class="line">   length--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="1-4-5-获取栈顶元素"><a href="#1-4-5-获取栈顶元素" class="headerlink" title="1.4.5 获取栈顶元素"></a>1.4.5 获取栈顶元素</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>  <span class="title function_">top</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(sq-&gt;next == <span class="literal">NULL</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> sq-&gt;next-&gt;elem;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="1-4-6-获取栈实际长度"><a href="#1-4-6-获取栈实际长度" class="headerlink" title="1.4.6 获取栈实际长度"></a>1.4.6 获取栈实际长度</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">size</span><span class="params">(<span class="built_in">stack</span> &amp;sq)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> length;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-5-栈的问题解决"><a href="#1-5-栈的问题解决" class="headerlink" title="1.5 栈的问题解决"></a>1.5 栈的问题解决</h3><h5 id="1-5-1-括号匹配"><a href="#1-5-1-括号匹配" class="headerlink" title="1.5.1 括号匹配"></a>1.5.1 括号匹配</h5><p>苗苗今天刚刚学会使用括号，不过他分不清小括号，中括号，大括号和尖括号，不知道怎么使用这些括号，请帮助他判断括号使用是否正确。</p>
<p>输入格式</p>
<p>共一行，包含一个由 <code>&lt;</code>,<code>(</code>,<code>&#123;</code>,<code>[</code>,<code>&gt;</code>,<code>)</code>,<code>&#125;</code>,<code>]</code> 构成的字符串。</p>
<p>输出格式</p>
<p>如果输入的字符串中的括号正确匹配则输出 <code>yes</code>，否则输出 <code>no</code>。</p>
<p>数据范围</p>
<p>输入字符串长度不超过 1000010000。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line">using namespace <span class="built_in">std</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stack&gt;</span></span></span><br><span class="line"><span class="built_in">stack</span>&lt;<span class="type">char</span>&gt; sq;</span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="built_in">string</span> s;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; s.size(); i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i] == <span class="string">&#x27;(&#x27;</span> || s[i] == <span class="string">&#x27;[&#x27;</span> || s[i] == <span class="string">&#x27;&lt;&#x27;</span> || s[i] == <span class="string">&#x27;&#123;&#x27;</span>)&#123;</span><br><span class="line">            sq.push(s[i]);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//如果不是左括号并且栈内无元素，直接判断非法</span></span><br><span class="line">            <span class="keyword">if</span> (sq.empty())</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;no&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//接下来逐一判断是否匹配</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (s[i] == <span class="string">&#x27;&gt;&#x27;</span> &amp;&amp; sq.top() == <span class="string">&#x27;&lt;&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                sq.pop();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (s[i] == <span class="string">&#x27;)&#x27;</span> &amp;&amp; sq.top() == <span class="string">&#x27;(&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                sq.pop();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (s[i] == <span class="string">&#x27;]&#x27;</span> &amp;&amp; sq.top() == <span class="string">&#x27;[&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                sq.pop();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (s[i] == <span class="string">&#x27;&#125;&#x27;</span> &amp;&amp; sq.top() == <span class="string">&#x27;&#123;&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                sq.pop();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;no&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//需要再次判断栈中是否还有字符</span></span><br><span class="line">    <span class="keyword">if</span>(sq.empty())&#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;yes&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;no&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>PicGo图床配置</title>
    <url>/2021/11/28/%E5%9B%BE%E5%BA%8A%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h2 id="1-github上创建仓库"><a href="#1-github上创建仓库" class="headerlink" title="1.github上创建仓库"></a>1.github上创建仓库</h2><p>当然，这要先注册一个github账号😁😁😁</p>
<p>进入主页后点击New进入创建界面</p>
<p>填好仓库名字，选择public类型（即开放仓库），勾选add a REAMDE file</p>
<p>然后点击Create repository创建。</p>
<p>然后，点击个人头像，找到设置（Setting），点击Developer setting（开发人员设置）。</p>
<p>进入Personal access tokens（个人访问令牌），点击Generate new token（生成新令牌）</p>
<p><img src="https://raw.githubusercontent.com/MgL02/MyPicGo/main/images/202111282055068.png" alt="img"></p>
<p>在Note下方输入令牌名字，勾选repo后生成即可。</p>
<p>复制生成的token（后面会用到）</p>
<h2 id="2-PicGo设置"><a href="#2-PicGo设置" class="headerlink" title="2.PicGo设置"></a>2.PicGo设置</h2><p>找到图床设置下的<strong>Github图床</strong>，输入好对应信息，确定。</p>
<p>点击PicGo设置中的 <strong>设置Server</strong> 设置监听地址为 127.0.0.1，监听端口为36677。（这个一般都设置好了）</p>
<h2 id="3-Typora"><a href="#3-Typora" class="headerlink" title="3.Typora"></a>3.Typora</h2><p>Ctrl + ，进入设置，选择图像</p>
<p><img src="https://raw.githubusercontent.com/MgL02/MyPicGo/main/images/Typora%E5%9B%BE%E5%BA%8A%E8%AE%BE%E7%BD%AE.png" alt="img"></p>
<p>上传服务选择PicGo。路径选择你的安装路径。</p>
<h2 id="4-上传失败"><a href="#4-上传失败" class="headerlink" title="4.上传失败"></a>4.上传失败</h2><p>重启PicGo。</p>
]]></content>
      <tags>
        <tag>图床</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux学习笔记</title>
    <url>/2021/11/25/Linux%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="Linux学习笔记"><a href="#Linux学习笔记" class="headerlink" title="Linux学习笔记"></a>Linux学习笔记</h1><p>写在前面</p>
<p>善用tab 补全命令</p>
<p>Ctrl+C：中断命令</p>
<p>Ctrl+D：直接离开界面（相当于exit）</p>
<p>startx：进入图形化界面（前提：安装了图形化界面，如KDE、GNOME）</p>
<p>退出图形化界面：init 数字（1-6），Ctrl + Alt + F2 - F6</p>
<p>man 命令名：查看命令使用方法</p>
<p>man page 与 info page：求助命令，也就是查看命令信息</p>
<p>vi：i a o 插入（即编辑模式），q 退出，w 保存，esc 退出编辑</p>
<p>date：查看时间日期</p>
<p>cal：显示日历</p>
<p>bc：打开计算器</p>
<p>su 其他账号：进入其他账号（即限root用户）</p>
<h2 id="1-文件命令"><a href="#1-文件命令" class="headerlink" title="1.文件命令"></a>1.文件命令</h2><p>vi（vim）：创建文件并进入文件进行编辑（属于vi编辑器的范围）</p>
<p>cd：进入某个目录</p>
<p>ls：显示当前目录下的除隐藏文件外的所有文件</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ls [-选项] [filename|directory]</span><br><span class="line">-a:显示指定目录下所有子目录和文件，包括隐藏文件</span><br><span class="line">-c:按文件修改时间排序</span><br><span class="line">-d:若参数是目录，则只显示其名称不显示其下的各文件</span><br><span class="line">-i:在输出的第一列显示文件的i节点</span><br><span class="line">-l:以长格式显示文件详细信息</span><br></pre></td></tr></table></figure>

<p>mkdir：创建文件目录（为蓝色），相当于文件夹</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdir [-选项] dirname</span><br><span class="line">-m:对新建目录设置存取权限，也可通过chmod来设置</span><br><span class="line">-p:可以一次建立多个目录，如mkdir -p dir1/dir2/dir3将依次建立dir1、dir2、dir3三个目录</span><br></pre></td></tr></table></figure>

<p>rmdir：删除文件目录，相当于删除文件夹</p>
<p>pwd：显示当前所在路径</p>
<p>cp：复制文件或目录（会增加文件个数）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cp [-选项] sourcefilename | directorydestfilename | dirctory</span><br><span class="line">-a:整个目录复制</span><br><span class="line">-f:覆盖已存在的目标文件且不提示</span><br><span class="line">-i:覆盖已存在的目标文件前询问用户是否覆盖(交互式覆盖)</span><br><span class="line">-p:除了复制文件内容，还复制源文件的修改时间和文件权限</span><br><span class="line">-R:源文件是目录时，，递归复制该目录下的子目录和文件</span><br><span class="line">-l:不复制，只作为链接文件</span><br></pre></td></tr></table></figure>

<p>mv：移动文件或目录，可以用来更改文件或目录名字</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mv [-选项] sourcefilename | directorydestfilename | directory</span><br><span class="line">-i:交互式操作，若存在与目标文件重名的文件会导致覆盖，系统会询问是否覆盖</span><br><span class="line">-f:禁止交互操作，无论是否有与目标文件重名的文件，都直接覆盖</span><br></pre></td></tr></table></figure>

<p>rm：删除文件或目录</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">rm [-选项] filename | directory···</span><br><span class="line">-f:忽略不存在的文件，从不给出提示</span><br><span class="line">-r:指示rm将参数中列出的全部目录和子目录均递归删除掉</span><br><span class="line">-i:进行交互式删除</span><br></pre></td></tr></table></figure>

<p>rm -rf * 会删除系统内的文件（删库跑路专属）</p>
<p>cat：不打开文件直接显示文件的内容（即不使用vi命令即可看到内容）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cat [-选项] filename [filename2 ···]</span><br><span class="line">-n:由1开始编号</span><br><span class="line">-b:由1开始编号，空白行除外</span><br><span class="line">-s:将两个空白行替换为一个空白行</span><br><span class="line">-v:显示非打印文本</span><br></pre></td></tr></table></figure>

<p>tac：cat的相反命令，用来逆序显示文件内容</p>
<p>more：若文件内容过长，将文件内容分屏显示，按空格键显示下一页内容</p>
<p>less：与more相似，允许用户向前或向后查看文件，按B键向前翻阅，按P键向后翻阅，输入百分比显示指定位置，按Q键退出查看</p>
<p>chmod： 修改文件权限（读 写 执行r w x）</p>
<table>
<thead>
<tr>
<th></th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<p>chown： 修改文件拥有者</p>
<p>chgrp： 修改文件所属群组</p>
<h2 id="2-用户和组管理命令"><a href="#2-用户和组管理命令" class="headerlink" title="2.用户和组管理命令"></a>2.用户和组管理命令</h2><h3 id="2-1用户管理"><a href="#2-1用户管理" class="headerlink" title="2.1用户管理"></a>2.1用户管理</h3><p>adduser：添加用户</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">adduser [-选项] username</span><br><span class="line">-d:指定用于取代默认/home/username的用户主目录</span><br><span class="line">-g:用户所属的用户组的组名或组id（指定前需存在这个组）</span><br><span class="line">-m:若指定用户主目录不存在则创建</span><br><span class="line">-p:使用crypt加密的口令</span><br><span class="line">-s:指定用户登录shell，默认为/bin/bash</span><br><span class="line">-u uid:指定用户的UID，必须唯一且不小于500</span><br></pre></td></tr></table></figure>

<p>passwd：修改用户的口令</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">passwd [-选项] [username]</span><br><span class="line">-l:锁定口令，即禁用账号</span><br><span class="line">-u:口令解锁</span><br><span class="line">-d:使账号无口令</span><br><span class="line">-f:强迫用户下次登录修改口令</span><br><span class="line">若不指定用户，默认修改当前用户的口令</span><br></pre></td></tr></table></figure>

<p>userdel：删除用户（将&#x2F;etc&#x2F;passwd等系统文件中该用户的记录删除）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">userdel [-选项] username</span><br><span class="line">-r:把用户的主目录一起删除</span><br></pre></td></tr></table></figure>

<p>完全删除一个用户包括：</p>
<p>​	删除&#x2F;etc&#x2F;passwd文件中该用户的记录</p>
<p>​	删除&#x2F;etc&#x2F;group文件中该用户的信息</p>
<p>​	删除用户的主目录</p>
<p>​	删除该用户创建的或属于该用户的文件</p>
<p>su：改变身份</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">su [username]</span><br><span class="line">不指定用户名，默认切换到root</span><br></pre></td></tr></table></figure>

<h3 id="2-2用户组管理"><a href="#2-2用户组管理" class="headerlink" title="2.2用户组管理"></a>2.2用户组管理</h3><p>groupadd：添加用户组</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">groupadd [-选项] username</span><br><span class="line">-g GID:指定用户组的GID，唯一且不小于500</span><br><span class="line">-r:创建GID小于500的系统用户组</span><br><span class="line">-f:若用户组存在，退出并显示错误（原用户组不发生改变）</span><br></pre></td></tr></table></figure>

<p>groupdel:删除用户组</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">注意：</span><br><span class="line">组中用户文件不能自行删除，不能改变文件所属的组</span><br><span class="line">若组是用户的基本组，则这个组无法删除</span><br><span class="line">若组中有用户在系统中处于登录状态不能删除该组，需删除用户后在删除组</span><br></pre></td></tr></table></figure>

<p>groupmod：修改用户组属性</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">gtoupmod [-选项] groupname</span><br><span class="line">-g:为用户组指定新的组标识号</span><br><span class="line">-n:将用户组的名字改为新名字</span><br></pre></td></tr></table></figure>

<p>为用户组添加用户：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">将一个用户添加到一个组内：</span><br><span class="line">通过编辑/etc/group文件，将用户名写到组名后面来实现，有多个用户名使用逗号分隔</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title></title>
    <url>/2021/12/11/%E9%81%AE%E7%BD%A9%E7%9A%84%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<h1 id="遮罩的应用"><a href="#遮罩的应用" class="headerlink" title="遮罩的应用"></a>遮罩的应用</h1><h2 id="遮罩"><a href="#遮罩" class="headerlink" title="遮罩"></a>遮罩</h2><p>遮罩是什么？</p>
<p>遮罩（Matte）即遮挡、遮盖，遮挡部分图像内容，并显示特定区域的图像内容，相当于一个窗口，遮罩是作为一个单独的图层存在的，并且通常是上对下遮挡的关系。</p>
<p>遮罩的作用：</p>
<p>​	①用在整个场景或一个特定区域，使场景外的对象或特定区域外的对象不可见</p>
<p>​	②用来遮罩住某一元件的一部分，从而实现一些特殊的效果。</p>
<h2 id="三种制作遮罩的方法"><a href="#三种制作遮罩的方法" class="headerlink" title="三种制作遮罩的方法"></a>三种制作遮罩的方法</h2><p>1.剪辑面板</p>
<p>将一段视频素材放入时间线并设置合成模式为前景，新建一个字幕条并设置合成模式为Alpha</p>
<p>2.Fusion面板</p>
<p>创建一个文本节点</p>
<p>将该节点的合成模式设置为Alpha，与mediain1的Alpha入口相连接</p>
<p>3.调色面板</p>
<p>先创建一个Alpha输出，选择遮罩形状，连接Alpha输出。</p>
<p>这个遮罩的形状选择调色面板的窗口可以自由选择，其中钢笔工具的自由度更高</p>
<h2 id="调色面板"><a href="#调色面板" class="headerlink" title="调色面板"></a>调色面板</h2><p>调色三个重要参数：色相、明度、饱和度</p>
<p>示波器的使用</p>
<p>分量图、波形图、矢量图、直方图、CIE色度图</p>
]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2021/12/24/%E9%9D%99%E6%80%81%E8%B7%AF%E7%94%B1%E5%92%8C%E5%8A%A8%E6%80%81%E8%B7%AF%E7%94%B1%E7%9A%84%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="静态路由和动态路由的配置"><a href="#静态路由和动态路由的配置" class="headerlink" title="静态路由和动态路由的配置"></a>静态路由和动态路由的配置</h1><h2 id="1-准备工作"><a href="#1-准备工作" class="headerlink" title="1.准备工作"></a>1.准备工作</h2><p>首先，选择三个2811型号的路由器和两个主机。</p>
<p>对三个路由器分别添加 WIC-2T 模块（添加前先将路由器关机，添加后开机）</p>
<p>将各个组件相连（使用自动选择连接）</p>
<p>为PC0、PC1分配IP地址，分别为10.1.1.2、40.1.1.2，子网掩码均改为255.255.255.0</p>
<p>为各个路由器的接口配置IP地址如下：</p>
<p>Router0:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int f0/0</span><br><span class="line">ip addr 10.1.1.1</span><br><span class="line">no shutdown</span><br><span class="line">int s0/0/0</span><br><span class="line">ip addr 20.1.1.1</span><br><span class="line">no shutdown</span><br></pre></td></tr></table></figure>
<p>Router1:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int s0/0/0</span><br><span class="line">ip addr 20.1.1.2</span><br><span class="line">no shutdown</span><br><span class="line">int s0/0/1</span><br><span class="line">ip addr 30.1.1.1</span><br><span class="line">no shutdown</span><br></pre></td></tr></table></figure>
<p>Router2:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int s0/0/0</span><br><span class="line">ip addr 30.1.1.2</span><br><span class="line">no shutdown</span><br><span class="line">int f0/0</span><br><span class="line">ip addr 40.1.1.1</span><br><span class="line">no shutdown</span><br></pre></td></tr></table></figure>

<h2 id="2、静态路由配置"><a href="#2、静态路由配置" class="headerlink" title="2、静态路由配置"></a>2、静态路由配置</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ip route 目标IP地址 目标IP地址子网掩码 下一跳IP地址（可使用接口代替）距离x（可选）</span><br></pre></td></tr></table></figure>

<p>Router0:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ip route 40.1.1.0 255.255.255.0 s0/0/0 180</span><br></pre></td></tr></table></figure>
<p>Router1:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ip route 40.1.1.0 255.255.255.0 s0/0/1 180</span><br><span class="line">ip route 10.1.1.0 255.255.255.0 s0/0/0 180</span><br></pre></td></tr></table></figure>
<p>Router2:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ip route 10.1.1.0 255.255.255.0 s0/0/0 180</span><br></pre></td></tr></table></figure>

<p>此时开始在一台主机ping目的主机。</p>
<h2 id="3、动态路由配置"><a href="#3、动态路由配置" class="headerlink" title="3、动态路由配置"></a>3、动态路由配置</h2><h3 id="3-1-rip配置"><a href="#3-1-rip配置" class="headerlink" title="3.1 rip配置"></a>3.1 rip配置</h3><p>格式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route rip//选择需要配置的协议</span><br><span class="line">version 2//选择协议版本</span><br><span class="line">net 邻居网络//选择邻居之一</span><br><span class="line">net 邻居网络</span><br></pre></td></tr></table></figure>

<p>每个路由器都是如此。</p>
<p>Router0的邻居网络为10.1.1.0、20.1.1.0</p>
<p>Router1的邻居网络为20.1.1.0、30.1.1.0</p>
<p>Router2的邻居网络为30.1.1.0、40.1.1.0</p>
<p>则配置如下：</p>
<p>Router0</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route rip</span><br><span class="line">ver 2</span><br><span class="line">net 10.1.1.0</span><br><span class="line">net 20.1.1.0</span><br></pre></td></tr></table></figure>
<p>Router1</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route rip</span><br><span class="line">ver 2</span><br><span class="line">net 20.1.1.0</span><br><span class="line">net 30.1.1.0</span><br></pre></td></tr></table></figure>
<p>Router2</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route rip</span><br><span class="line">ver 2</span><br><span class="line">net 30.1.1.0</span><br><span class="line">net 40.1.1.0</span><br></pre></td></tr></table></figure>

<p>3.2 ospf配置</p>
<p>格式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">router ospf 1//选择协议和版本</span><br><span class="line">net 邻居网络 该网络子网掩码的反码 网络所属区域</span><br><span class="line">net 邻居网络 该网络子网掩码的反码 网络所属区域</span><br></pre></td></tr></table></figure>

<p>配置如下：</p>
<p>Router0</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route ospf 1</span><br><span class="line">net 10.1.1.0 0.0.0.255 area 0</span><br><span class="line">net 20.1.1.0 0.0.0.255 area 0</span><br></pre></td></tr></table></figure>
<p>Router1</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route ospf 1</span><br><span class="line">net 20.1.1.0 0.0.0.255 area 0</span><br><span class="line">net 30.1.1.0 0.0.0.255 area 0</span><br></pre></td></tr></table></figure>
<p>Router2</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">route ospf 1</span><br><span class="line">net 30.1.1.0 0.0.0.255 area 0</span><br><span class="line">net 40.1.1.0 0.0.0.255 area 0</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2021/12/30/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<h1 id="计算机系统概述"><a href="#计算机系统概述" class="headerlink" title="计算机系统概述"></a>计算机系统概述</h1><h2 id="1-计算机系统概述"><a href="#1-计算机系统概述" class="headerlink" title="1.计算机系统概述"></a>1.计算机系统概述</h2><h3 id="1-1计算机基本工作原理"><a href="#1-1计算机基本工作原理" class="headerlink" title="1.1计算机基本工作原理"></a>1.1计算机基本工作原理</h3><h4 id="1-1-1-冯诺依曼结构思想"><a href="#1-1-1-冯诺依曼结构思想" class="headerlink" title="1.1.1 冯诺依曼结构思想"></a>1.1.1 冯诺依曼结构思想</h4><p>①采用“存储程序”工作方式</p>
<p>②五个基本部件：控制器、运算器、存储器、输入设备、输出设备</p>
<p>③控制器：自动执行指令</p>
<p>​	运算器：进行算术运算和逻辑运算</p>
<p>​	存储器：存放数据和指令（都是二进制形式）</p>
<p>​	输入输出设备：操作人员通过输入输出设备使用计算机</p>
<p>④指令和数据：二进制形式</p>
<p>​	指令组成：操作码（操作类型）和地址码（操作数地址）</p>
<p>​	程序：一串指令组成</p>
<h4 id="1-1-2冯诺依曼机基本结构"><a href="#1-1-2冯诺依曼机基本结构" class="headerlink" title="1.1.2冯诺依曼机基本结构"></a>1.1.2冯诺依曼机基本结构</h4><p>主存（内存）：存放指令和数据的主存储器</p>
<p>算术逻辑部件（Arithmetic Logic Unit，ALU）：进行算数逻辑运算的部件</p>
<p>控制部件（控制器，Control Unit，CU）：自动逐条取出指令并进行译码的部件</p>
<p>指令寄存器（Instruction Register，IR）：暂时存放从主存中取出的指令</p>
<p>程序计数器（Program Count，PC）：存放下一条指令的存放地址，实现按序读取主存中的指令</p>
<p>中央处理器（Central Processing Unit，CPU）：控制部件、运算部件和各类寄存器互连组成的电路</p>
<p>总线：连接不同部件进行信息传输的介质。包含 地址线、控制线、数据线。</p>
<p>主存地址寄存器（Memory Address Register，MAR）：存放CPU送到地址线的注册地址</p>
<p>主存数据寄存器（Memory Data Register，MDR）：存放发送到或从数据线取来的信息</p>
<h4 id="1-1-3程序和指令的执行过程"><a href="#1-1-3程序和指令的执行过程" class="headerlink" title="1.1.3程序和指令的执行过程"></a>1.1.3程序和指令的执行过程</h4><p>指令执行过程： 1. 取指令</p>
<p>​							2. 取数</p>
<p>​							3. 存数</p>
<p>​							4. ALU运算</p>
<p>一条指令的执行时间包含一个时钟周期或多个时钟周期</p>
<h3 id="1-2程序的开发与运行"><a href="#1-2程序的开发与运行" class="headerlink" title="1.2程序的开发与运行"></a>1.2程序的开发与运行</h3><h4 id="1-2-1程序设计语言和翻译程序"><a href="#1-2-1程序设计语言和翻译程序" class="headerlink" title="1.2.1程序设计语言和翻译程序"></a>1.2.1程序设计语言和翻译程序</h4><p>机器语言：使用计算机规定的指令格式而形成的0&#x2F;1序列（即二进制序列）</p>
<p>机器代码（机器语言程序）：计算机能够理解并执行的程序</p>
<p>机器指令：机器程序中的指令由0&#x2F;1序列组成</p>
<p>汇编语言：机器语言的符号表示，简短英文符号与机器指令建立一一对应关系</p>
<p>汇编指令：机器指令对应的符号表示</p>
<p>汇编语言程序：使用汇编语言编写的程序</p>
<p>机器级语言：由与特定的机器结构相关的指令构成的语言（如机器语言、汇编语言）</p>
<p><img src="https://raw.githubusercontent.com/MgL02/MyPicGo/main/images/%E8%AE%A1%E7%BB%84%E7%A8%8B%E5%BA%8F%E8%BD%AC%E6%8D%A2.jpg"></p>
<h2 id="2-数据的机器级表示与处理"><a href="#2-数据的机器级表示与处理" class="headerlink" title="2.数据的机器级表示与处理"></a>2.数据的机器级表示与处理</h2><h2 id="3-程序的转换及机器级表示"><a href="#3-程序的转换及机器级表示" class="headerlink" title="3.程序的转换及机器级表示"></a>3.程序的转换及机器级表示</h2><h2 id="4-程序的链接"><a href="#4-程序的链接" class="headerlink" title="4.程序的链接"></a>4.程序的链接</h2>]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2022/03/04/2022-3-4-%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h1 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h1><h2 id="一、计算机操作系统概述"><a href="#一、计算机操作系统概述" class="headerlink" title="一、计算机操作系统概述"></a>一、计算机操作系统概述</h2><h3 id="1-1-计算机系统"><a href="#1-1-计算机系统" class="headerlink" title="1.1 计算机系统"></a>1.1 计算机系统</h3><h4 id="1-1-1-计算机系统概述"><a href="#1-1-1-计算机系统概述" class="headerlink" title="1.1.1 计算机系统概述"></a>1.1.1 计算机系统概述</h4><h5 id="1-电子计算机"><a href="#1-电子计算机" class="headerlink" title="1.电子计算机"></a>1.电子计算机</h5><p>1946年，第一台电子计算机ENIAC，美国。</p>
<h5 id="2-计算机技术的发展"><a href="#2-计算机技术的发展" class="headerlink" title="2.计算机技术的发展"></a>2.计算机技术的发展</h5><p>①第一代电子管计算机（1946-1957）</p>
<p>②第二代晶体管计算机（1958-1964年）</p>
<p>③第三代集成电路计算机（1965-1977）</p>
<p>④第四代大规模与超大规模集成电路计算机（1978年至今）</p>
<h5 id="3-计算机系统的组成"><a href="#3-计算机系统的组成" class="headerlink" title="3.计算机系统的组成"></a>3.计算机系统的组成</h5><p>现代计算机系统：硬件与软件组成。硬件是软件运行的物理基础。软件能够充分发挥硬件潜能并扩充硬件功能，共同完成各种应用任务。</p>
<p>计算机硬件系统：各种物理部件的有机结合，计算机系统的工作载体。</p>
<p>计算机软件系统：各类程序和文件，指挥计算机系统按指定要求进行协同工作。（包括系统软件、支持软件、应用软件）关键系统软件：操作系统、语言处理程序。</p>
<h4 id="1-1-2-计算机硬件系统"><a href="#1-1-2-计算机硬件系统" class="headerlink" title="1.1.2 计算机硬件系统"></a>1.1.2 计算机硬件系统</h4><h5 id="1-计算机硬件系统的组成"><a href="#1-计算机硬件系统的组成" class="headerlink" title="1.计算机硬件系统的组成"></a>1.计算机硬件系统的组成</h5><p>中央处理器（运算单元和控制单元）、主存储器、外围设备（输入设备、输出设备、存储设备、通信设备），通过系统总线连接。</p>
<p>运算单元：执行具体的机器指令的运算。控制单元：解释机器指令。</p>
<p>主存：存储正在执行的程序和数据。</p>
<h5 id="2-冯·诺依曼模型"><a href="#2-冯·诺依曼模型" class="headerlink" title="2.冯·诺依曼模型"></a>2.冯·诺依曼模型</h5><p>五个组成部分：运算器、控制器、存储器、输入设备、输出设备</p>
<p>特点：</p>
<p>①运算单元为中心，控制流由指令流产生</p>
<p>②存储程序原理，面向主存组织数据流</p>
<p>③主存按地址访问，提供线性编址空间</p>
<p>④操作码与地址码组成指令</p>
<p>⑤数据以二进制编码</p>
<h5 id="3-计算机总线与网络总线"><a href="#3-计算机总线与网络总线" class="headerlink" title="3.计算机总线与网络总线"></a>3.计算机总线与网络总线</h5><p>总线：计算机各种功能部件之间传送信息的公共通信干线。</p>
<p>按传输信息分类：控制线、数据线、地址线</p>
<p>三类总线：</p>
<p>①内部总线：CPU内部各元件的连接</p>
<p>②系统总线：连接CPU、存储器和各个I&#x2F;O模块</p>
<p>③通信总线：计算机系统之间的通信</p>
<h2 id="二、处理器管理"><a href="#二、处理器管理" class="headerlink" title="二、处理器管理"></a>二、处理器管理</h2><h2 id="三、存储管理"><a href="#三、存储管理" class="headerlink" title="三、存储管理"></a>三、存储管理</h2><span id="more"></span>]]></content>
  </entry>
  <entry>
    <title>C++学习笔记</title>
    <url>/2025/03/07/C++/</url>
    <content><![CDATA[<h1 id="C"><a href="#C" class="headerlink" title="C++"></a>C++</h1><h2 id="指针"><a href="#指针" class="headerlink" title="指针"></a>指针</h2><p>1.悬空指针</p>
<p>定义：指针被释放后，还指向原来的内存空间。如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> *p = <span class="built_in">malloc</span>(size);</span><br><span class="line"><span class="built_in">assert</span>(p);</span><br><span class="line"><span class="built_in">free</span>(p);</span><br><span class="line"><span class="comment">//此时p为悬空指针</span></span><br></pre></td></tr></table></figure>

<p>避免方法：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> *p = <span class="built_in">malloc</span>(size);</span><br><span class="line"><span class="built_in">assert</span>(p);</span><br><span class="line"><span class="built_in">free</span>(p);</span><br><span class="line">p = <span class="literal">NULL</span>;<span class="comment">//赋值为NULL来避免悬空指针</span></span><br></pre></td></tr></table></figure>

<p>2.野指针</p>
<p>定义：指针不确定其具体指向的内存空间。如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> *p;</span><br><span class="line"><span class="comment">//此时p为野指针</span></span><br></pre></td></tr></table></figure>

<p>危害：可能指向任意内存段，因此它可能会损坏正常的数据，也有可能引发其他未知错误。</p>
<p>避免方法：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> *p = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure>



<h2 id="重载"><a href="#重载" class="headerlink" title="重载"></a>重载</h2><h3 id="运算符重载"><a href="#运算符重载" class="headerlink" title="运算符重载"></a>运算符重载</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">函数类型 <span class="keyword">operator</span>运算符(形参列表)</span><br><span class="line">&#123;</span><br><span class="line">    重载语句;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>operator</code>为关键字，专门用于定义运算符重载的函数。可以将 <strong>operator运算符</strong> 看成函数名称。</p>
<h4 id="运算符重载规则"><a href="#运算符重载规则" class="headerlink" title="运算符重载规则"></a>运算符重载规则</h4><p>1.并不是所有的运算符都能被重载，如长度运算符<code>sizeof</code>、条件运算符<code>: ?</code>（三元运算符）、成员选择符<code>.</code>和域解析运算符<code>::</code>不能被重载。</p>
<p>2.重载不能改变运算符的优先级和结合性。</p>
<p>3.重载不改变运算符的用法（即不改变原先的使用规则）。</p>
<p>4.运算符重载函数<strong>不能有默认参数</strong>，因为这会改变运算符操作数个数。</p>
<p>5.运算符重载函数既可以作为<strong>类的成员函数</strong>，也可以作为<strong>全局函数</strong>。</p>
<p>将运算符重载函数作为类的成员函数时，二元运算符的参数只有一个，一元运算符不需要参数。之所以少一个参数，是因为这个参数是隐含的。当类的对象调用重载后的运算符时，此对象就隐形作为一个参数了。</p>
<p>将运算符重载函数作为全局函数时，二元操作符就需要两个参数，一元操作符需要一个参数，<strong>而且其中必须有一个参数是对象</strong>，好让编译器区分这是程序员自定义的运算符，防止程序员修改用于内置类型的运算符的性质。</p>
<p>6.箭头运算符<code>-&gt;</code>、下标运算符<code>[ ]</code>、函数调用运算符<code>( )</code>、赋值运算符<code>=</code>只能以成员函数的形式重载。</p>
<h4 id="流操作符重载"><a href="#流操作符重载" class="headerlink" title="流操作符重载"></a>流操作符重载</h4><p>输入流操作符<code>&gt;&gt;</code>和输出流操作符<code>&lt;&lt;</code>。</p>
<p><code>cout</code>是<code>ostream类</code>对象，<code>cin</code>是<code>iostream类</code>对象。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//输入流操作符&gt;&gt;重载</span></span><br><span class="line">istream &amp; <span class="keyword">operator</span>&gt;&gt;(istream &amp;in, 类型 &amp;A)</span><br><span class="line">&#123;</span><br><span class="line">    in &gt;&gt; A.成员<span class="number">1</span> &gt;&gt; A.成员<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">return</span> in;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//返回 istream 类对象的引用，是为了能够连续读取复数，让代码书写更加漂亮</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//输出流操作符&lt;&lt;重载</span></span><br><span class="line">ostream &amp; <span class="keyword">operator</span>&lt;&lt;(ostream &amp;out, 类型 &amp;A)</span><br><span class="line">&#123;</span><br><span class="line">    out &lt;&lt; A.成员<span class="number">1</span> &lt;&lt; A.成员<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">return</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="下标运算符-重载"><a href="#下标运算符-重载" class="headerlink" title="下标运算符[]重载"></a>下标运算符<code>[]</code>重载</h4><p>只能以成员函数的形式进行重载，声明格式一般为：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//第一种方式:不仅可以访问元素，还可以修改元素。</span></span><br><span class="line">返回值类型 &amp; <span class="keyword">operator</span>[](形参);</span><br><span class="line"><span class="comment">//第二种方式:只能访问而不能修改元素</span></span><br><span class="line"><span class="type">const</span> 返回值类型 &amp; <span class="keyword">operator</span>[](形参) <span class="type">const</span>;</span><br></pre></td></tr></table></figure>

<p>可以通过重载<code>[]</code>来实现变长数组。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Array</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Array</span>(<span class="type">int</span> length = <span class="number">0</span>);</span><br><span class="line">    ~<span class="built_in">Array</span>();</span><br><span class="line">    <span class="type">int</span> &amp; <span class="keyword">operator</span>[](<span class="type">int</span> i);</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> &amp; <span class="keyword">operator</span>[](<span class="type">int</span> i) <span class="type">const</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_length;  <span class="comment">//数组长度</span></span><br><span class="line">    <span class="type">int</span> *m_p;  <span class="comment">//指向数组内存的指针</span></span><br><span class="line">&#125;;</span><br><span class="line">Array::~<span class="built_in">Array</span>()&#123;</span><br><span class="line">    <span class="keyword">delete</span>[] m_p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span>&amp; Array::<span class="keyword">operator</span>[](<span class="type">int</span> i)&#123;</span><br><span class="line">    <span class="keyword">return</span> m_p[i];</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp; Array::<span class="keyword">operator</span>[](<span class="type">int</span> i) <span class="type">const</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> m_p[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="操作符重载"><a href="#操作符重载" class="headerlink" title="操作符重载"></a>操作符重载</h3><p>（1）流操作符重载</p>
<p>必须作为友元函数（关键字：<strong>friend</strong>）或普通全局函数来重载。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">std:ostream&amp; <span class="keyword">operator</span>&lt;&lt;(std::ostream&amp; os,<span class="type">const</span> className&amp; object)&#123;</span><br><span class="line">    os &lt;&lt; object的内容;</span><br><span class="line">    <span class="keyword">return</span> os;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">std:istream&amp; <span class="keyword">operator</span>&gt;&gt;(std::istream&amp; is,className&amp; object)&#123;</span><br><span class="line">    os &gt;&gt; object的内容;</span><br><span class="line">    <span class="keyword">return</span> is;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>（2）一元运算符重载（++、–、-、!）</p>
<p>前缀形式重载调用 <strong>operator ++ ()</strong> ，后缀形式重载调用 <strong>operator ++ (int)</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重载前缀递增运算符（ ++ ）</span></span><br><span class="line">      Time <span class="keyword">operator</span>++ ()  </span><br><span class="line">      &#123;</span><br><span class="line">         ++minutes;          <span class="comment">// 对象加 1</span></span><br><span class="line">         <span class="keyword">if</span>(minutes &gt;= <span class="number">60</span>)  </span><br><span class="line">         &#123;</span><br><span class="line">            ++hours;</span><br><span class="line">            minutes -= <span class="number">60</span>;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> <span class="built_in">Time</span>(hours, minutes);</span><br><span class="line">      &#125;</span><br><span class="line"><span class="comment">// 重载后缀递增运算符（ ++ ）</span></span><br><span class="line">      Time <span class="keyword">operator</span>++( <span class="type">int</span> )         </span><br><span class="line">      &#123;</span><br><span class="line">         <span class="comment">// 保存原始值</span></span><br><span class="line">         <span class="function">Time <span class="title">T</span><span class="params">(hours, minutes)</span></span>;</span><br><span class="line">         <span class="comment">// 对象加 1</span></span><br><span class="line">         ++minutes;                    </span><br><span class="line">         <span class="keyword">if</span>(minutes &gt;= <span class="number">60</span>)</span><br><span class="line">         &#123;</span><br><span class="line">            ++hours;</span><br><span class="line">            minutes -= <span class="number">60</span>;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// 返回旧的原始值</span></span><br><span class="line">         <span class="keyword">return</span> T; </span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<p>（3）二元运算符重载（+、-、*、&#x2F;）</p>
<p>类成员函数或全局函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 类成员函数重载 + 运算符，用于把两个 Box 对象相加</span></span><br><span class="line">   Box <span class="keyword">operator</span>+(<span class="type">const</span> Box&amp; b)</span><br><span class="line">   &#123;</span><br><span class="line">      Box box;</span><br><span class="line">      box.length = <span class="keyword">this</span>-&gt;length + b.length;</span><br><span class="line">      box.breadth = <span class="keyword">this</span>-&gt;breadth + b.breadth;</span><br><span class="line">      box.height = <span class="keyword">this</span>-&gt;height + b.height;</span><br><span class="line">      <span class="keyword">return</span> box;</span><br><span class="line">   &#125;</span><br><span class="line"><span class="comment">//其他运算符类似</span></span><br><span class="line"><span class="comment">//全局函数重载</span></span><br><span class="line">Box <span class="keyword">operator</span>+(<span class="type">const</span> Box&amp; a, <span class="type">const</span> Box&amp; b)</span><br><span class="line">   &#123;</span><br><span class="line">      Box box;</span><br><span class="line">      box.length = a.length + b.length;</span><br><span class="line">      box.breadth = a.breadth + b.breadth;</span><br><span class="line">      box.height = a.height + b.height;</span><br><span class="line">      <span class="keyword">return</span> box;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>（4）赋值运算符</p>
<ul>
<li>参数类型：引用传参，用const修饰，即const 类&amp;<br>引用传参可以提高传参效率。</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="keyword">operator</span>=(<span class="type">const</span> Distance &amp;D )</span><br><span class="line">      &#123; </span><br><span class="line">         feet = D.feet;</span><br><span class="line">         inches = D.inches;</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>返回值类型：引用返回，即 类&amp;<br>引用返回可以提高返回的效率，有返回值目的是为了支持连续赋值功能。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Data <span class="keyword">operator</span>=(<span class="type">const</span> Data&amp; d)<span class="comment">//可以提高效率</span></span><br><span class="line">	&#123;</span><br><span class="line">			_year = d._year;</span><br><span class="line">			_month = d._month;</span><br><span class="line">			_day = d._day;</span><br><span class="line">			<span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>


</li>
<li><p>要检查是否给自己赋值</p>
</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Data <span class="keyword">operator</span>=(<span class="type">const</span> Data&amp; d)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">this</span> != &amp;d)<span class="comment">//如果两个对象的地址不相同那么就可以进行赋值</span></span><br><span class="line">	&#123;</span><br><span class="line">		_year = d._year;</span><br><span class="line">		_month = d._month;</span><br><span class="line">		_day = d._day;</span><br><span class="line">		<span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>返回*this：要符合连续赋值的含义。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Data&amp; <span class="keyword">operator</span>=(<span class="type">const</span> Data&amp; d)<span class="comment">//用引用返回，可以提高效率,减少拷贝</span></span><br><span class="line">	&#123;</span><br><span class="line">			_year = d._year;</span><br><span class="line">			_month = d._month;</span><br><span class="line">			_day = d._day;</span><br><span class="line">			<span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>（5）函数调用运算符()重载</p>
<p>函数调用运算符 () 可以被重载用于类的对象。当重载 () 时，您不是创造了一种新的调用函数的方式，相反地，这是创建一个可以传递任意数目参数的运算符函数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重载函数调用运算符</span></span><br><span class="line">      <span class="function">Distance <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> c)</span></span></span><br><span class="line"><span class="function">      </span>&#123;</span><br><span class="line">         Distance D;</span><br><span class="line">         <span class="comment">// 进行随机计算</span></span><br><span class="line">         D.feet = a + c + <span class="number">10</span>;</span><br><span class="line">         D.inches = b + c + <span class="number">100</span> ;</span><br><span class="line">         <span class="keyword">return</span> D;</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<p>（6）下标运算符[]重载</p>
<p>下标操作符 [] 通常用于访问数组元素。重载该运算符用于增强操作 C++ 数组的功能。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>&amp; <span class="keyword">operator</span>[](<span class="type">int</span> i)</span><br><span class="line">      &#123;</span><br><span class="line">          <span class="keyword">if</span>( i &gt;= SIZE )</span><br><span class="line">          &#123;</span><br><span class="line">              cout &lt;&lt; <span class="string">&quot;索引超过最大值&quot;</span> &lt;&lt;endl; </span><br><span class="line">              <span class="comment">// 返回第一个元素</span></span><br><span class="line">              <span class="keyword">return</span> arr[<span class="number">0</span>];</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">return</span> arr[i];</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<p>（7）类成员访问运算符-&gt;重载</p>
<p>运算符 -&gt; 通常与指针引用运算符 * 结合使用，用于实现”智能指针”的功能。</p>
<p>语句 p-&gt;m 被解释为 (p.operator-&gt;())-&gt;m</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Obj* <span class="keyword">operator</span>-&gt;() <span class="type">const</span> </span><br><span class="line">   &#123;</span><br><span class="line">     <span class="keyword">if</span>(!oc.a[index])</span><br><span class="line">     &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Zero value&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> (Obj*)<span class="number">0</span>;</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="keyword">return</span> oc.a[index];</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>



<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><p>引用必须在声明时初始化，初始化后无法改变指向。</p>
<p><strong>左值、右值的区别：</strong></p>
<p>左值：可以取地址的对象</p>
<p>右值：不可以取地址的对象（如常量、表达式、函数返回值）</p>
<h3 id="左值引用"><a href="#左值引用" class="headerlink" title="左值引用"></a>左值引用</h3><p>就是对左值进行引用</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1.左值引用只能引用左值</span></span><br><span class="line"><span class="type">int</span> t = <span class="number">8</span>;</span><br><span class="line"><span class="type">int</span>&amp; rt1 = t;</span><br><span class="line"><span class="comment">//int&amp; rt2 = 8;  // 编译报错，因为8是右值，不能直接引用右值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2.但是const左值引用既可以引用左值</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; rt3 = t;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; rt4 = <span class="number">8</span>;  <span class="comment">// 也可以引用右值</span></span><br><span class="line"><span class="type">const</span> <span class="type">double</span>&amp; r1 = x + y;</span><br><span class="line"><span class="type">const</span> <span class="type">double</span>&amp; r2 = <span class="built_in">fmin</span>(x, y);</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="右值引用"><a href="#右值引用" class="headerlink" title="右值引用"></a>右值引用</h3><p>就是对右值进行引用</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1.右值引用只能引用右值</span></span><br><span class="line"><span class="type">int</span>&amp;&amp; rr1 = <span class="number">10</span>;</span><br><span class="line"><span class="type">double</span>&amp;&amp; rr2 = x + y;</span><br><span class="line"><span class="type">const</span> <span class="type">double</span>&amp;&amp; rr3 = x + y;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> t = <span class="number">10</span>;</span><br><span class="line"><span class="comment">//int&amp;&amp; rrt = t;  // 编译报错，不能直接引用左值</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2.但是右值引用可以引用被move的左值</span></span><br><span class="line"><span class="type">int</span>&amp;&amp; rrt = std::<span class="built_in">move</span>(t);</span><br><span class="line"><span class="type">int</span>*&amp;&amp; rr4 = std::<span class="built_in">move</span>(p);</span><br><span class="line"><span class="type">int</span>&amp;&amp; rr5 = std::<span class="built_in">move</span>(*p);</span><br><span class="line"><span class="type">const</span> <span class="type">int</span>&amp;&amp; rr6 = std::<span class="built_in">move</span>(b);</span><br><span class="line"></span><br><span class="line"><span class="comment">//std::move移动语义：将一个对象中的资源移动到另一个对象（资源控制权的转移）</span></span><br></pre></td></tr></table></figure>

<h2 id="深拷贝与浅拷贝"><a href="#深拷贝与浅拷贝" class="headerlink" title="深拷贝与浅拷贝"></a>深拷贝与浅拷贝</h2><p>浅拷贝是创建一个新对象，新对象和原对象共享同一个底层资源，简单的赋值拷贝。浅拷贝在拷贝后对象共享同一份底层资源，可以提高效率，但是当对象<strong>析构</strong>时可能会出现不确定的行为，因为<strong>资源会被重复释放</strong>。</p>
<p>深拷贝则是创建一个新对象，，在堆中重新分配空间，新对象拥有原对象的全部资源，二者之间互不影响。深拷贝则会为每个对象创建独立的底层资源，避免了这个问题，但是会<strong>占用更多的内存</strong>。</p>
<p>当对象中有<strong>指针指向动态分配的内存</strong>时，为了安全地复制对象，需要显式地实现深拷贝，通常通过重载类的拷贝构造函数和赋值操作符来完成</p>
<h2 id="类与结构体"><a href="#类与结构体" class="headerlink" title="类与结构体"></a>类与结构体</h2><p>C语言中，结构体只是用来封装不同数据类型的数据，没有构造函数和成员函数。</p>
<p>C++中，结构体除了默认权限和继承默认权限不一样外，其他功能与类一样。（结构体默认public，类默认private）</p>
<p>在类定义中的<strong>定义</strong>的函数都是<strong>内联函数</strong>，即使没有使用 <strong>inline</strong> 说明符。</p>
<h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><p>派生类可以访问基类中所有的<strong>非私有</strong>成员。因此基类成员如果不想被派生类的成员函数访问，则应在基类中声明为 private。</p>
<p>一个派生类继承了所有的基类方法，但下列情况除外：</p>
<ul>
<li>基类的构造函数、析构函数和拷贝构造函数。</li>
<li>基类的重载运算符。</li>
<li>基类的友元函数。</li>
</ul>
<h3 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h3><p>C++ 多态意味着调用成员函数时，会根据调用函数的对象的类型来执行不同的函数。</p>
<p>编译时多态、静态多态（静态链接或早绑定）：函数调用在程序执行前就准备好了。有时候这也被称为<strong>早绑定</strong>，因为 area() 函数在程序编译期间就已经设置好了，如<strong>函数重载</strong>和<strong>运算符重载</strong></p>
<p>运行时多态、动态多态（动态链接或后期绑定）：根据所调用的对象类型来选择调用的函数，如派生类中的<strong>虚函数重写</strong></p>
<h4 id="虚函数与纯虚函数"><a href="#虚函数与纯虚函数" class="headerlink" title="虚函数与纯虚函数"></a>虚函数与纯虚函数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> returnType <span class="title">functionName</span><span class="params">()</span></span>&#123;</span><br><span class="line">	···</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//纯虚函数,派生类必须重写纯虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> returnType <span class="title">functionName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>当类中有虚函数时，会为该类生成一个虚函数指针表（虚函数表），同时为该类添加一个虚函数表指针成员（用于访问虚函数表），表中包含一个或多个函数指针，指向该类的虚函数地址</p>
<h3 id="数据抽象"><a href="#数据抽象" class="headerlink" title="数据抽象"></a>数据抽象</h3><p>只向外界提供关键信息，并隐藏其后台的实现细节，即只表现必要的信息而不呈现细节。</p>
<p><strong>优势：</strong></p>
<ul>
<li>类的内部受到保护，不会因无意的用户级错误导致对象状态受损。</li>
<li>类实现可能随着时间的推移而发生变化，以便应对不断变化的需求，或者应对那些要求不改变用户级代码的错误报告。</li>
</ul>
<h3 id="数据封装"><a href="#数据封装" class="headerlink" title="数据封装"></a>数据封装</h3><p>把数据和操作数据的函数捆绑在一起，通过将数据和操作数据的函数封装在一个类中来实现。</p>
<p><strong>访问修饰符</strong></p>
<ul>
<li><strong>private</strong>: 私有成员只能在类的内部访问，不能被类的外部代码直接访问。</li>
<li><strong>public</strong>: 公有成员可以被类的外部代码直接访问。</li>
<li><strong>protected</strong>: 受保护成员可以被类和其派生类访问。</li>
</ul>
<p><strong>优点：</strong></p>
<ul>
<li><strong>数据隐藏</strong>: 通过将数据成员声明为私有，防止外部代码直接访问这些数据。</li>
<li><strong>提高代码可维护性</strong>: 提供公共方法来访问和修改数据，这使得可以在不影响外部代码的情况下修改类的内部实现。</li>
<li><strong>增强安全性</strong>: 防止不合法的数据输入和不当的修改操作。</li>
<li><strong>实现抽象</strong>: 提供了一种机制，使得用户不需要了解类的内部实现细节，只需要了解如何使用类的公共接口即可。</li>
</ul>
<h3 id="接口（抽象类ABC）"><a href="#接口（抽象类ABC）" class="headerlink" title="接口（抽象类ABC）"></a>接口（抽象类ABC）</h3><p>类中<strong>至少</strong>有一个函数被声明为<strong>纯虚函数</strong>，则这个类就是抽象类。纯虚函数是通过在声明中使用 “&#x3D; 0” 来指定的。C++的接口是通过抽象类来实现的，抽象类不能被用于实例化对象，它只能作为<strong>接口</strong>使用。</p>
<p>如果一个 ABC 的子类需要被实例化，则必须实现每个纯虚函数，这也意味着 C++ 支持使用 ABC 声明接口。如果没有在派生类中重写纯虚函数，就尝试实例化该类的对象，会导致编译错误。可用于实例化对象的类被称为<strong>具体类</strong>。</p>
<h3 id="构造函数类"><a href="#构造函数类" class="headerlink" title="构造函数类"></a>构造函数类</h3><p>按参数区分：有参、无参（默认构造函数）</p>
<p>按照类型区分：普通、拷贝(或复制构造)</p>
<p>拷贝构造调用情景：</p>
<p>1.对象以值的形式作为函数参数</p>
<p>2.对象以值的形式作为函数返回值</p>
<p>3.将一个对象用于给另一对象进行初始化时</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span>&#123;</span><br><span class="line">    <span class="comment">//拷贝构造，形参必须为引用，一般会加const</span></span><br><span class="line">    <span class="comment">//一般拷贝构造函数为浅拷贝，当成员变量中存在指针变量时需定义一个深拷贝构造函数</span></span><br><span class="line">    <span class="built_in">Person</span>(<span class="type">const</span> Person &amp;p)&#123;</span><br><span class="line">        <span class="meta">#statement</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h2><h3 id="vector"><a href="#vector" class="headerlink" title="vector"></a>vector</h3>]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux下gdb调试</title>
    <url>/2022/08/16/2022-8-16-Linux%E4%B8%8Bgdb%E8%B0%83%E8%AF%95/</url>
    <content><![CDATA[<h1 id="Linux下gdb调试"><a href="#Linux下gdb调试" class="headerlink" title="Linux下gdb调试"></a>Linux下gdb调试</h1><p>使用<code>gcc infile -o outfile -g</code>可以使得在进行gdb调试时查看源代码</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">gdb 可执行文件名 //进入gdb调试</span><br><span class="line">quit //退出gdb</span><br><span class="line"></span><br><span class="line">set args xxx //设置参数</span><br><span class="line">show args //获取设置参数</span><br><span class="line"></span><br><span class="line">help //获取gdb使用帮助</span><br><span class="line"></span><br><span class="line">//运行gdb程序</span><br><span class="line">start //转到程序第一行</span><br><span class="line">run //执行代码直到遇上断点</span><br><span class="line"></span><br><span class="line">//显示代码</span><br><span class="line">l/list //</span><br><span class="line">l/list 行号/函数名 //</span><br><span class="line">l/list 外部源文件:行号/函数名 //</span><br><span class="line">show list/listsize //显示使用list显示的代码行数</span><br><span class="line">set list/listsize 数字 //设置行数</span><br><span class="line"></span><br><span class="line">//断点</span><br><span class="line">b/break 行号</span><br><span class="line">b/break 函数名</span><br><span class="line">b/break 外部源文件:行号/函数名</span><br><span class="line"></span><br><span class="line">//调试</span><br><span class="line">c/continue //遇上断点时使用，至下一个断点处停</span><br><span class="line">n/next //向下执行一行代码，不会进入函数体</span><br><span class="line">s/step //向下单步调试，会进入函数体</span><br><span class="line">finish //跳出函数体</span><br><span class="line"></span><br><span class="line">//变量操作</span><br><span class="line">p/print 变量名 //打印变量值</span><br><span class="line">ptype 变量名 //打印变量类型</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//自动变量操作</span><br><span class="line">display 变量名 //自动打印指定变量值（回车后）</span><br><span class="line">i/info display //显示display详细信息</span><br><span class="line">undisplay 编号 //删除相关编号的display</span><br><span class="line"></span><br><span class="line">//其他</span><br><span class="line">set var 变量名=变量值 //</span><br><span class="line">until //跳出循环</span><br></pre></td></tr></table></figure>

<span id="more"></span>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>软件测试</title>
    <url>/2022/01/24/2022-1-24-%E8%BD%AF%E4%BB%B6%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<h1 id="软件测试"><a href="#软件测试" class="headerlink" title="软件测试"></a>软件测试</h1><h2 id="1-分类"><a href="#1-分类" class="headerlink" title="1. 分类"></a>1. 分类</h2><p>开发阶段分类：</p>
<ul>
<li>单元测试：对最小模块测试</li>
<li>集成测试：小模块集成后测试</li>
<li>系统测试：所有流程统一的测试，包括功能测试、兼容性测试、性能测试、安全测试</li>
<li>验收测试：上线前的测试，包括<ul>
<li>α测试：内测，开发环境下测试模拟用户测试</li>
<li>β测试：公测，实际环境下用户的测试</li>
</ul>
</li>
</ul>
<p>是否查看代码：</p>
<ul>
<li>白盒测试：查看源代码进行测试，研究产品内部源代码和程序结构，例如单元测试。</li>
<li>黑盒测试（数据驱动测试）：完全查看源代码进行测试，不考虑程序内部结构和内部特性，注重与测试软件的功能需求，只关心软件的输入数据和输出数据。</li>
<li>灰盒测试：只看一部分源代码进行测试，介于白盒与黑盒之间的测试</li>
</ul>
<p>测试执行方式：</p>
<ul>
<li>静态测试：不运行程序，只是检查代码</li>
<li>动态测试：运行程序，检查输入数据与输出数据是否符合逻辑</li>
</ul>
<p>是否手工测试：</p>
<ul>
<li>手动测试：即人工测试</li>
<li>自动测试：使用代码进行测试</li>
</ul>
<p>其他：</p>
<ul>
<li>冒烟测试：</li>
<li>回归测试：软件测试的第二轮开始，测试此前的bug是否修复</li>
<li>随机测试：模拟用户测试（用户测试一般是随机的），一般在最后一次</li>
<li>探索性测试：升级版的随机测试，未来的发展方向</li>
</ul>
<h2 id="2-自动化分层测试体系"><a href="#2-自动化分层测试体系" class="headerlink" title="2.自动化分层测试体系"></a>2.自动化分层测试体系</h2><ul>
<li>70%单元测试</li>
<li>20%服务测试</li>
<li>10%用户界面测试</li>
</ul>
<h2 id="3-黑盒测试"><a href="#3-黑盒测试" class="headerlink" title="3.黑盒测试"></a>3.黑盒测试</h2><p>常用测试方法：</p>
<ul>
<li><p>等价类划分</p>
<p>输入域明确：把程序的输入域划分为若干个子集</p>
<p>分类：每个子集中选取少数代表性数据作为测试用例，其等价于所在类的其他值</p>
<p>常见分类：有效、无效等价类</p>
</li>
<li><p>边界值分析：将边界值作为重点测试数据，对等价类的补充</p>
</li>
<li><p>因果图：一个有向图，显示因果模型中变量之间的因果关系</p>
</li>
<li><p>判定表：一种表达因果关系的逻辑表达方式，使用表格分类条件、中间结果、最终结果之间的关系</p>
</li>
<li><p>决策树：判定表的另一种表示（比因果图和判定表好），可使用流程图表示</p>
</li>
<li><p>探索性测试：一种软件测试风格。基于上下文质量反馈的测试风格。适时调整测试执行</p>
<ul>
<li>成本低，可以不用提前设计大量测试用例</li>
<li>可以激发测试工程师的创造性，更快发现问题</li>
<li>测试覆盖度无法得到有效保障</li>
<li>多数测试活动都是由探索性测试与脚本用例结合</li>
</ul>
</li>
</ul>
<h2 id="3-测试用例"><a href="#3-测试用例" class="headerlink" title="3.测试用例"></a>3.测试用例</h2><p>最终结果：<strong>成功</strong> 或 <strong>失败</strong></p>
<p>三大核心要素：标题、步骤、断言</p>
<ul>
<li>标题：测试用例的描述</li>
<li>步骤：测试执行过程的描述</li>
<li>断言：实际结果与预期结果对比</li>
</ul>
]]></content>
      <tags>
        <tag>测试</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent三大范式</title>
    <url>/2026/03/22/Agent%E8%8C%83%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="workflow与agent的区别"><a href="#workflow与agent的区别" class="headerlink" title="workflow与agent的区别"></a>workflow与agent的区别</h2><h3 id="workflow（工作流）"><a href="#workflow（工作流）" class="headerlink" title="workflow（工作流）"></a>workflow（工作流）</h3><p>核心是对一系列任务或步骤进行预先定义的、结构化的编排。本质上是一个精确的、静态的流程图，规定了在何种条件下、以什么顺序来执行什么操作。</p>
<p>简单地说，workflow就是让AI按部就班地执行指令。</p>
<h3 id="agent"><a href="#agent" class="headerlink" title="agent"></a>agent</h3><p>一个具备自主性、以目标为导向的系统。在一定程度上理解环境、进行推理、制定计划，并动态地采取行动来达成目标。</p>
<p>简单地说，agent就是赋予AI自由度去自主地达成目标。</p>
<p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260322205854253.png?token=AR3LVDAK3HM5FJLAEPRWFNDJX7T4W"></p>
<h2 id="Agent经典范式"><a href="#Agent经典范式" class="headerlink" title="Agent经典范式"></a>Agent经典范式</h2><h3 id="ReAct范式"><a href="#ReAct范式" class="headerlink" title="ReAct范式"></a>ReAct范式</h3><p>ReAct的巧妙之处在于，它认识到<strong>思考与行动是相辅相成的</strong>。思考指导行动，而行动的结果又反过来修正思考。为此，ReAct范式通过一种特殊的提示工程来引导模型，使其每一步的输出都遵循一个固定的轨迹：</p>
<ul>
<li><strong>Thought (思考)：</strong> 这是智能体的“内心独白”。它会分析当前情况、分解任务、制定下一步计划，或者反思上一步的结果。</li>
<li><strong>Action (行动)：</strong> 这是智能体决定采取的具体动作，通常是调用一个外部工具，例如 <code>Search[&#39;华为最新款手机&#39;]</code>。</li>
<li><strong>Observation (观察)：</strong> 这是执行<code>Action</code>后从外部工具返回的结果，例如搜索结果的摘要或API的返回值。</li>
</ul>
<p>智能体将不断重复这个 <strong>Thought -&gt; Action -&gt; Observation</strong> 的循环，将新的观察结果追加到历史记录中，形成一个不断增长的上下文，直到它在<code>Thought</code>中认为已经找到了最终答案，然后输出结果。这个过程形成了一个强大的协同效应：<strong>推理使得行动更具目的性，而行动则为推理提供了事实依据。</strong></p>
<p>（1）ReAct 的主要特点</p>
<ol>
<li><strong>高可解释性</strong>：ReAct 最大的优点之一就是透明。通过 <code>Thought</code> 链，我们可以清晰地看到智能体每一步的“心路历程”——它为什么会选择这个工具，下一步又打算做什么。这对于理解、信任和调试智能体的行为至关重要。</li>
<li><strong>动态规划与纠错能力</strong>：与一次性生成完整计划的范式不同，ReAct 是“走一步，看一步”。它根据每一步从外部世界获得的 <code>Observation</code> 来动态调整后续的 <code>Thought</code> 和 <code>Action</code>。如果上一步的搜索结果不理想，它可以在下一步中修正搜索词，重新尝试。</li>
<li><strong>工具协同能力</strong>：ReAct 范式天然地将大语言模型的推理能力与外部工具的执行能力结合起来。LLM 负责运筹帷幄（规划和推理），工具负责解决具体问题（搜索、计算），二者协同工作，突破了单一 LLM 在知识时效性、计算准确性等方面的固有局限。</li>
</ol>
<p>（2）ReAct 的固有局限性</p>
<ol>
<li><strong>对LLM自身能力的强依赖</strong>：ReAct 流程的成功与否，高度依赖于底层 LLM 的综合能力。如果 LLM 的逻辑推理能力、指令遵循能力或格式化输出能力不足，就很容易在 <code>Thought</code> 环节产生错误的规划，或者在 <code>Action</code> 环节生成不符合格式的指令，导致整个流程中断。</li>
<li><strong>执行效率问题</strong>：由于其循序渐进的特性，完成一个任务通常需要多次调用 LLM。每一次调用都伴随着网络延迟和计算成本。对于需要很多步骤的复杂任务，这种串行的“思考-行动”循环可能会导致较高的总耗时和费用。</li>
<li><strong>提示词的脆弱性</strong>：整个机制的稳定运行建立在一个精心设计的提示词模板之上。模板中的任何微小变动，甚至是用词的差异，都可能影响 LLM 的行为。此外，并非所有模型都能持续稳定地遵循预设的格式，这增加了在实际应用中的不确定性。</li>
<li><strong>可能陷入局部最优</strong>：步进式的决策模式意味着智能体缺乏一个全局的、长远的规划。它可能会因为眼前的 <code>Observation</code> 而选择一个看似正确但长远来看并非最优的路径，甚至在某些情况下陷入“原地打转”的循环中。</li>
</ol>
<p>（3）调试技巧</p>
<p>当你构建的 ReAct 智能体行为不符合预期时，可以从以下几个方面入手进行调试：</p>
<ul>
<li><strong>检查完整的提示词</strong>：在每次调用 LLM 之前，将最终格式化好的、包含所有历史记录的完整提示词打印出来。这是追溯 LLM 决策源头的最直接方式。</li>
<li><strong>分析原始输出</strong>：当输出解析失败时（例如，正则表达式没有匹配到 <code>Action</code>），务必将 LLM 返回的原始、未经处理的文本打印出来。这能帮助你判断是 LLM 没有遵循格式，还是你的解析逻辑有误。</li>
<li><strong>验证工具的输入与输出</strong>：检查智能体生成的 <code>tool_input</code> 是否是工具函数所期望的格式，同时也要确保工具返回的 <code>observation</code> 格式是智能体可以理解和处理的。</li>
<li>**调整提示词中的示例 (Few-shot Prompting)**：如果模型频繁出错，可以在提示词中加入一两个完整的“Thought-Action-Observation”成功案例，通过示例来引导模型更好地遵循你的指令。</li>
<li><strong>尝试不同的模型或参数</strong>：更换一个能力更强的模型，或者调整 <code>temperature</code> 参数（通常设为0以保证输出的确定性），有时能直接解决问题。</li>
</ul>
<h3 id="PlanAndSlove"><a href="#PlanAndSlove" class="headerlink" title="PlanAndSlove"></a>PlanAndSlove</h3><p><strong>Plan-and-Solve</strong>。顾名思义，这种范式将任务处理明确地分为两个阶段：**先规划 (Plan)，后执行 (Solve)**。</p>
<p>如果说 ReAct 像一个经验丰富的侦探，根据现场的蛛丝马迹（Observation）一步步推理，随时调整自己的调查方向；那么 Plan-and-Solve 则更像一位建筑师，在动工之前必须先绘制出完整的蓝图（Plan），然后严格按照蓝图来施工（Solve）。事实上我们现在用的很多大模型工具的Agent模式都融入了这种设计模式。</p>
<h4 id="Plan-and-Solve-的工作原理"><a href="#Plan-and-Solve-的工作原理" class="headerlink" title="Plan-and-Solve 的工作原理"></a>Plan-and-Solve 的工作原理</h4><p>Plan-and-Solve Prompting 由 Lei Wang 在2023年提出。其核心动机是为了解决思维链在处理多步骤、复杂问题时容易“偏离轨道”的问题。</p>
<p>与 ReAct 将思考和行动融合在每一步不同，Plan-and-Solve 将整个流程解耦为两个核心阶段，如图4.2所示：</p>
<ol>
<li><strong>规划阶段 (Planning Phase)<strong>： 首先，智能体会接收用户的完整问题。它的第一个任务不是直接去解决问题或调用工具，而是</strong>将问题分解，并制定出一个清晰、分步骤的行动计划</strong>。这个计划本身就是一次大语言模型的调用产物。</li>
<li><strong>执行阶段 (Solving Phase)<strong>： 在获得完整的计划后，智能体进入执行阶段。它会</strong>严格按照计划中的步骤，逐一执行</strong>。每一步的执行都可能是一次独立的 LLM 调用，或者是对上一步结果的加工处理，直到计划中的所有步骤都完成，最终得出答案。</li>
</ol>
<p>这种“先谋后动”的策略，使得智能体在处理需要长远规划的复杂任务时，能够保持更高的目标一致性，避免在中间步骤中迷失方向。</p>
<p>我们可以将这个两阶段过程进行形式化表达。首先，规划模型 πplan 根据原始问题 q 生成一个包含 n 个步骤的计划 P&#x3D;(p1,p2,…,pn)：<br>$$<br>P&#x3D;π_{plan}(q)<br>$$<br>随后，在执行阶段，执行模型 πsolve 会逐一完成计划中的步骤。对于第 i 个步骤，其解决方案 si 的生成会同时依赖于原始问题 q、完整计划 P 以及之前所有步骤的执行结果 (s1,…,si−1)：<br>$$<br>s_i&#x3D;π_{solve}(q,P,(s_1,…,s_{i−1}))<br>$$<br>最终的答案就是最后一个步骤的执行结果 sn。</p>
<p>Plan-and-Solve 尤其适用于那些结构性强、可以被清晰分解的复杂任务，例如：</p>
<ul>
<li><strong>多步数学应用题</strong>：需要先列出计算步骤，再逐一求解。</li>
<li><strong>需要整合多个信息源的报告撰写</strong>：需要先规划好报告结构（引言、数据来源A、数据来源B、总结），再逐一填充内容。</li>
<li><strong>代码生成任务</strong>：需要先构思好函数、类和模块的结构，再逐一实现。</li>
</ul>
<p>规划阶段的目标是让大语言模型接收原始问题，并输出一个清晰、分步骤的行动计划。这个计划必须是结构化的，以便我们的代码可以轻松解析并逐一执行。因此，我们设计的提示词需要明确地告诉模型它的角色和任务，并给出一个输出格式的范例。</p>
<p>执行器的提示词与规划器不同。它的目标不是分解问题，而是<strong>在已有上下文的基础上，专注解决当前这一个步骤</strong>。因此，提示词需要包含以下关键信息：</p>
<ul>
<li><strong>原始问题</strong>： 确保模型始终了解最终目标。</li>
<li><strong>完整计划</strong>： 让模型了解当前步骤在整个任务中的位置。</li>
<li><strong>历史步骤与结果</strong>： 提供至今为止已经完成的工作，作为当前步骤的直接输入。</li>
<li><strong>当前步骤</strong>： 明确指示模型现在需要解决哪一个具体任务。</li>
</ul>
<h3 id="Reflection"><a href="#Reflection" class="headerlink" title="Reflection"></a>Reflection</h3><p>Reflection 机制的灵感来源于人类的学习过程：我们完成初稿后会进行校对，解出数学题后会进行验算。这一思想在多个研究中得到了体现，例如 Shinn, Noah 在2023年提出的 Reflexion 框架。其核心工作流程可以概括为一个简洁的三步循环：<strong>执行 -&gt; 反思 -&gt; 优化</strong>。</p>
<ol>
<li>**执行 (Execution)**：首先，智能体使用我们熟悉的方法（如 ReAct 或 Plan-and-Solve）尝试完成任务，生成一个初步的解决方案或行动轨迹。这可以看作是“初稿”。</li>
<li>**反思 (Reflection)**：接着，智能体进入反思阶段。它会调用一个独立的、或者带有特殊提示词的大语言模型实例，来扮演一个“评审员”的角色。这个“评审员”会审视第一步生成的“初稿”，并从多个维度进行评估，例如：<ul>
<li><strong>事实性错误</strong>：是否存在与常识或已知事实相悖的内容？</li>
<li><strong>逻辑漏洞</strong>：推理过程是否存在不连贯或矛盾之处？</li>
<li><strong>效率问题</strong>：是否有更直接、更简洁的路径来完成任务？</li>
<li><strong>遗漏信息</strong>：是否忽略了问题的某些关键约束或方面？ 根据评估，它会生成一段结构化的**反馈 (Feedback)**，指出具体的问题所在和改进建议。</li>
</ul>
</li>
<li>**优化 (Refinement)**：最后，智能体将“初稿”和“反馈”作为新的上下文，再次调用大语言模型，要求它根据反馈内容对初稿进行修正，生成一个更完善的“修订稿”。</li>
</ol>
<p>这个循环可以重复进行多次，直到反思阶段不再发现新的问题，或者达到预设的迭代次数上限。我们可以将这个迭代优化的过程形式化地表达出来。首先，行动模型&#x2F;智能体会根据prompt来完成任务；反思模型&#x2F;智能体对任务完成情况进行判断，生成反馈；随后，优化模型&#x2F;智能体会结合原始任务、上一版任务完成情况以及反馈，生成新一版的输出 。</p>
<p>与前两种范式相比，Reflection 的价值在于：</p>
<ul>
<li>它为智能体提供了一个内部纠错回路，使其不再完全依赖于外部工具的反馈（ReAct 的 Observation），从而能够修正更高层次的逻辑和策略错误。</li>
<li>它将一次性的任务执行，转变为一个持续优化的过程，显著提升了复杂任务的最终成功率和答案质量。</li>
<li>它为智能体构建了一个临时的<strong>“短期记忆”</strong>。整个“执行-反思-优化”的轨迹形成了一个宝贵的经验记录，智能体不仅知道最终答案，还记得自己是如何从有缺陷的初稿迭代到最终版本的。更进一步，这个记忆系统还可以是<strong>多模态的</strong>，允许智能体反思和修正文本以外的输出（如代码、图像等），为构建更强大的多模态智能体奠定了基础。</li>
</ul>
<h4 id="Reflection-机制的成本收益分析"><a href="#Reflection-机制的成本收益分析" class="headerlink" title="Reflection 机制的成本收益分析"></a>Reflection 机制的成本收益分析</h4><p>尽管 Reflection 机制在提升任务解决质量上表现出色，但这种能力的获得并非没有代价。在实际应用中，我们需要权衡其带来的收益与相应的成本。</p>
<p>（1）主要成本</p>
<ol>
<li><strong>模型调用开销增加</strong>:这是最直接的成本。每进行一轮迭代，至少需要额外调用两次大语言模型（一次用于反思，一次用于优化）。如果迭代多轮，API 调用成本和计算资源消耗将成倍增加。</li>
<li><strong>任务延迟显著提高</strong>:Reflection 是一个串行过程，每一轮的优化都必须等待上一轮的反思完成。这使得任务的总耗时显著延长，不适合对实时性要求高的场景。</li>
<li><strong>提示工程复杂度上升</strong>:如我们的案例所示，Reflection 的成功在很大程度上依赖于高质量、有针对性的提示词。为“执行”、“反思”、“优化”等不同阶段设计和调试有效的提示词，需要投入更多的开发精力。</li>
</ol>
<p>（2）核心收益</p>
<ol>
<li><strong>解决方案质量的跃迁</strong>:最大的收益在于，它能将一个“合格”的初始方案，迭代优化成一个“优秀”的最终方案。这种从功能正确到性能高效、从逻辑粗糙到逻辑严谨的提升，在很多关键任务中是至关重要的。</li>
<li><strong>鲁棒性与可靠性增强</strong>:通过内部的自我纠错循环，智能体能够发现并修复初始方案中可能存在的逻辑漏洞、事实性错误或边界情况处理不当等问题，从而大大提高了最终结果的可靠性。</li>
</ol>
<p>综上所述，Reflection 机制是一种典型的“以成本换质量”的策略。它非常适合那些<strong>对最终结果的质量、准确性和可靠性有极高要求，且对任务完成的实时性要求相对宽松</strong>的场景。例如:</p>
<ul>
<li>生成关键的业务代码或技术报告。</li>
<li>在科学研究中进行复杂的逻辑推演。</li>
<li>需要深度分析和规划的决策支持系统。</li>
</ul>
<p>反之，如果应用场景需要快速响应，或者一个“大致正确”的答案就已经足够，那么使用更轻量的 ReAct 或 Plan-and-Solve 范式可能会是更具性价比的选择。</p>
]]></content>
      <categories>
        <category>Agent</category>
        <category>范式</category>
      </categories>
      <tags>
        <tag>Agent</tag>
      </tags>
  </entry>
  <entry>
    <title>深度学习激活函数学习</title>
    <url>/2026/01/17/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E6%BF%80%E6%B4%BB%E5%87%BD%E6%95%B0%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h2 id="一、激活函数原理"><a href="#一、激活函数原理" class="headerlink" title="一、激活函数原理"></a>一、激活函数原理</h2><p>激活函数（activation function），就是在神经网络的神经元上运行的函数，负责将神经元的输入映射到输出层，旨在帮组神经网络学习数据中的复杂模式。</p>
<h2 id="二、激活函数的作用"><a href="#二、激活函数的作用" class="headerlink" title="二、激活函数的作用"></a>二、激活函数的作用</h2><p>使用激活函数能够为神经网络引入非线性因素，使得神经网络可以任意逼近任何非线性函数，使深层神经网络表达能力更加强大，使得神经网络可以应用到总舵的非线性模型中。如果不使用激活函数，那神经网络的每一层输出都是上层输入的线性函数，无论神经网络的层数是多少，其输出都是输入的线性组合，这样就退化为原始的感知机模型了。</p>
<h2 id="三、激活函数分类"><a href="#三、激活函数分类" class="headerlink" title="三、激活函数分类"></a>三、激活函数分类</h2><p>激活函数可以分为<strong>饱和激活函数</strong>与<strong>非饱和激活函数</strong>。</p>
<p>3.1 饱和的概念</p>
<p>假设h(x)是一个激活函数，有如下一个极限：<br>$$<br>\lim_{n\rightarrow\pm\infty}h^\prime(x)&#x3D;0<br>$$<br>当n趋于正无穷，激活函数的导数趋于0，则称之为右饱和；若当n趋于负无穷时，激活函数的导数趋于0，则称之为左饱和。当一个函数同时满足左饱和和右饱和时，我们称之为饱和，不然则称之为非饱和。</p>
<ul>
<li>饱和激活函数：sigmoid、tanh</li>
<li>非饱和激活函数：ReLU、 Leakly ReLU、elu、PReLU、RReLU</li>
</ul>
<p>相比于饱和激活函数，非饱和激活函数的优势在于两点：</p>
<ul>
<li>能解决深度神经网络带来的梯度消失问题</li>
<li>能加快收敛速度</li>
</ul>
<h2 id="四、常见激活函数"><a href="#四、常见激活函数" class="headerlink" title="四、常见激活函数"></a>四、常见激活函数</h2><h3 id="4-1-sigmoid"><a href="#4-1-sigmoid" class="headerlink" title="4.1 sigmoid"></a>4.1 sigmoid</h3><p>其数学表达式如下，输出范围在[0, 1]之间。<br>$$<br>sigmoid(x) &#x3D; \frac{1}{1+e^{-x}}<br>$$<br>其导数表达式为：<br>$$<br>sigmoid\prime(x) &#x3D; sigmoid(x)(1-sigmoid(x)) &#x3D;\frac{1}{1+e^{-x}}\frac{e^{-x}}{1+e^{-x}}&#x3D;\frac{e^{-x}}{(1+e^{-x})^2}<br>$$<br>其函数图像如下：</p>
<p><img src="https://i-blog.csdnimg.cn/blog_migrate/af5f15ef44f1b276c78bec5e140e7082.png" alt="图片"></p>
<p>其代码实现：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> math</span><br><span class="line"><span class="keyword">import</span> scipy</span><br><span class="line"><span class="comment"># math.exp()只适用于单数值</span></span><br><span class="line"><span class="comment"># numpy.exp()适用于数值、列表、数值、矩阵</span></span><br><span class="line"><span class="comment"># scipy.special.expit()同numpy，但更稳定，速度较慢</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sigmoid</span>(<span class="params">x</span>):</span><br><span class="line">    <span class="comment"># math</span></span><br><span class="line">    math_y = <span class="number">1</span> / (<span class="number">1</span> + math.exp(-x))</span><br><span class="line">    <span class="comment"># np</span></span><br><span class="line">    np_y = <span class="number">1</span> / (<span class="number">1</span> + np.exp(-x))</span><br><span class="line">    <span class="comment"># scipy</span></span><br><span class="line">    sci_y = <span class="number">1</span> / (<span class="number">1</span> + scipy.special.expit(x))</span><br><span class="line">    <span class="keyword">return</span> np_y</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sigmoid_derivative</span>(<span class="params">x</span>):</span><br><span class="line">    <span class="keyword">return</span> sigmoid(x) * (<span class="number">1</span> - sigmoid(x))</span><br></pre></td></tr></table></figure>

<p>使用场景：</p>
<ul>
<li>sigmoid函数的输出范围是0到1。适合作为模型的输出函数用于输出一个0-1范围内的概率值，如表示二分类的类别或者用于表示置信度。</li>
<li>sigmoid函数梯度平滑，方便求导，可以用于防止模型训练过程中出现突变的梯度。</li>
</ul>
<p>缺点：</p>
<ul>
<li>容易造成梯度消失。</li>
<li>函数输出不是以0为中心的，梯度可能会向特定方向一点，从而降低权重更新的效率。</li>
<li>sigmoid函数执行指数运算，计算速度慢，计算资源需求大。</li>
</ul>
<h3 id="4-2-Tanh函数"><a href="#4-2-Tanh函数" class="headerlink" title="4.2 Tanh函数"></a>4.2 Tanh函数</h3><p>其数学表达式如下，输出范围在[-1, 1]之间。<br>$$<br>Tanh(x) &#x3D; \frac{e^x-e^{-x}}{e^x+e^{-x}}&#x3D;\frac{e^x-e^{-x}}{e^x+e^{-x}} \frac{e^{-x}}{e^{-x}}&#x3D;\frac{1-e^{-2x}}{1+e^{-2x}}<br>$$<br>实际上，Tanh函数是 sigmoid 的变形：<br>$$<br>Tanh(x)&#x3D;2sigmoid(2x)-1&#x3D;\frac{1-e^{-2x}}{1+e^{-2x}}<br>$$<br>其导数表达式为：<br>$$<br>Tanh\prime(x) &#x3D;\frac{(e^x+e^{-x})^2-(e^x-e^{-x})^2}{({e^x+e^{-x}})^2}&#x3D;\frac{4}{(1+e^{-x})^2}&#x3D;\frac{4e^{-2x}}{(1+e^{-2x})^2}<br>$$<br>与sigmoid函数不同的三十，tanh是以0为中心的。因此，在实际应用中，tanh会比ssigmoid更好一点。但在饱和神经元的情况下辖，tanh仍没有解决梯度消失的问题。</p>
<p>其函数图像如下：</p>
<p><img src="https://i-blog.csdnimg.cn/blog_migrate/e58d7204fef7f7410e92b4bf82e91938.png" alt="img"></p>
<p>其代码实现：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> math</span><br><span class="line"><span class="keyword">import</span> scipy</span><br><span class="line"><span class="comment"># math.exp()只适用于单数值</span></span><br><span class="line"><span class="comment"># numpy.exp()适用于数值、列表、数值、矩阵</span></span><br><span class="line"><span class="comment"># scipy.special.expit()同numpy，但更稳定，速度较慢</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sigmoid</span>(<span class="params">x</span>):</span><br><span class="line">    <span class="comment"># math</span></span><br><span class="line">    math_y = (math.exp(x) - math.exp(-x)) / (math.exp(x) + math.exp(-x))</span><br><span class="line">    <span class="comment"># np</span></span><br><span class="line">    np_y = (np.exp(x) - np.exp(-x)) / (np.exp(x) + np.exp(-x))</span><br><span class="line">    <span class="comment"># scipy</span></span><br><span class="line">    sci_y = (scipy.special.expit(x) - scipy.special.expit(-x)) / (scipy.special.expit(x) + scipy.special.expit(-x))</span><br><span class="line">    <span class="keyword">return</span> np_y</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sigmoid_derivative</span>(<span class="params">x</span>):</span><br><span class="line">    <span class="keyword">return</span> sigmoid(x) * (<span class="number">1</span> - sigmoid(x))</span><br></pre></td></tr></table></figure>

<p>Tanh函数优点：</p>
<ul>
<li>Tanh的输出间隔为1，并且整个函数以0为中心，比sigmoid函数更好；</li>
<li>在Tanh函数图中，负输入被强映射为负，二零输入被映射为接近0。</li>
</ul>
<p>Tanh函数缺点：</p>
<ul>
<li>仍存在梯度饱和的问题；</li>
<li>依然进行的是指数运算。</li>
</ul>
<h3 id="4-3-ReLU函数"><a href="#4-3-ReLU函数" class="headerlink" title="4.3 ReLU函数"></a>4.3 ReLU函数</h3><p>其数学表达式为：<br>$$<br>ReLU(x)&#x3D;\max(0,x)<br>$$<br>函数图像如下：</p>
<p><img src="https://i-blog.csdnimg.cn/blog_migrate/a2f4f9f3c522518908e20d7865beb48c.png" alt="img"></p>
<p>ReLU函数优点：</p>
<ul>
<li>ReLU解决了梯度消失的问题，当输入值为正是，神经元不会饱和；</li>
<li>由于ReLU函数的线性、非饱和性质，在SGD中能够快速收敛；</li>
<li>计算复杂度低，不需要进行指数运算。</li>
</ul>
<p>ReLU函数缺点：</p>
<ul>
<li>与simoid函数一样，其输出不是以0为中心的；</li>
<li>Dead ReLU问题。当输入为负时，梯度为0。此时，神经元及之后的神经元梯度永远为0，不再对任何数据有所响应，导致相应参数永远不会被更新。</li>
</ul>
<p>训练神经网络时，若学习率没设置好，第一次更新权重时输入为负值，则这个含有ReLU的神经节点会死亡，再也不会被激活。因此，要设置一个合适的较小学习率，来降低这种情况的发生。</p>
]]></content>
      <categories>
        <category>深度学习</category>
        <category>激活函数</category>
      </categories>
  </entry>
  <entry>
    <title>数据库学习笔记</title>
    <url>/2026/04/07/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="数据库系统的基本原理"><a href="#数据库系统的基本原理" class="headerlink" title="数据库系统的基本原理"></a>数据库系统的基本原理</h2><p>介绍基本概念、发展阶段、数据模型、DBMS的功能及组成、DBS的组成及全局结构</p>
<h3 id="数据库概述"><a href="#数据库概述" class="headerlink" title="数据库概述"></a>数据库概述</h3><h4 id="1数据库系统的应用"><a href="#1数据库系统的应用" class="headerlink" title="1数据库系统的应用"></a>1数据库系统的应用</h4><p>广泛应用在电信业、银行业、金融业、销售业、电商、大学、航空业、人力资源、制造业</p>
<h4 id="数据库系统的概念"><a href="#数据库系统的概念" class="headerlink" title="数据库系统的概念"></a>数据库系统的概念</h4><h5 id="数据Date"><a href="#数据Date" class="headerlink" title="数据Date"></a>数据Date</h5><p>数据：数据库存储的基本对象，是描述现实世界中各种事物或抽象概念的、可存储并具有明确意义的符号记录。</p>
<p>具体事物：有形且看得见的事物		抽象事物：无形且看不见的事物</p>
<p>记录是数据库系统表示和存储数据的一种格式</p>
<h5 id="数据库DB"><a href="#数据库DB" class="headerlink" title="数据库DB"></a>数据库DB</h5><p>简单上，数据库：相互关联的数据集合</p>
<p>严格上，数据库：长期存储在计算机内、有组织、可共享的大量数据的集合。</p>
<p>数据库具有较小的冗余度（不能杜绝数据冗余）、较高的数据独立性、易扩展性</p>
<h5 id="数据库管理系统DBMS"><a href="#数据库管理系统DBMS" class="headerlink" title="数据库管理系统DBMS"></a>数据库管理系统DBMS</h5><p>数据库系统核心部分，介于用户和OS之间。提供访问数据库的方法（数据库定义、建立、查询、更新及其他数据控制）</p>
<h4 id="主要功能"><a href="#主要功能" class="headerlink" title="主要功能"></a><strong>主要</strong>功能</h4><h5 id="数据定义功能"><a href="#数据定义功能" class="headerlink" title="数据定义功能"></a>数据定义功能</h5><p>使用数据定义语言（DDL），在数据库中<strong>定义数据对象</strong>（表、视图、索引、存储过程等）、数据的完整性约束等</p>
<p>数据的完整性（主要）：实体完整性、参照完整性、用户定义的完整性</p>
<h5 id="数据操纵功能"><a href="#数据操纵功能" class="headerlink" title="数据操纵功能"></a>数据操纵功能</h5><p>使用数据操纵语言（DML），对数据库进行<strong>增、删、改、查</strong>操作（insert、delete、update、select）</p>
<p>查询功能使用频率最高</p>
<h5 id="数据控制功能"><a href="#数据控制功能" class="headerlink" title="数据控制功能"></a>数据控制功能</h5><p>使用数据控制语言（DCL），进行用户权限的变动，如授予或撤销，即安全性控制：解决多用户对数据库的并发使用所产生的事物处理问题（并发控制；数据库的转储、恢复功能；数据库的性能监视、分析功能）</p>
<h5 id="数据库系统DBS"><a href="#数据库系统DBS" class="headerlink" title="数据库系统DBS"></a>数据库系统DBS</h5><p>数据库系统：计算机系统引入数据库后的系统，由DB、DBMS、应用系统、数据库管理员DBA构成。</p>
<h5 id="数据库应用系统DBAS"><a href="#数据库应用系统DBAS" class="headerlink" title="数据库应用系统DBAS"></a>数据库应用系统DBAS</h5><p>DBAS：指实现业务逻辑的应用程序</p>
<p>要求：提供一个有好的、人性化的操作数据的GUI</p>
<h4 id="数据管理技术的发展阶段"><a href="#数据管理技术的发展阶段" class="headerlink" title="数据管理技术的发展阶段"></a>数据管理技术的发展阶段</h4><h5 id="人工管理阶段"><a href="#人工管理阶段" class="headerlink" title="人工管理阶段"></a>人工管理阶段</h5><p>用途：科学计算</p>
<p>处理方式：批处理</p>
<p>特点：数据不保存、不具有独立性、不共享；只有程序概念，无文件概念</p>
<h5 id="文件管理系统阶段"><a href="#文件管理系统阶段" class="headerlink" title="文件管理系统阶段"></a>文件管理系统阶段</h5><p>用途：科学计算、信息管理</p>
<p>处理方式：批处理、联机实时处理</p>
<p>缺点：数据冗余性和不一致（数据重复存储且可能有差异）、数据独立性差、数据孤立</p>
<h5 id="数据库系统阶段"><a href="#数据库系统阶段" class="headerlink" title="数据库系统阶段"></a>数据库系统阶段</h5><p>用途：大规模管理</p>
<p>处理方式：批处理、实时联机处理、分布处理</p>
<p>特点：数据结构化；数据共享性高、冗余度低、易扩充；数据独立性高（物理独立性、逻辑独立性）；DBMS统一管理和控制（包括数据的完整性检查、并发控制、数据的安全性保护、数据库的恢复）</p>
<h4 id="数据库系统的用户"><a href="#数据库系统的用户" class="headerlink" title="数据库系统的用户"></a>数据库系统的用户</h4><h5 id="数据库管理员"><a href="#数据库管理员" class="headerlink" title="数据库管理员"></a>数据库管理员</h5><p>①参与数据库的设计</p>
<p>②定义数据的安全性要求和完整性约束条件</p>
<p>③日常维护：定期备份数据库、监视数据库的运行、保障有充足的空余磁盘空间</p>
<p>④数据库的改进、重组和重构</p>
<h5 id="数据库用户"><a href="#数据库用户" class="headerlink" title="数据库用户"></a>数据库用户</h5><p>①最终用户：现实系统中的业务人员，数据库的主要用户</p>
<p>②专业用户：包括工程师、科学家、经济学家等具有较高科学技术背景的人员</p>
<p>③系统分析员和数据库设计人员</p>
<p>④应用程序员</p>
<h3 id="数据模型"><a href="#数据模型" class="headerlink" title="数据模型"></a>数据模型</h3><p>模型是对现实世界的抽象</p>
<h4 id="数据抽象的过程"><a href="#数据抽象的过程" class="headerlink" title="数据抽象的过程"></a>数据抽象的过程</h4><p>美国国家标准化协会（ANSI）根据<strong>数据抽象级别</strong>定义了四种模型：概念模型、逻辑模型、外部模型、内部模型</p>
<p>数据抽象过程：</p>
<p>①综合：根据用户需求设计数据库的概念模型</p>
<p>②转换：根据转换规则将概念模型转换为数据库的逻辑模型</p>
<p>③实现：根据用户业务特点设计不同的外部模型</p>
<p>④根据逻辑模型设计内部模型</p>
<p>模型介绍：</p>
<h5 id="概念模型"><a href="#概念模型" class="headerlink" title="概念模型"></a>概念模型</h5><p>抽象级别最高，表达用户需求观点的数据库全局逻辑结构的模型</p>
<p>特点：</p>
<p>①表达了数据库的整体逻辑结构，<strong>企业管理人员</strong>对整个<strong>企业组织</strong>的全面概述</p>
<p>②从<strong>用户需求</strong>的观点出发对数据建模</p>
<p>③独立于软硬件。不依赖于硬件设备，不依赖于实现时的DBMS软件</p>
<p>④数据库设计人员与用户之间进行交流的工具</p>
<p>现在概念模型多是实体-联系模型（E-R模型，使用E-R图表示）。</p>
<p>实体是现实世界或客观世界中可相互区别的对象（抽象或具体）。</p>
<p>联系：两个或多个实体之间的关联，分为三种：</p>
<p>①一对一（1:1）②一对多（1：N）③多对多（M:N）</p>
<h5 id="逻辑模型"><a href="#逻辑模型" class="headerlink" title="逻辑模型"></a>逻辑模型</h5><p>特点：</p>
<p>①表达了数据库的整体逻辑结构，<strong>设计人员</strong>对整个<strong>企业组织数据库</strong>的全面概述（与概念模型不同，主要区分）</p>
<p>②从<strong>数据库实现</strong>的观点出发对数据建模</p>
<p>③硬件独立，软件依赖</p>
<p>④数据库设计人员与应用程序员之间交流的工具</p>
<p>类别：层次模型（数据结构为树状结构）、网状结构（数据结构为有向图）、关系模型（数据结构为二维表格，现在关系型DBMS均采用关系模型）</p>
<h5 id="外部模型"><a href="#外部模型" class="headerlink" title="外部模型"></a>外部模型</h5><p>特点：</p>
<p>①逻辑结构的一个逻辑子集（可以有多个）</p>
<p>②硬件独立，软件依赖</p>
<p>③反映了用户使用数据库的观点</p>
<p>系统考查上，特点为：简化了用户观点、有助于数据库的安全性保护、对概念模型的支持</p>
<h5 id="内部模型（物理模型）"><a href="#内部模型（物理模型）" class="headerlink" title="内部模型（物理模型）"></a>内部模型（物理模型）</h5><p>数据库最底层的抽象（描述了数据在磁盘上的存储方式、存取设备、存取方法）。</p>
<p>与硬件和软件紧密相连的。</p>
<h4 id="关系模型"><a href="#关系模型" class="headerlink" title="关系模型"></a>关系模型</h4><p>1970年，IBM公司 E.F.Codd 首次提出</p>
<h5 id="数据模型的三要素"><a href="#数据模型的三要素" class="headerlink" title="数据模型的三要素"></a>数据模型的三要素</h5><p>①数据结构</p>
<p>所描述的对象类型的集合，对系统静态特性的描述。</p>
<p>②数据操作</p>
<p>对系统动态特性的描述。</p>
<p>③数据的完整性约束</p>
<p>一组完整性规则，保证数据的正确性、有效性、相容性、</p>
<p>必须满足实体完整性和参照完整性。</p>
<h5 id="关系数据模型的数据结构"><a href="#关系数据模型的数据结构" class="headerlink" title="关系数据模型的数据结构"></a>关系数据模型的数据结构</h5><p>①关系（relation）</p>
<p>一个关系即为一个规范的二维表。</p>
<p>基本条件：关系的每一列不可再分。即不允许表中还有表</p>
<p>②元组（tuple）</p>
<p>二维表中除第一行外的每一行为一个元组。</p>
<p>一个元祖代表一条完整的信息。</p>
<p>③属性（attribute）</p>
<p>二维表中的每一列为一个属性。</p>
<p>④码（key）</p>
<p>码：关键字或关键码。</p>
<p>由一个属性或属性的组合能够<strong>唯一</strong>确定一个元组，则该属性或属性的组合称为码。</p>
<p>⑤关系模式</p>
<p>一般描述：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">关系名（属性1，属性2，属性3，属性4，···，属性n）</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><h5 id="关系数据模型的操作和完整性约束"><a href="#关系数据模型的操作和完整性约束" class="headerlink" title="关系数据模型的操作和完整性约束"></a>关系数据模型的操作和完整性约束</h5></li>
</ol>
<p>主要操作：查询、插入、删除、更新数据（必须满足关系的完整性约束条件）</p>
<p>关系的完整性约束条件：实体参照性、参照完整性、用户定义的完整性</p>
<h3 id="数据库体系结构"><a href="#数据库体系结构" class="headerlink" title="数据库体系结构"></a>数据库体系结构</h3><h4 id="数据库系统的三级结构"><a href="#数据库系统的三级结构" class="headerlink" title="数据库系统的三级结构"></a>数据库系统的三级结构</h4><p>1975年，美国ANSI&#x2F;SPARC报告提出三级划分法。</p>
<h5 id="用户级数据库"><a href="#用户级数据库" class="headerlink" title="用户级数据库"></a>用户级数据库</h5><p>对应<strong>外模式</strong>，最接近用户，用户可以看到和使用的数据库（又称用户视图）。</p>
<p>主要由外部记录组成，不同视图可重叠。</p>
<p>所有操作都是针对用户视图。</p>
<h5 id="概念级数据库"><a href="#概念级数据库" class="headerlink" title="概念级数据库"></a>概念级数据库</h5><p>对应<strong>概念模式</strong>，介于用户级和物理级之间。数据库管理人员看到和使用的数据库（又称DBA视图）。</p>
<h5 id="物理级数据库"><a href="#物理级数据库" class="headerlink" title="物理级数据库"></a>物理级数据库</h5><p>对应<strong>内模式</strong>，数据库底层表示。描述数据的实际存储组织，最接近与物理存储的级别（又称内部视图）。</p>
<p>由内部记录组成。</p>
<p>注意：物理级数据库不是真正的物理存储（只是对物理存储的抽象），最接近物理存储。</p>
<h4 id="数据库系统的三级模式"><a href="#数据库系统的三级模式" class="headerlink" title="数据库系统的三级模式"></a>数据库系统的三级模式</h4><h5 id="1概念模式"><a href="#1概念模式" class="headerlink" title="1概念模式"></a>1概念模式</h5><p>又称<strong>模式</strong>或<strong>逻辑模式</strong>。</p>
<p>数据库中<strong>全体</strong>数据的逻辑结构和特征的描述，<strong>所有用户</strong>的公共数据视图。</p>
<p>具有唯一性。即一个数据库只有一个概念模式。</p>
<h5 id="外部模式"><a href="#外部模式" class="headerlink" title="外部模式"></a>外部模式</h5><p>又称<strong>子模式</strong>和<strong>用户模式</strong>。</p>
<p>数据库用户可以看到和使用的<strong>局部</strong>数据的逻辑结构和特征的描述。数据库用户的数据视图</p>
<p>具有不唯一性。</p>
<h5 id="内部模式"><a href="#内部模式" class="headerlink" title="内部模式"></a>内部模式</h5><p>又称<strong>存储模式</strong>或<strong>物理模式</strong>。</p>
<p>数据物理结构和存储方式的描述，数据在数据库内的表示方式。</p>
<p>具有唯一性。</p>
<h4 id="数据库系统的二级映射与数据独立性"><a href="#数据库系统的二级映射与数据独立性" class="headerlink" title="数据库系统的二级映射与数据独立性"></a>数据库系统的二级映射与数据独立性</h4><h5 id="数据库系统的二级映射"><a href="#数据库系统的二级映射" class="headerlink" title="数据库系统的二级映射"></a>数据库系统的二级映射</h5><p>外模式&#x2F;模式映射				模式&#x2F;内模式映射</p>
<p>三个抽象级通过二级映射进行相互转换，形成一个统一的整体。</p>
<h5 id="数据独立性"><a href="#数据独立性" class="headerlink" title="数据独立性"></a>数据独立性</h5><p>应用程序与数据之间的独立性。包括两种如下：</p>
<p>①物理独立性</p>
<p>应用程序与磁盘上的数据库中的数据是独立的。</p>
<p>通过模式&#x2F;内模式映射来实现的。</p>
<p>②逻辑独立性</p>
<p>应用程序与逻辑结构是相互独立的。通过外模式&#x2F;模式映射来实现的。</p>
<h2 id="关系数据库标准语言SQL"><a href="#关系数据库标准语言SQL" class="headerlink" title="关系数据库标准语言SQL"></a>关系数据库标准语言SQL</h2><h3 id="SQL-语言介绍"><a href="#SQL-语言介绍" class="headerlink" title="SQL 语言介绍"></a>SQL 语言介绍</h3><p>近期版本是SQL2016，主要新特性为行模式识别、支持json对象、多态表函数等</p>
<h4 id="SQL数据库的体系结构"><a href="#SQL数据库的体系结构" class="headerlink" title="SQL数据库的体系结构"></a>SQL数据库的体系结构</h4><p>特点：</p>
<p>①SQL用户可以是应用程序或终端用户。</p>
<p>②使用SQL语言对基本表和视图进行查询。</p>
<p>③一个视图是从若干个基本表或其他视图上导出的表。</p>
<p>④一个或一些基本表对应一个数据文件。</p>
<p>⑤一个基本表可以带若干个索引。</p>
<p>⑥一个表空间可以由若干个数据文件组成。</p>
<p>⑦一个数据库可以由若干个存储文件组成。</p>
<h4 id="SQL的特点"><a href="#SQL的特点" class="headerlink" title="SQL的特点"></a>SQL的特点</h4><p>Structured Query Language 结构化查询语言</p>
<p>①综合统一</p>
<p>②高度非过程化</p>
<p>③面向对象的操作方式</p>
<p>④同一种语法结构提供两种使用方式（即是独立语言又是嵌入式语言）</p>
<p>⑤语言简介、易学易用（结构化查询语言）</p>
<h4 id="SQL语言的组成"><a href="#SQL语言的组成" class="headerlink" title="SQL语言的组成"></a>SQL语言的组成</h4><p>1.数据定义语言（DDL）</p>
<p>create：创建数据库、表、视图等</p>
<p>alter：修改</p>
<p>drop：删除数据库、表、视图等</p>
<p>rename：重命名</p>
<p>truncate：删除表中所有数据但不删除表</p>
<p>2.数据操作语言（DML）</p>
<p>select：查询</p>
<p>inset：添加新数据</p>
<p>update：更新现有的数据</p>
<p>delete：删除指定行的数据</p>
<p>3.数据控制语言（DCL）</p>
<p>DCL用于事务控制、并发控制、完整性和安全性控制等。</p>
<p>commit：保存修改</p>
<p>rollback：回滚修改</p>
<p>savepoint：设置保存点</p>
<p>locktable：锁定数据库中的一个表</p>
<h2 id="数据库编程"><a href="#数据库编程" class="headerlink" title="数据库编程"></a>数据库编程</h2><h3 id="常量"><a href="#常量" class="headerlink" title="常量"></a>常量</h3><p>常量：程序本身不能改变其值的数据</p>
<p>（1）字符串常量</p>
<p>使用单引号或双引号括起来的字符串序列，一般使用单引号。</p>
<p>（2）数值常量</p>
<p>整数常量和小数常量。可使用科学计数法表示，如 <strong>1e10</strong>、<strong>1.25E10</strong>*。</p>
<p>（3）日期和时间常量</p>
<p>单引号括的特定格式的字符日期值。如 <strong>‘yyyy\mm\dd hh:mm:ss’<strong>、</strong>yyyy-mm-dd hh:mm:ss</strong>.</p>
<p>日期时间之间可以使用比较符号比较大小</p>
<p>（4）布尔值常量</p>
<p>只有true、false两个值。SQL命令的运行结果：1表示true，0表示false。</p>
<p>（5）NULL值</p>
<p>NULL值适用于各种类型的字段，表示“不确定的值”。有NULL值参与的运算结果一定为NULL值。</p>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><p>变量：程序运行过程中可以改变其值的数据</p>
<h4 id="局部变量"><a href="#局部变量" class="headerlink" title="局部变量"></a>局部变量</h4><p>格式：@+局部变量名</p>
<p>（1）定义与赋值</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">set @局部变量名[,@局部变量名,···];#定义</span><br><span class="line">set @局部变量名 = 表达式1[,@局部变量名 = 表达式2,···];#定义并赋值</span><br><span class="line">select 列名[,列名,···] into @局部变量名[,@局部变量名,···] from 表名;#将查询到的数据赋值给局部变量名</span><br></pre></td></tr></table></figure>

<p>（2）显示</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select @局部变量名;</span><br></pre></td></tr></table></figure>

<p>（3）其他</p>
<p>局部变量名可作为 select语句 中 where子句 的比较条件。</p>
<p>如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from 表名 where name = @局部变量名</span><br></pre></td></tr></table></figure>

<h4 id="全局变量"><a href="#全局变量" class="headerlink" title="全局变量"></a>全局变量</h4><p>全局变量：MySQL 系统提供并赋值的变量。用户不能定义全局变量，只能使用。</p>
<p>格式：@@+全局变量名</p>
<table>
<thead>
<tr>
<th align="center">全局变量名</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">@@back_log</td>
<td align="center">返回MySQL主要连接请求的数量</td>
</tr>
<tr>
<td align="center">@@basedir</td>
<td align="center">返回MySQL安装目录基准</td>
</tr>
<tr>
<td align="center">@@license</td>
<td align="center">返回服务器的许可类型</td>
</tr>
<tr>
<td align="center">@@port</td>
<td align="center">返回服务器侦听TCP&#x2F;IP链接的端口号</td>
</tr>
<tr>
<td align="center">@@storage_engine</td>
<td align="center">返回存储引擎</td>
</tr>
<tr>
<td align="center">@@version</td>
<td align="center">返回服务器版本号</td>
</tr>
</tbody></table>
<p>显示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select @@全局变量名</span><br></pre></td></tr></table></figure>

<h3 id="SQL查询语句的执行顺序"><a href="#SQL查询语句的执行顺序" class="headerlink" title="SQL查询语句的执行顺序"></a>SQL查询语句的执行顺序</h3><p>先执行 FROM 确定主表，再执行 JOIN 连接，然后 WHERE 进行过滤，接着 GROUP BY 进行分组，HAVING 过滤聚合结果，SELECT 选择最终列，ORDER BY 排序，最后 LIMIT 限制返回行数。</p>
<p>WHERE 先执行是为了减少数据量，HAVING 只能过滤聚合数据，ORDER BY 必须在 SELECT 之后排序最终结果，LIMIT 最后执行以减少数据传输。</p>
<p>这个执行顺序与编写 SQL 语句的顺序不同，这也是为什么有时候在 SELECT 子句中定义的别名不能在 WHERE 子句中使用得原因，因为 WHERE 是在 SELECT 之前执行的。</p>
<p><strong>LIMIT 为什么在最后执行？</strong></p>
<p>因为 LIMIT 是在最终结果集上执行的，如果在 WHERE 之前执行 LIMIT，那么就会先返回所有行，然后再进行 LIMIT 限制，这样会增加数据传输的开销。</p>
<p><strong>ORDER BY 为什么在 SELECT 之后执行？</strong></p>
<p>因为排序需要基于最终返回的列，如果 ORDER BY 早于 SELECT 执行，计算 <code>COUNT(*)</code> 之类的聚合函数就会出问题。</p>
<table>
<thead>
<tr>
<th align="center">执行顺序</th>
<th align="center">SQL 关键字</th>
<th align="center">作用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">①</td>
<td align="center">FROM</td>
<td align="center">确定主表，准备数据</td>
</tr>
<tr>
<td align="center">②</td>
<td align="center">ON</td>
<td align="center">连接多个表的条件</td>
</tr>
<tr>
<td align="center">③</td>
<td align="center">JOIN</td>
<td align="center">执行 INNER JOIN &#x2F; LEFT JOIN 等</td>
</tr>
<tr>
<td align="center">④</td>
<td align="center">WHERE</td>
<td align="center">过滤行数据（提高效率）</td>
</tr>
<tr>
<td align="center">⑤</td>
<td align="center">GROUP BY</td>
<td align="center">进行分组</td>
</tr>
<tr>
<td align="center">⑥</td>
<td align="center">HAVING</td>
<td align="center">过滤聚合后的数据</td>
</tr>
<tr>
<td align="center">⑦</td>
<td align="center">SELECT</td>
<td align="center">选择最终返回的列</td>
</tr>
<tr>
<td align="center">⑧</td>
<td align="center">DISTINCT</td>
<td align="center">进行去重</td>
</tr>
<tr>
<td align="center">⑨</td>
<td align="center">ORDER BY</td>
<td align="center">对最终结果排序</td>
</tr>
<tr>
<td align="center">⑩</td>
<td align="center">LIMIT</td>
<td align="center">限制返回行数</td>
</tr>
</tbody></table>
<p><img src="https://cdn.paicoding.com/stutymore/mysql-20250306153200.png" alt="博客园数据派：查询语句执行顺序"></p>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><p>MySQL 的常用命令主要包括<strong>数据库操作命令、表操作命令、行数据 CRUD 命令、索引和约束的创建修改命令、用户和权限管理的命令、事务控制的命令</strong>等。</p>
<h4 id="数据库操作命令"><a href="#数据库操作命令" class="headerlink" title="数据库操作命令"></a>数据库操作命令</h4><p><code>CREATE DATABASE database_name;</code> 用于创建数据库；<code>DROP DATABASE database_name;</code> 用于删除数据库；<code>SHOW DATABASES;</code> 用于显示所有数据库；<code>USE database_name;</code> 用于切换数据库。</p>
<h4 id="表操作命令"><a href="#表操作命令" class="headerlink" title="表操作命令"></a>表操作命令</h4><p><code>CREATE TABLE table_name (列名1 数据类型1, 列名2 数据类型2,...);</code> 用于创建表；</p>
<p><code>DROP TABLE table_name;</code> 用于删除表；</p>
<p><code>SHOW TABLES;</code> 用于显示所有表；</p>
<p><code>DESCRIBE table_name;</code> 用于查看表结构；</p>
<p><code>ALTER TABLE table_name ADD column_name datatype;</code> 用于修改表。</p>
<h4 id="行数据的-CRUD-命令"><a href="#行数据的-CRUD-命令" class="headerlink" title="行数据的 CRUD 命令"></a>行数据的 CRUD 命令</h4><p><code>INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);</code> 用于插入数据；</p>
<p><code>SELECT column_names FROM table_name WHERE condition;</code> 用于查询数据；</p>
<p><code>UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition;</code> 用于更新数据；</p>
<p><code>DELETE FROM table_name WHERE condition;</code> 用于删除数据。</p>
<h4 id="索引和约束的创建修改命令"><a href="#索引和约束的创建修改命令" class="headerlink" title="索引和约束的创建修改命令"></a>索引和约束的创建修改命令</h4><p><code>CREATE INDEX index_name ON table_name (column_name);</code> 用于创建索引；</p>
<p><code>ALTER TABLE table_name ADD PRIMARY KEY (column_name);</code> 用于添加主键；</p>
<p><code>ALTER TABLE table_name ADD CONSTRAINT fk_name FOREIGN KEY (column_name) REFERENCES parent_table (parent_column_name);</code> 用于添加外键。</p>
<h4 id="用户和权限管理的命令"><a href="#用户和权限管理的命令" class="headerlink" title="用户和权限管理的命令"></a>用户和权限管理的命令</h4><p><code>CREATE USER &#39;username&#39;@&#39;host&#39; IDENTIFIED BY &#39;password&#39;;</code> 用于创建用户；</p>
<p><code>GRANT ALL PRIVILEGES ON database_name.table_name TO &#39;username&#39;@&#39;host&#39;;</code> 用于授予权限；</p>
<p><code>REVOKE ALL PRIVILEGES ON database_name.table_name FROM &#39;username&#39;@&#39;host&#39;;</code> 用于撤销权限；</p>
<p><code>DROP USER &#39;username&#39;@&#39;host&#39;;</code> 用于删除用户。</p>
<h4 id="事务控制的命令"><a href="#事务控制的命令" class="headerlink" title="事务控制的命令"></a>事务控制的命令</h4><p><code>START TRANSACTION;</code> 用于开始事务；</p>
<p><code>COMMIT;</code> 用于提交事务；</p>
<p><code>ROLLBACK;</code> 用于回滚事务。</p>
<h3 id="连接"><a href="#连接" class="headerlink" title="连接"></a>连接</h3><p>连接可分为三类：</p>
<p>inner join（内连接或等值连接）：获取两个表中字段匹配关系的记录，即2个表中值都存在的记录；</p>
<p>left join：获取左表中所有记录，即使右表中没有对应的匹配记录，右表中记录不存在则使用null替代；</p>
<p>right join：获取右表中所有记录，即使左表中没有对应的匹配记录，左表中记录不存在则使用null替代。</p>
<p>总结：</p>
<p>　　①A inner join B on {条件}：取交集；</p>
<p>　　②A left join B on {条件}：：取A全部，B没有对应的值，则为null；</p>
<p>　　③A right join B on {条件}：：取B全部，A没有对应的值，则为null；</p>
<p>　　④A full outer join B on {条件}：：取并集，彼此没有对应的值为null；</p>
<p>　　上述4种的对应条件，在on后填写。</p>
<h3 id="limit-和-offset"><a href="#limit-和-offset" class="headerlink" title="limit 和 offset"></a>limit 和 offset</h3><p>在 SQL 查询中，<strong>LIMIT</strong> 和 <strong>OFFSET</strong> 子句用于控制查询结果的返回行数和起始位置。它们通常用于分页查询，以便从大量数据中获取特定范围的记录。</p>
<p><strong>LIMIT</strong> 子句用于限制返回的记录数。它接受一个或两个整数参数：</p>
<ul>
<li>一个参数：表示返回的最大记录数。</li>
<li>两个参数：第一个参数表示偏移量（即跳过的记录数），第二个参数表示返回的最大记录数。</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 返回前3行数据</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> employees LIMIT <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 跳过前2行，返回第3行数据</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> employees LIMIT <span class="number">2</span>, <span class="number">1</span>;</span><br></pre></td></tr></table></figure>





<p><strong>OFFSET</strong> 子句用于指定查询结果的起始位置。它通常与 <strong>LIMIT</strong> 子句一起使用，以实现分页效果。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 跳过前2行，返回第3行数据</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> employees LIMIT <span class="number">1</span> <span class="keyword">OFFSET</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>操作系统学习笔记</title>
    <url>/2026/04/12/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h1><h2 id="什么是操作系统"><a href="#什么是操作系统" class="headerlink" title="什么是操作系统"></a>什么是操作系统</h2><p>操作系统(Operating System)，是介于硬件资源和应用程序之间的一个系统软件 ，能控制和管理整个计算机系统的硬件和软件资源，调度计算机的工作与资源的分配，进而为用户和其他软件提供服务，<strong>操作系统是计算机系统中最基本的系统软件。</strong></p>
<p>如果将它理解为“掌控计算机的系统”是否更能精确的描述OS所做的事情呢？</p>
<p>如果要更深入的掌握这个问题，可以问一问：如果没有了操作系统，你使用的PC机还能干什么？或者说，你能够使用你的PC机做什么呢？</p>
<h2 id="操作系统的特征："><a href="#操作系统的特征：" class="headerlink" title="操作系统的特征："></a>操作系统的特征：</h2><ul>
<li>并发：并发指的是两个或多个事件在同一时间间隔内发生，计算机系统中同时存在多个运行的程序，因此具有处理和调度多个程序同时执行的能力。</li>
</ul>
<blockquote>
<p>⚠️注意：并行和并发的区别：并发指的是同一时间间隔，并行指的是同一时刻。</p>
</blockquote>
<ul>
<li>共享：系统中的资源可以供内存中多个并发执行的进程共同使用。<ul>
<li>互斥共享：一段时间内只允许一个进程访问该资源。<strong>一段时间内只允许一个进程访问的资源称为临界资源。</strong></li>
<li>同时访问：一段时间内允许多个进程“同时”访问，“同时”通常是宏观的，实际上是交替的对该资源进行访问。</li>
</ul>
</li>
<li>虚拟：把一个物理上的实体变为若干逻辑上的对应物。</li>
<li>异步：进程的执行并不是一贯到底的，而是以不可预知的速度向前推进。</li>
</ul>
<h2 id="操作系统的功能"><a href="#操作系统的功能" class="headerlink" title="操作系统的功能"></a>操作系统的功能</h2><blockquote>
<p>操作系统位于硬件资源之上，管理硬件资源; 应用程序之下，为应用程序提供服务，同时管理应用程序</p>
</blockquote>
<p><strong>1、资源分配，资源回收</strong></p>
<p>计算机必要重要的硬件资源无非就是 CPU、内存、硬盘、I&#x2F;O设备。 而这些资源总是有限的，因此需要有效管理，资源管理最终只有两个问题：资源分配、资源回收。</p>
<p><strong>资源分配：</strong> 体现在CPU上，比如进程调度，多个进程同时请求CPU下，应该给哪一个进程呢？再比如内存分配，内存不够了怎么办？A进程非法访问了B进程的内存地址怎么办？内存内、外碎片问题等。</p>
<p><strong>资源回收：</strong> 考虑内存回收后的合并等等。</p>
<p><strong>2、为应用程序提供服务</strong></p>
<p>操作系统将硬件资源的操作封装起来，提供相对统一的接口（系统调用）供开发者调用。</p>
<p>如果没有操作系统，应用程序将直接面对硬件，除去给开发者带来的编程困难不说，直接访问硬件，使用不当极有可能直接损坏硬件资源。</p>
<p><strong>3、管理应用程序</strong></p>
<p>即控制进程的生命周期：进程开始时的环境配置和资源分配、进程结束后的资源回收、进程调度等。</p>
<p><strong>4、操作系统内核的功能</strong></p>
<p>（1）<strong>进程调度能力：</strong> 管理进程、线程，决定哪个进程、线程使用CPU。</p>
<p>（2）<strong>内存管理能力：</strong> 决定内存的分配和回收。</p>
<p>（3）<strong>硬件通信能力：</strong> 管理硬件，为进程和硬件之间提供通信。</p>
<p>（4）<strong>系统调用能力：</strong> 应用程序进行更高限权运行的服务，需要系统调用，用户程序和操作系统之间的接口。</p>
<h2 id="操作系统的角色"><a href="#操作系统的角色" class="headerlink" title="操作系统的角色"></a>操作系统的角色</h2><p><strong>1、管理者</strong></p>
<p>主要分为：CPU管理、内存管理、外存管理、IO管理；以及自己的健壮性和安全性管理。</p>
<blockquote>
<p>健壮性，又称鲁棒性，即使很粗鲁的对待程序，它还是可以很好的运行。</p>
</blockquote>
<p><strong>2、魔术师:</strong></p>
<p>比如操作系统会让每个进程都觉得自己独占CPU、独占整片物理内存，而实际上每个进程都只是在某一时间段内占用CPU，仅仅只是占用实际一点点物理内存。</p>
<h2 id="用户程序与操作系统的关系"><a href="#用户程序与操作系统的关系" class="headerlink" title="用户程序与操作系统的关系"></a>用户程序与操作系统的关系</h2><blockquote>
<p>用户程序和操作系统之间是<strong>相互调用</strong>的关系</p>
</blockquote>
<p><strong>1、操作系统的角度</strong></p>
<p>计算机启动后启动的第一个软件就是操作系统，随后启动的所有进程都运行在操作系统之上，使用操作系统提供的服务，同时被操作系统监控，进程结束后也由操作系统回收。</p>
<p><strong>2、进程角度</strong></p>
<p>调用操作系统提供的服务，实现自己的功能。</p>
<h1 id="进程与线程"><a href="#进程与线程" class="headerlink" title="进程与线程"></a>进程与线程</h1><h2 id="进程和线程的区别？"><a href="#进程和线程的区别？" class="headerlink" title="进程和线程的区别？"></a>进程和线程的区别？</h2><h3 id="【简要回答】"><a href="#【简要回答】" class="headerlink" title="【简要回答】"></a>【简要回答】</h3><h4 id="进程和线程的概念"><a href="#进程和线程的概念" class="headerlink" title="进程和线程的概念"></a>进程和线程的概念</h4><ul>
<li><strong>进程</strong>：进程（<strong>Process</strong>）是计算机中一个执行的程序实例，每个进程对应一个<strong>运行中</strong>的程序；进程是操作系统<strong>资源分配</strong>的最小单位，在未引入线程概念的操作系统中，同样是<strong>调度</strong>的最小单位。</li>
<li><strong>线程</strong>：线程（<strong>Threads</strong>）是比进程更小的概念，也称“轻量级进程（Light Weight Process, LWP），是将进程进一步细分的单位；在多道程序操作系统中，线程概念的引入可以进一步提高程序的并发执行程度，一个进程可以同时拥有多个线程，借助多个线程实现程序的并发执行（而不必借助进程）。</li>
</ul>
<h4 id="进程和线程的对比"><a href="#进程和线程的对比" class="headerlink" title="进程和线程的对比"></a>进程和线程的对比</h4><ul>
<li><strong>调度单位</strong>：引入内核级线程后，进程不再是调度的最小单位；线程是调度的最小单位。</li>
<li><strong>资源分配</strong>：无论是否引入“线程”，进程始终都是资源分配的最小单位；线程只拥有少量资源（来自创建它的进程）。</li>
<li><strong>并发性</strong>：多个进程间可以并发执行；一个进程的多个线程也能并发执行。</li>
<li><strong>独立性</strong>：进程之间默认不共享全局变量，且拥有独立的地址空间和不同资源；线程之间只有少数资源不能共享（例如线程的栈区）。</li>
<li><strong>系统开销</strong>：“进程通信”，“进程切换”开销大；“线程通信”，“线程切换”开销小。</li>
<li><strong>多处理机</strong>：单个进程只能运行在一个处理机上；多线程进程可以充分利用多处理机（内核级线程）。</li>
<li><strong>协同工作</strong>：进程之间互不影响；用户级线程的阻塞会影响到整个进程的执行。</li>
</ul>
<h3 id="【详细回答】"><a href="#【详细回答】" class="headerlink" title="【详细回答】"></a>【详细回答】</h3><ul>
<li>进程是具有独立功能的程序在一个数据集合上运行的过程，它是系统进行资源分配和调度的独立单位。</li>
<li>进程的主要特点：<ol>
<li><strong>动态性</strong>：从创建到消亡，是动态的，具有生命周期。</li>
<li><strong>并发性</strong>：多个进程并发执行时，会有以下3个特性：<ul>
<li>间断性：并发的进程有间断运行的特点，当多个进程协同完成同一任务时，需要进行同步等操作，进而导致进程的暂停。</li>
<li>失去封闭性：并发进程会共享系统中的资源，任意进程都可能改变某个资源的状态，从而影响到其他进程。失去封闭性意味着进程的执行结果取决于其执行速度，使得进程的执行有不可再现性（每次执行的结果不一定相同）；<strong>失去封闭性导致了不可再现性</strong>。</li>
<li>不可再现性：在相同的初始环境和条件下，并发执行的进程可能得到不同的结果。</li>
</ul>
</li>
<li><strong>独立性</strong>：进程能够独立运行，是资源分配的最小单位，各个进程获得的资源独立于其他进程。</li>
<li><strong>异步性</strong>：进程以不可预知的状态向前推进。<strong>进程的并发性导致了进程的异步性</strong>。但是，通过同步与互斥，可以保证在异步性的前提下得到确定（可再现）的结果。</li>
</ol>
</li>
<li>未引入线程概念时，进程有两个特点：<br>①是<strong>资源所有权</strong>最小单位，指一个进程拥有对资源的控制和所有权，包括内存、I&#x2F;O通道、I&#x2F;O设备等；<br>②是<strong>调度&#x2F;执行</strong>的最小单位，指进程是一个可以被操作系统内核调度的独立实体。</li>
<li>“线程”可以看作是在<strong>调度&#x2F;执行</strong>这一属性上对进程的进一步细分——在引入线程前，进程是操作系统调度的最小单位；在引入线程且是由操作系统内核支持的线程（即<strong>内核级线程</strong>）后，线程是操作系统调度的最小单位。</li>
<li>引入线程的优势：<ol>
<li><strong>进一步提高并发性</strong>：在单处理机系统中，线程模型可以分离同一个进程内的CPU计算和I&#x2F;O访问，让它们并发执行；在多处理机系统中，引入线程模型后，每个内核级线程都能占用一个处理机，这使得多处理机系统的优势得以发挥。</li>
<li><strong>共享地址空间与可用数据</strong>：线程之间可以直接共享数据而不需要经由操作系统内核，在速度上有很大优势。</li>
<li><strong>更轻量级</strong>：在很多操作系统中，一个线程的创建比一个进程的创建快10~100倍。（线程的切换和撤销也远快于进程）</li>
<li><strong>更好的交互性</strong>：线程可以提高图形界面程序的交互性，避免程序“卡死”，使用户得到更好的体验。</li>
</ol>
</li>
</ul>
<h3 id="【知识图解】"><a href="#【知识图解】" class="headerlink" title="【知识图解】"></a>【知识图解】</h3><ol>
<li><strong>进程的基本结构和资源所有权</strong>，示意图如下：<br><img src="https://file1.kamacoder.com/i/bagu/processStructure_resourceOwnership.jpg" alt="image"></li>
<li><strong>多进程模型 和 多线程模型 的内存布局</strong>，示意图如下：<br><img src="https://file1.kamacoder.com/i/bagu/multi-pricesses_multi-threads_memoryLayout.jpg" alt="image"></li>
</ol>
<h3 id="【知识拓展】"><a href="#【知识拓展】" class="headerlink" title="【知识拓展】"></a>【知识拓展】</h3><ul>
<li>线程的实现方式主要分为两类——<strong>内核级线程</strong>（Kernel Supported Threads，KST）和<strong>用户级线程</strong>（User-Level Threads，ULT）。<ol>
<li><strong>用户级线程</strong>：线程的管理由应用程序实现，在用户空间下完成，操作系统感知不到线程的存在。在这种实现方式下，<strong>系统调度的最小单位依然是进程</strong>，线程的调度则由其所在进程实现。</li>
<li><strong>内核级线程</strong>：需要操作系统内核的支持，其创建、阻塞、撤销和切换都是在内核空间下实现的，内核通过TCB（线程控制块）来对线程进行感知和控制。在这种实现方式下，<strong>线程是系统调度的最小单位</strong>。</li>
</ol>
</li>
</ul>
<h2 id="进程间有哪些通信方式？"><a href="#进程间有哪些通信方式？" class="headerlink" title="进程间有哪些通信方式？"></a>进程间有哪些通信方式？</h2><h3 id="简要回答"><a href="#简要回答" class="headerlink" title="简要回答"></a>简要回答</h3><ol>
<li><strong>信号量机制（Semaphore）</strong>：进程通过<strong>P &#x2F; V操作</strong>控制信号量，可以有效地解决<strong>同步和互斥</strong>问题；信号量机制适合需要同步的场景，如避免竞争条件和死锁。</li>
<li><strong>共享存储机制（Shared Memory）</strong>：共享内存是一种高效的进程间通信方式，允许多个进程直接访问同一块内存区域。共享存储机制适合需要<strong>高效数据传输</strong>的场景。</li>
<li><strong>消息传递机制（Message Passing）</strong>：消息传递机制需要操作系统<strong>内核</strong>的支持，通过<strong>消息队列</strong> 或 <strong>信箱</strong>传递数据，消息以数据块的形式传输。消息传递机制支持不同类型的数据，适用于无关进程之间的通信。</li>
<li><strong>管道通信机制（Pipeline）</strong>：管道通信机制适合进程间的简单数据传输，其又可细分为两类——<strong>①匿名管道</strong>：半双工通信，适用于父子进程或兄弟进程；<strong>②命名管道</strong>：具有文件名，适用于无关进程。</li>
<li><strong>套接字机制（Socket）</strong>：Socket（套接字）是一种<strong>网络通信</strong>机制，可以在不同主机上的进程之间进行通信；Socket机制适合分布式系统和网络通信。</li>
</ol>
<h3 id="详细回答"><a href="#详细回答" class="headerlink" title="详细回答"></a>详细回答</h3><ol>
<li><strong>信号量机制（Semaphore）</strong>：信号量是一种<strong>同步通信机制</strong>，用于控制多个进程对共享资源的访问；信号量支持<strong>两种原子操作：P()（等待）和V()（信号）</strong>。</li>
<li><strong>共享存储机制（Shared Memory）</strong>：共享存储机制又可细分为两种方式——①<strong>共享数据结构</strong>的通信方式：进程间共享同一个数据结构，编程人员需要考虑进程间的同步互斥问题，传输效率低；②<strong>共享存储区</strong>的通信方式：操作系统在内存中开辟一块共享存储区，多个进程可以对该内存区域进行读写，传输效率高。当进程希望对共享存储区进行存取时，会将自己的虚地址空间映射到共享存储区，并向操作系统提出申请。</li>
<li><strong>消息传递机制（Message Passing）</strong>：进程之间可以调用由操作系统实现的通信命令来传递消息；实现消息传递即分别实现<strong>Send</strong> 和 <strong>Receive</strong>两个<strong>原语</strong>，用于消息的发送和接收。消息传递机制又可细分为两类——①<strong>直接通信方式</strong>：发送进程直接将消息发送给接收进程，将信息挂载到<strong>接收进程的消息队列</strong>上，接收进程从自己的消息队列中获取信息；②<strong>间接通信方式</strong>：操作系统额外<strong>创建一片空间用于存储各种消息</strong>，类似一个信箱，发送进程和接收进程依靠这个中间实体实现信息的发送和接收，这种方式也被称为 “信箱通信模式”，在计算机网络中被广泛运用。</li>
<li><strong>管道通信机制（Pipeline）</strong>：管道机制又可细分为两种类型——<br><strong>①匿名管道</strong>：即我们平时所说的“管道”通信，它使用一种共享文件来实现进程间通信，这种共享文件被称为“管道” 或 “pipe文件”，可以连接一个读进程和一个写进程。写进程以字节流的形式，将数据送入管道，读进程通过管道将数据读出。匿名管道是一种半双工的通信方式，数据只能单向流动，适用于父子进程或兄弟进程之间的通信；在UNIX&#x2F;Linux系统中，使用pipe()系统调用创建匿名管道。<br><strong>②命名管道（Named Pipe&#x2F;FIFO）</strong>：一种特殊的管道，具有文件名，可以在无关进程之间使用；在UNIX&#x2F;Linux系统中，使用mkfifo()系统调用创建命名管道。</li>
<li><strong>套接字机制（Socket）</strong>：Socket是一种<strong>网络通信</strong>机制，支持不同主机上的进程之间进行通信。Socket支持网络通信，适用于分布式系统，其通信方式可以是面向连接的（如TCP），也可以是无连接的（如UDP）。</li>
</ol>
<h3 id="知识图解"><a href="#知识图解" class="headerlink" title="知识图解"></a>知识图解</h3><ol>
<li><strong>消息传递机制的示意图</strong>如下所示：<br><img src="https://file1.kamacoder.com/i/bagu/Message_Passing.jpg" alt="image"></li>
<li><strong>管道实现全双工通信</strong>的示意图，如下所示：<br><img src="https://file1.kamacoder.com/i/bagu/full_duplex_communication.jpg" alt="image"></li>
</ol>
<h3 id="知识拓展"><a href="#知识拓展" class="headerlink" title="知识拓展"></a>知识拓展</h3><ul>
<li>操作系统实现管道机制，需要提供三种机制——<strong>①</strong>同步机制；<strong>②</strong>互斥机制；<strong>③</strong>通信进程间确定对方存在的机制。</li>
<li><strong>管道具有以下特点</strong>：<ol>
<li>同步：写进程向管道中写入一定量数据后，会进入阻塞状态，等待读进程将数据读出后将其唤醒；读进程在将管道中数据读完后，也会进入阻塞状态，等待写进程对管道写操作完毕后将其唤醒。</li>
<li>互斥：当有一个进程在对一个管道读&#x2F;写时，其他想要读&#x2F;写该管道的进程必须等待。</li>
<li>管道需要通信双方确定对方的存在，否则会陷入阻塞。</li>
<li>管道是一个固定大小的 缓冲区（如4KB），在<strong>内核</strong>中实现，其大小不受磁盘大小的影响。</li>
<li>管道的实质是一个共享文件，读进程将管道视为输出文件，写进程将管道视为输入文件，因此管道通信机制可以利用文件系统机制实现。</li>
<li>读数据操作是一次性的，管道中的数据一旦被读取就会被抛弃。</li>
<li>管道是<strong>半双工通信</strong>的，某一时刻，数据只能单向传输，若希望实现双向传输（即全双工通信），可以使用两个管道。</li>
</ol>
</li>
<li>当多个进程读写同一个管道时，可能会出现读写错乱，解决方案——<ol>
<li>一个管道允许多个写进程，一个读进程；</li>
<li>允许有多个写进程，多个读进程，但系统会让各个读进程轮流从管道中读数据。</li>
</ol>
</li>
</ul>
<h2 id="进程调度算法你了解多少"><a href="#进程调度算法你了解多少" class="headerlink" title="进程调度算法你了解多少"></a>进程调度算法你了解多少</h2><h3 id="简要回答-1"><a href="#简要回答-1" class="headerlink" title="简要回答"></a>简要回答</h3><ol>
<li><strong>先来先服务（FCFS, First-Come, First-Served）</strong>：<ul>
<li><strong>工作原理</strong>：进程按照到达顺序进入就绪队列，CPU依次执行队列中的进程。FCFS是<strong>非抢占式</strong>，一旦进程开始执行，直到完成才会释放CPU。</li>
<li><strong>适用情景</strong>：批处理系统，任务执行时间差异不大的场景。</li>
<li><strong>优点</strong>：实现<strong>简单</strong>，<strong>公平</strong>性较高。</li>
<li><strong>缺点</strong>：可能导致“饥饿”现象，长作业会阻塞短作业；平均等待时间较长，<strong>不适合交互式系统</strong>。</li>
</ul>
</li>
<li><strong>短进程优先调度算法（SPF和SRT）</strong>：<ul>
<li><strong>工作原理</strong>：<br><strong>1&gt; SPF（Shortest Process First）</strong>：非抢占式，选择当前就绪队列中<strong>运行时间最短</strong>的进程执行。<br><strong>2&gt; SRT（Shortest Remaining Time）</strong>：抢占式，选择<strong>剩余运行时间最短</strong>的进程执行。</li>
<li><strong>适用情景</strong>：适合批处理系统，尤其是短作业较多的场景。</li>
<li><strong>优点</strong>：<br><strong>1&gt; SPF（Shortest Process First）</strong>：减少平均等待时间，适合短作业。<br><strong>2&gt; SRT（Shortest Remaining Time）</strong>：进一步优化平均等待时间。</li>
<li><strong>缺点</strong>：<br><strong>1&gt; SPF（Shortest Process First）</strong>：长作业可能“饥饿”。<br><strong>2&gt; SRT（Shortest Remaining Time）</strong>：需要频繁计算剩余时间，开销较大。</li>
</ul>
</li>
<li><strong>优先级调度算法（PSA, Priority Scheduling Algorithm）</strong>：<ul>
<li><strong>工作原理</strong>：每个进程分配一个优先级，CPU选择优先级最高的进程执行；可以是<strong>抢占式或非抢占式</strong>。</li>
<li><strong>适用情景</strong>：适合<strong>实时系统</strong>或需要区分任务优先级的场景。</li>
<li><strong>优点</strong>：灵活，可根据任务重要性调整优先级。</li>
<li><strong>缺点</strong>：低优先级进程可能“饥饿”；动态调整优先级会增加开销。</li>
</ul>
</li>
<li><strong>高响应比优先调度算法（HRRN, Highest Response Ratio Next）</strong>：<ul>
<li><strong>工作原理</strong>：选择响应比最高的进程执行，<strong>响应比 &#x3D; (等待时间 + 运行时间) &#x2F; 运行时间</strong>。</li>
<li><strong>适用情景</strong>：适合批处理系统，任务执行时间差异较大的场景，兼顾等待时间和运行时间。</li>
<li><strong>优点</strong>：兼顾公平性和效率，减少平均等待时间。</li>
<li><strong>缺点</strong>：需要计算响应比，开销较大。</li>
</ul>
</li>
<li><strong>时间片轮转调度算法（RR, Round Robin）</strong>：<ul>
<li><strong>工作原理</strong>：每个进程分配一个时间片（如10ms），时间片用完后切换到下一个进程。</li>
<li><strong>适用情景</strong>：<strong>交互式系统</strong>，如桌面操作系统。</li>
<li><strong>优点</strong>：公平性高，适合交互式系统。</li>
<li><strong>缺点</strong>：时间片过大会退化为FCFS。</li>
</ul>
</li>
<li><strong>多级队列调度算法（Multilevel Queue Scheduling）</strong>：<ul>
<li><strong>工作原理</strong>：将进程按优先级或类型分配到不同的队列，<strong>每个队列采用不同的调度算法</strong>（如FCFS、RR）；队列之间可以有优先级，高优先级队列的进程优先执行。</li>
<li><strong>适用情景</strong>：适合任务类型多样的系统，如混合批处理和交互式任务。</li>
<li><strong>优点</strong>：灵活，可根据任务类型定制调度策略。</li>
<li><strong>缺点</strong>：可能导致低优先级队列的进程“饥饿”。</li>
</ul>
</li>
<li><strong>多级反馈队列调度算法（Multilevel Feedback Queue Scheduling）</strong>：<ul>
<li><strong>工作原理</strong>：将就绪队列分为多个子队列，每个队列的时间片大小不同；<strong>新进程到达首先进入最高优先级队列，时间片用完后降级到下一队列</strong>；低优先级队列的时间片较大，避免频繁切换。</li>
<li><strong>适用情景</strong>：通用操作系统，如Linux、Windows。</li>
<li><strong>优点</strong>：能较好地满足各类进程对处理器的需求；适合通用操作系统。</li>
<li><strong>缺点</strong>：实现复杂，开销较大。</li>
</ul>
</li>
</ol>
<h3 id="详细回答-1"><a href="#详细回答-1" class="headerlink" title="详细回答"></a>详细回答</h3><ol>
<li><strong>先来先服务（FCFS, First-Come, First-Served）</strong>：<ul>
<li><strong>工作原理</strong>：也称为FIFO（First In First Out），可以适用于作业调度，进程调度；每次进程调度时，系统会从就绪队列中选择一个最先进入该队列的进程，将该进程的状态转为执行态并为其分配处理器资源，直到下一个调度的时机到来。</li>
<li><strong>适用情景</strong>：批处理系统，任务执行时间差异不大的场景。</li>
<li><strong>优点</strong>：逻辑<strong>简单</strong>，<strong>公平</strong>性较高。</li>
<li><strong>缺点</strong>：可能导致“饥饿”现象，长作业会阻塞短作业；效率差，平均等待时间较长，<strong>不适合交互式系统</strong>；无法实现人机交互；未考虑到不同进程间的差异性（例如进程的紧急性）；更加偏向处理器密集型进程 和 长进程。</li>
</ul>
</li>
<li><strong>短进程优先调度算法（SPF和SRT）</strong>：- <strong>工作原理</strong>： 可以适用于作业调度、进程调度。 <strong>PS：**有些教材将SPF专指非抢占式短进程优先调度算法，而将抢占式短进程优先调度算法称为最短剩余时间优先调度算法，即SRT，此处也这样划分。<br>**1&gt; SPF（Shortest Process First）**：**非抢占式**，在进程调度时，根据就绪队列中排队的进程所预估的处理器使用时间，每次调度选择预估剩余处理器使用时间最短的进程；在作业调度时，称为短作业优先调度算法（Shortest Job First, SJF），根据外存队列中作业所要求的执行时间来调度作业，每次调度选择预估剩余处理器使用时间最短的作业。<br>**2&gt; SRT（Shortest Remaining Time）**：**抢占式**，选择**剩余运行时间最短**的进程执行；如果在当前进程运行过程中，就绪队列中出现了要求时间更短的进程，则这个要求时间更短的进程会</strong>抢占<strong>处理器资源，当前运行的进程状态会由执行态变为就绪态。- <strong>适用情景</strong>：适合批处理系统，尤其是短作业较多的场景。- <strong>优点</strong>：<br><strong>1&gt; SPF（Shortest Process First）</strong>：较FCFS性能更好，</strong>如果调度时满足“待调度进程同时可运行” 或者 “待调度进程都几乎同时到达”，那么SPF的平均等待时间和平均周转实际是最优的**。<br><strong>2&gt; SRT（Shortest Remaining Time）</strong>：进一步优化平均等待时间。- <strong>缺点</strong>：<br><strong>1&gt; SPF（Shortest Process First）</strong>：长作业可能“饥饿”；算法需要进程预估其运行时间，在预估时，可能出现估算时间不准确 或者 进程“谎报”时间等问题；同FCFS一样，未考虑到不同进程间的差异性。<br><strong>2&gt; SRT（Shortest Remaining Time）</strong>：需要频繁计算剩余时间，开销较大。</li>
<li><strong>优先级调度算法（PSA, Priority Scheduling Algorithm）</strong>：<ul>
<li><strong>工作原理</strong>：可以适用于作业调度、进程调度；作业（进程）的优先级由外界赋予，系统只需要根据外界赋予的优先级来进行调度即可；根据是否可以抢占，优先级调度算法可以分为“<strong>非抢占式优先级调度算法</strong>” 和 “<strong>抢占式优先级调度算法</strong>”；根据在调度程序运行过程中优先级是否可以动态变化，优先级调度算法又可分为“<strong>静态优先级</strong>” 和 “<strong>动态优先级</strong>”。对于静态优先级，是指各个进程的优先级在调度程序运行之初就已经确定好，整个运行期间不会改变；对于动态优先级，是指在调度程序运行的过程中，各个进程的优先级是动态变化的。</li>
<li><strong>适用情景</strong>：适合<strong>实时系统</strong>或需要区分任务优先级的场景。</li>
<li><strong>优点</strong>：灵活，可根据任务重要性调整优先级。</li>
<li><strong>缺点</strong>：低优先级进程可能“饥饿”；动态调整优先级会增加开销。</li>
</ul>
</li>
<li><strong>高响应比优先调度算法（HRRN, Highest Response Ratio Next）</strong>：<ul>
<li><strong>工作原理</strong>：可以适用于作业调度、进程调度；高响应比优先调度算法综合考虑了进程（作业）的等待时间和运行时间来调度进程，<strong>响应比 &#x3D; (等待时间 + 运行时间) &#x2F; 运行时间</strong>；随着进程的等待时间增加，该进程的优先级就会变高，更容易被调度。。</li>
<li><strong>适用情景</strong>：适合批处理系统，任务执行时间差异较大的场景，兼顾等待时间和运行时间。</li>
<li><strong>优点</strong>：兼顾公平性和效率，减少平均等待时间。</li>
<li><strong>缺点</strong>：需要计算响应比，开销较大。</li>
</ul>
</li>
<li><strong>时间片轮转调度算法（RR, Round Robin）</strong>：<ul>
<li><strong>工作原理</strong>：适用于进程调度一般不适用于作业调度；RR算法将处理器使用时间分成一个个时间片，公平地分给每一个等待处理器资源的就绪进程，当时钟中断发生时，系统会修改当前进程在时间片内的剩余时间，当一个进程分配的时间片耗尽后，其会重新进入就绪队列等待下一次分配时间片。<strong>系统会将所有就绪进程按照FCFS算法来维护就绪队列，每个时间片结束后操作系统都会结束当前进程，将当前进程放回到就绪队列末尾，并调度下一个就绪程序运行</strong>；在这种策略下，每个进程都能公平地使用处理器资源，也保证了进程的响应时间。</li>
<li><strong>适用情景</strong>：分时操作系统，交互式系统，如桌面操作系统。</li>
<li><strong>优点</strong>：公平性高，适合交互式系统。</li>
<li><strong>缺点</strong>：时间片过大会退化为FCFS。</li>
</ul>
</li>
<li><strong>多级队列调度算法（Multilevel Queue Scheduling）</strong>：<ul>
<li><strong>工作原理</strong>：仅适用于进程调度；该算法将就绪队列置为多个，根据就绪进程的特点和类型，将其分配在不同的就绪队列中。<strong>针对每个就绪队列，可以采用不同的调度算法</strong>；这样不但可以凸显出各种调度算法的优势，也能在一定程度上弥补这些算法的缺点。每个队列都可以是抢占式的，也都可以是非抢占式的，并且也都可能导致“饥饿”现象，具体要取决于各个队列的调度策略和优先级设置。<br>尤其是在<strong>多处理器系统</strong>中，可以利用该调度算法为每个处理器资源指定一个就绪队列，这样既可以根据进程类型和紧急程度等因素为进程分配更合理的就绪队列，还有利于需要多组线程或进程协作完成任务的情况，即将这些线程或进程分配到不同的就绪队列中，使得它们可以同时获得处理器资源。</li>
<li><strong>适用情景</strong>：适合任务类型多样的系统，如混合批处理和交互式任务。</li>
<li><strong>优点</strong>：灵活，可根据任务类型定制调度策略。</li>
<li><strong>缺点</strong>：可能导致低优先级队列的进程“饥饿”。</li>
</ul>
</li>
<li><strong>多级反馈队列调度算法（Multilevel Feedback Queue Scheduling）</strong>：- <strong>工作原理</strong>：仅适用于进程调度；将就绪队列分为多个子队列，每个子队列的时间片大小不同；<strong>为每个子队列依次分配递减等级的优先级</strong>，第一个就绪队列拥有最高的优先级，第二个就绪队列的优先级次于第一个就绪队列，以此类推；在每个就绪队列中，根据该队列的优先级，划分大小不同的时间片，<strong>一般高优先级的队列中时间片更小，低优先级的队列中时间片更大</strong>。<br><strong>PS_1:</strong> 当一个作业的进程被创建并分配资源后，首先将其放入<strong>第一个就绪队列</strong>的末尾，并依据<strong>FCFS算法</strong>进行调度，如果该进程并调度执行后在一个时间片内未完成其任务，就会被降级到下一个队列，重新进行上述过程，一直到进程被降级到<strong>最低优先级</strong>的队列中，此时进程不会继续降级，而是在最低优先级的就绪队列中遵循<strong>时间片轮转调度算法（RR）</strong> 等待调度。<br><strong>PS_2:</strong> 调度时，按照队列的优先级进行调度，高优先级就绪队列中的进程会被优先调度，对于第i级队列，只有当1 ~ i-1队列都不存在就绪进程时，才可以调度i级队列中的进程；如果当前使用处理器资源的进程来自第i级队列，而此时第一级队列中进入了新的就绪进程，那么会立刻进行<strong>抢占式</strong>进程调度，并将此时<strong>正在运行的程序放回i级队列末尾</strong>。- <strong>适用情景</strong>：通用操作系统，如Linux、Windows。- <strong>优点</strong>：能较好地满足各类进程对处理器的需求；适合通用操作系统（许多现代操作系统使用的调度算法都是基于MLFQ的变体）。- <strong>缺点</strong>：实现复杂，开销较大。</li>
</ol>
<h3 id="知识图解-1"><a href="#知识图解-1" class="headerlink" title="知识图解"></a>知识图解</h3><ol>
<li><strong>七种调度算法的对比，如下图所示：</strong> <img src="https://file1.kamacoder.com/i/bagu/seven_common_Process_Scheduling_algorithms_form.jpg" alt="image"></li>
<li><strong>多级队列调度算法</strong>，示意图如下：<br><img src="https://file1.kamacoder.com/i/bagu/multiLevel_Queue_Scheduling.jpg" alt="image"></li>
<li><strong>多级反馈队列调度算法</strong>，示意图如下：<br><img src="https://file1.kamacoder.com/i/bagu/multiLevel_Feedback_Queue.jpg" alt="image"></li>
</ol>
<h3 id="知识拓展-1"><a href="#知识拓展-1" class="headerlink" title="知识拓展"></a>知识拓展</h3><ul>
<li>在多道程序系统中，一个作业从被提交并加入等待队列开始，到运行结束这一过程中，可能发生<strong>三种层次的调度</strong>，分别是“作业调度”，“内存调度” 和 “进程调度”，根据调度发生的层次，又依次称为“高级调度”，“中级调度” 和 “低级调度”——<ol>
<li><strong>高级调度（High Level Scheduling）</strong>：又称作业调度 或 长程调度，调度的对象是外存中等待的<strong>作业</strong>，调度的行为是将作业从外存调度到内存中；高级调度的发生频率较低。</li>
<li><strong>中级调度（Intermediate Scheduling）</strong>：又称内存调度，调度的对象是暂时不能运行的进程，调度的行为是将<strong>目标进程的相关数据和资源</strong>在内存和外存间移动，即将进程由就绪态→就绪挂起态，或者是阻塞态→阻塞挂起态（涉及到进程 &#x2F; 线程的七状态模型）；中级调度的发生频率不高也不低。</li>
<li><strong>低级调度（Low Level Scheduling）</strong>：又称进程调度，调度的对象是<strong>进程（或内核级线程）</strong>，调度的行为是决定将处理器资源先分配给哪个进程；低级调度的发生频率很高。</li>
</ol>
</li>
</ul>
<h2 id="进程调度算法有哪些？适用场景是什么？"><a href="#进程调度算法有哪些？适用场景是什么？" class="headerlink" title="进程调度算法有哪些？适用场景是什么？"></a>进程调度算法有哪些？适用场景是什么？</h2><p>面试时没有回答好问题是很正常的，被面试官质疑时，要有今天不会明天会的底气，面后做好复盘，不在同一道题目上跌倒第二次，今天就从 C++中的inline函数 vs #define宏的区别 背起来</p>
<p>你对进程调度算法的了解有哪些？适用场景是什么？</p>
<h3 id="简要回答-2"><a href="#简要回答-2" class="headerlink" title="简要回答"></a>简要回答</h3><p>进程调度算法是<strong>操作系统决定哪个就绪进程获得CPU时间的方法</strong>。</p>
<p>主要分为<strong>非抢占式</strong>（FCFS、SJF）和<strong>抢占式</strong>（RR、优先级调度、多级队列）。</p>
<p>现代系统采用多级反馈队列（MLFQ）结合<strong>多种策略，平衡响应时间、吞吐量和公平性</strong>。</p>
<h3 id="详细回答-2"><a href="#详细回答-2" class="headerlink" title="详细回答"></a>详细回答</h3><ul>
<li>调度算法分类体系：</li>
</ul>
<p><strong>批处理调度</strong>：FCFS、SJF、HRRN - 注重<strong>吞吐量</strong></p>
<p><strong>交互式调度</strong>：RR、优先级调度 - 注重<strong>响应时间</strong></p>
<p><strong>实时调度</strong>：EDF、RMS - <strong>满足时间约束</strong></p>
<p><strong>混合调度</strong>：MLFQ - <strong>自适应多种负载</strong></p>
<ul>
<li>关键调度指标：</li>
</ul>
<p>周转时间：从<strong>提交到完成的总时间</strong></p>
<p>响应时间：从<strong>提交到首次响应</strong>的时间</p>
<p>吞吐量：<strong>单位时间完成的进程数</strong></p>
<p>公平性：<strong>各进程获得CPU时间的均衡度</strong></p>
<p>CPU利用率：<strong>CPU忙碌时间的比例</strong></p>
<p>调度算法就像不同的管理风格：</p>
<p>FCFS（先来先服务）：<strong>排队文化</strong>，先到先得，<strong>简单公平但效率可能不高</strong></p>
<p>SJF（短作业优先）：<strong>效率优先</strong>，让<strong>耗时小的作业先执行</strong>，但可能饿死大作业</p>
<p>RR（时间片轮转）：民主分配，每进程<strong>轮流用一会儿</strong>，保证响应速度</p>
<p>优先级调度：<strong>特权制度，重要任务优先</strong>，但<strong>要防止低优先级饿死</strong></p>
<p>现代操作系统像经验丰富的项目经理，会<strong>根据任务特点动态调整策略</strong>。</p>
<p>进程调度算法特性比较表</p>
<table>
<thead>
<tr>
<th>调度算法</th>
<th>抢占性</th>
<th>公平性</th>
<th>响应时间</th>
<th>吞吐量</th>
<th>实现复杂度</th>
<th>主要特点</th>
</tr>
</thead>
<tbody><tr>
<td><strong>FCFS</strong> (先来先服务)</td>
<td>非抢占</td>
<td>高</td>
<td>差</td>
<td>中</td>
<td>低</td>
<td>• 简单公平 • 可能产生 convoy 效应 • 对长作业有利</td>
</tr>
<tr>
<td><strong>SJF</strong> (短作业优先)</td>
<td>非抢占</td>
<td>低</td>
<td>好</td>
<td>高</td>
<td>中</td>
<td>• 最小化平均等待时间 • 可能饿死长作业 • 需要预知执行时间</td>
</tr>
<tr>
<td><strong>SRTF</strong> (最短剩余时间优先)</td>
<td>抢占</td>
<td>低</td>
<td>很好</td>
<td>高</td>
<td>高</td>
<td>• SJF的抢占式版本 • 响应时间优秀 • 实现复杂度高</td>
</tr>
<tr>
<td><strong>RR</strong> (时间片轮转)</td>
<td>抢占</td>
<td>高</td>
<td>好</td>
<td>中</td>
<td>中</td>
<td>• 公平性好 • 时间片大小影响性能 • 适合交互式系统</td>
</tr>
<tr>
<td><strong>优先级调度</strong></td>
<td>可选</td>
<td>低</td>
<td>好</td>
<td>高</td>
<td>中</td>
<td>• 支持优先级区分 • 可能饿死低优先级 • 可配置抢占性</td>
</tr>
<tr>
<td><strong>MLFQ</strong> (多级反馈队列)</td>
<td>抢占</td>
<td>中</td>
<td>很好</td>
<td>高</td>
<td>高</td>
<td>• 自适应调度 • 平衡响应和吞吐 • 防止饿死机制</td>
</tr>
<tr>
<td><strong>CFS</strong> (完全公平调度)</td>
<td>抢占</td>
<td>很高</td>
<td>很好</td>
<td>高</td>
<td>很高</td>
<td>• 红黑树管理进程 • 基于vruntime • 高度公平性</td>
</tr>
</tbody></table>
<h3 id="知识拓展-2"><a href="#知识拓展-2" class="headerlink" title="知识拓展"></a>知识拓展</h3><ul>
<li>各种调度算法的典型应用场景</li>
</ul>
<p>FCFS适用场景： 批处理系统， 简单的嵌入式系统， 教学演示环境， 负载极轻的服务器</p>
<p>SJF&#x2F;SRTF适用场景： 已知作业长度的批处理， 科学计算任务调度， 编译服务器， 需要最小化平均等待时间的场景</p>
<p>RR适用场景： 分时操作系统， 交互式系统（桌面、服务器）， 需要公平性的多用户环境， 实时性要求不高的通用系统</p>
<p>优先级调度适用场景： 实时系统， 关键任务系统， 有明确优先级区分的应用， 操作系统内核任务调度</p>
<p>MLFQ适用场景： 现代通用操作系统（Windows、macOS的某些版本）， 混合型工作负载（交互式+批处理）， 需要平衡响应时间和吞吐量的系统</p>
<p>CFS适用场景： Linux桌面和服务器系统， 需要高度公平性的环境， 虚拟化环境， 多核处理器系统</p>
<ul>
<li>知识图解</li>
</ul>
<p><img src="https://file1.kamacoder.com/i/bagu/2025120201.png" alt="image"></p>
<ul>
<li>面试官很能追问</li>
</ul>
<p>Q1: 什么是 convoy效应？如何避免？</p>
<p>A1: Convoy效应是指一个<strong>长进程阻塞了后面多个短进程的执行</strong>。</p>
<p>表现：短进程需要等待长进程完成，导致平均等待时间增加</p>
<p>原因：FCFS调度算法对长进程友好，对短进程不利</p>
<p>避免方法：<strong>使用抢占式调度</strong>（如RR、SRTF）或<strong>设置最大执行时间限制</strong></p>
<p>Q2: 多级反馈队列如何防止进程饿死？</p>
<p>A2: MLFQ通过以下机制防止饿死：</p>
<p><strong>优先级提升</strong>：低优先级进程等待一定时间后提升到高优先级队列</p>
<p><strong>时间片调整</strong>：低优先级队列使用更大的时间片，保证获得足够的CPU时间</p>
<p><strong>周期重置</strong>：定期将所有进程移回最高优先级队列，重置调度历史</p>
<p>Q3: 实时调度中EDF和RMS的区别是什么？</p>
<p>A3: <strong>主要区别</strong>：</p>
<p>EDF（最早截止时间优先）：动态优先级，选择绝对截止时间最早的任务</p>
<p>RMS（速率单调调度）：静态优先级，周期越短的任务优先级越高</p>
<p>可调度性：RMS有固定的CPU利用率上限（69%），EDF可达100%</p>
<p>适用性：RMS适合固定周期任务，EDF适合动态周期任务</p>
<h1 id="中断与异常"><a href="#中断与异常" class="headerlink" title="中断与异常"></a>中断与异常</h1><h2 id="什么是中断、异常？"><a href="#什么是中断、异常？" class="headerlink" title="什么是中断、异常？"></a>什么是中断、异常？</h2><h3 id="简要回答-3"><a href="#简要回答-3" class="headerlink" title="简要回答"></a>简要回答</h3><p><strong>中断</strong>和<strong>异常</strong>都是<strong>CPU响应特殊事件</strong>的机制，会<strong>打断当前执行流程</strong>：</p>
<p>中断（Interrupt）：来自<strong>CPU外部的异步事件</strong>，如I&#x2F;O设备请求、时钟信号</p>
<p>异常（Exception）：来自<strong>CPU内部的同步事件</strong>，如指令执行错误、系统调用</p>
<p>两者都会触发CPU从<strong>用户态切换到内核态</strong>，执行相应的处理程序。</p>
<h3 id="专业回答"><a href="#专业回答" class="headerlink" title="专业回答"></a>专业回答</h3><p><strong>中断</strong>和<strong>异常</strong>是处理器响应<strong>突发事件</strong>的核心机制，统称为<strong>中断向量</strong>（Interrupt Vector）</p>
<ul>
<li>中断（Interrupt）</li>
</ul>
<p>定义：由<strong>外部硬件设备产生的异步事件信号</strong>，用于通知<strong>CPU有需要处理的事件</strong>。</p>
<p>特性： 异步性：与<strong>CPU当前执行指令无关</strong>，<strong>随时可能发生</strong>；可屏蔽：可以<strong>通过中断屏蔽位暂时禁止某些中断</strong>；可嵌套：<strong>高级中断可以打断低级中断处理</strong></p>
<p>分类： <strong>硬件中断</strong>（Hardware Interrupt） 可屏蔽中断（INTR）：可<strong>通过CLI指令屏蔽 非屏蔽中断</strong>（NMI）：<strong>不可屏蔽</strong>，如内存奇偶校验错</p>
<p><strong>软件中断</strong>（Software Interrupt） <strong>INT指令触发</strong>，如系统调用（Linux的int 0x80）</p>
<ul>
<li>异常（Exception）</li>
</ul>
<p>定义：由CPU内部执行指令时检测到的同步事件。</p>
<p>特性： 同步性：<strong>特定指令</strong>执行必然产生；不可屏蔽：<strong>无法通过软件屏蔽</strong>；精确性：可以精确定位到出错指令</p>
<p>分类： <strong>故障</strong>（Fault）：<strong>可恢复错误</strong>，如缺页异常、除零异常 <strong>陷阱</strong>（Trap）：<strong>有意产生</strong>，如断点调试、系统调用 <strong>终止</strong>（Abort）：<strong>严重错误</strong>，无法恢复，如硬件故障</p>
<h3 id="知识拓展-3"><a href="#知识拓展-3" class="headerlink" title="知识拓展"></a>知识拓展</h3><ul>
<li>知识图解</li>
</ul>
<p><img src="https://file1.kamacoder.com/i/bagu/2025122001.png" alt="image"></p>
<ul>
<li>适用场景</li>
</ul>
<p><strong>中断适用场景</strong> 实时响应：键盘输入、网络包到达， 周期性处理：系统时钟、任务调度， 异步通知：DMA传输完成， 设备管理：磁盘I&#x2F;O完成</p>
<p><strong>异常适用场景</strong> 错误处理：除零、溢出、非法内存访问， 调试支持：断点、单步执行， 资源管理：页错误（虚拟内存）， 系统调用：从用户态陷入内核态</p>
<ul>
<li>面试官很能追问</li>
</ul>
<p>Q1：中断处理为什么分上下半部？</p>
<p>A1：上半部（硬中断处理）快速响应，只做必要操作（保存数据），下半部（软中断&#x2F;任务队列）处理耗时操作，减少中断屏蔽时间，提高系统响应性。</p>
<p>Q2：中断和异常处理时，CPU保存哪些上下文？</p>
<p>A2：至少包括程序计数器（PC）、处理器状态字（PSW）、关键寄存器。x86会保存EFLAGS、CS、EIP；ARM保存CPSR和返回地址。</p>
<p>Q3：页错误是中断还是异常？为什么？</p>
<p>A3：是异常（故障类）。因为它是同步的——访问无效页时立即触发，与当前指令直接相关，且必须处理才能继续执行。</p>
<h2 id="中断和异常的区别"><a href="#中断和异常的区别" class="headerlink" title="中断和异常的区别"></a>中断和异常的区别</h2><h3 id="详细回答-3"><a href="#详细回答-3" class="headerlink" title="详细回答"></a>详细回答</h3><h4 id="异常和中断的基本概念"><a href="#异常和中断的基本概念" class="headerlink" title="异常和中断的基本概念"></a>异常和中断的基本概念</h4><ul>
<li>当一条指令执行结束时，需要进行内部异常和外部中断请求的判断。如果存在异常或中断请求，需要进入异常或中断响应过程，主要任务是<strong>保存断点和程序状态</strong>，<strong>识别异常事件或中断源</strong>，并<strong>进入相应的服务程序</strong>进行处理。</li>
<li>异常和中断机制是<strong>操作系统获得CPU控制权的唯一方式</strong>，当中断或者异常事件发生时，CPU切换至<strong>内核态</strong>，操作系统得以获得所有特权指令的权限，通过执行操作系统提供的 异常或中断处理程序 来处理异常或中断事件。</li>
<li><strong>异常</strong>：是指CPU执行一条指令时，由CPU在其<strong>内部</strong>检测到的、与正在执行的指令相关的<strong>同步事件</strong>。</li>
<li><strong>中断</strong>：是指一种典型的由<strong>外部</strong>设备触发的、与当前正在执行的指令无关的<strong>异步事件</strong>。</li>
</ul>
<h4 id="异常和中断的分类"><a href="#异常和中断的分类" class="headerlink" title="异常和中断的分类"></a>异常和中断的分类</h4><ol>
<li><strong>内部异常（Exception）</strong> ：<ul>
<li>也被称为<strong>内部中断</strong> 或者 <strong>软件中断</strong>，异常是CPU执行当前指令产生的事件，是同步发生的，与CPU正在执行的指令密切相关。异常事件可进一步划分为<strong>故障（Fault）</strong>，<strong>自陷（Trap）</strong>，<strong>终止（Abort）</strong>。</li>
<li><strong>故障（Fault）</strong> ：通常是由<strong>指令执行</strong>引起的异常，如未定义指令、越权指令、段故障、缺页故障、除数为零、浮点溢出、整数溢出等。对于<strong>可恢复的故障</strong>（如数据缺页），可以由操作系统进行页面调度修复故障，再回到发生缺页故障的指令继续执行。此时的断点是当前指令而不是当前指令的下一条指令。对于<strong>不可恢复的故障</strong>（如未定义指令、越权指令），由操作系统终止当前进程的执行。</li>
<li><strong>自陷（Trap）</strong> ：是一种事先安排的“异常”事件，通过在程序中显式地调用<strong>自陷指令</strong>来触发异常，用于在用户态调用操作系统内核程序，以请求内核的服务，如系统调用，条件陷阱指令。自陷异常是由自陷指令执行触发的，类似于函数调用，但不存在程序断点（自陷指令<strong>不会在用户栈上</strong>保存返回地址），执行这些指令就会有条件地或无条件地调用操作系统内核程序并执行，执行完毕后返回自陷指令的下一条指令。</li>
<li><strong>终止（Abort）</strong> ：是指使CPU无法继续执行的<strong>硬件故障</strong>，和具体的指令无关。如机器校验错、总线错误、异常错误、异常处理中再次异常的双错等。此时当前程序无法继续执行，只能由操作系统终止，并由异常服务处理程序来重启系统。</li>
</ul>
</li>
<li><strong>外部中断（Interrupt）</strong> ：<ul>
<li><strong>外部中断来自CPU外部，与具体的指令无关，是随机事件</strong>，中断是指外部设备向CPU发出的中断请求（如鼠标点击、键盘按键等），中断提供了外设与CPU交流的机制，它也是一种重要的<strong>I&#x2F;O方式</strong>。CPU会在当前指令执行完毕后响应中断请求，进而转移到对应的中断处理程序，处理完毕后返回到发生中断的那条指令的下一条指令（因为被中断指令已经执行完毕）。</li>
<li>按中断请求是否可被屏蔽分类，可分为<strong>可屏蔽中断</strong> 和 <strong>非屏蔽中断</strong>。<br>① 可屏蔽中断在<strong>关中断</strong>情况下不会被响应，属于硬件中断。<br>② 非屏蔽中断即使在<strong>关中断</strong>的情况下也会被响应，属于硬件中断。</li>
<li>按中断事件能否直接提供中断服务地址分类，可分为<strong>向量中断</strong> 和 <strong>非向量中断</strong>。<br>① 向量中断是指中断事件可以提供<strong>中断服务程序入口地址</strong>的中断。<br>② 非向量中断是指中断事件不能直接提供<strong>中断服务程序入口地址</strong>的中断。</li>
<li>按中断处理过程能否被打断分类，可分为<strong>单重中断</strong> 和 <strong>多重中断</strong>。<br>① 单重中断是指CPU执行中断服务程序过程中<strong>不能被其他中断请求打断</strong>的中断。<br>② 多重中断是指CPU执行中断服务程序过程中<strong>可以去响应更高优先级的中断请求</strong>的中断，又称为 “<strong>中断嵌套</strong>”。</li>
</ul>
</li>
</ol>
<h4 id="异常和中断的处理"><a href="#异常和中断的处理" class="headerlink" title="异常和中断的处理"></a>异常和中断的处理</h4><ul>
<li>异常与中断的处理方式基本一致，当发生中断事件时，CPU接收到中断请求，在当前指令结束时CPU进入<strong>中断周期</strong>进行中断响应，并在<strong>中断响应</strong>中引入中断服务程序，由中断服务程序执行后续的<strong>中断处理</strong>。当然也有例外，例如产生故障异常的指令并没有执行完毕，但必须立即进行中断响应。</li>
<li><strong>中断响应</strong>：当CPU检测到未被屏蔽的中断请求信号时，进入中断响应阶段，这一阶段<strong>由硬件自动完成</strong>。中断响应阶段主要有以下三个步骤：<ol>
<li><strong>关中断</strong>：CPU首先要关中断，禁止在进行中断处理时又去响应新的中断，防止保存的断点、程序状态字、现场信息被破坏。</li>
<li><strong>保存断点和程序状态字</strong>：断点就是程序的返回地址，程序状态字是一个进程产生的各种状态信息。断点和程序状态字信息分别保存在特殊的寄存器PC 和 PSW中，CPU会将这两个寄存器内容压栈，以便后续恢复被中断进程的执行流和状态。</li>
<li><strong>引出中断服务程序</strong>：CPU检测到中断信号后对具体中断源进行识别，以此引出对应的中断服务程序。</li>
</ol>
</li>
<li><strong>中断处理</strong>：当中断响应引出中断服务程序后，CPU控制权交由操作系统，通过执行操作系统提供的中断服务程序完成中断处理过程。中断处理阶段主要有以下三个步骤：<ol>
<li><strong>保护现场</strong>：保存一些通用寄存器信息，防止接下来执行中断处理程序后寄存器内容被破坏。</li>
<li><strong>执行中断处理程序</strong>：也就是具体的中断服务。</li>
<li><strong>恢复现场</strong>：恢复保存的通用寄存器信息，开中断，恢复PC 和 PSW的信息，使得被中断进程完全恢复原来的状态。</li>
</ol>
</li>
</ul>
<h3 id="知识拓展-4"><a href="#知识拓展-4" class="headerlink" title="知识拓展"></a>知识拓展</h3><p><strong>程序中断方式流程图</strong> 和 <strong>中断处理流程图</strong>，如下图所示：<br><img src="https://file1.kamacoder.com/i/bagu/program_interrupt_mechanism.jpg" alt="image"></p>
<h2 id="计算机中中断的过程是什么？"><a href="#计算机中中断的过程是什么？" class="headerlink" title="计算机中中断的过程是什么？"></a>计算机中中断的过程是什么？</h2><h3 id="简要回答-4"><a href="#简要回答-4" class="headerlink" title="简要回答"></a>简要回答</h3><p>中断是计算机系统中由硬件或软件触发的一种异步事件处理机制，它<strong>打断CPU当前</strong>正在执行的程序，转而<strong>执行预先定义好的中断处理程序</strong>，处理完毕后再恢复原程序的执行。</p>
<p>中断过程包括<strong>中断请求、中断响应、中断处理、中断返回</strong>四个阶段，涉及<strong>中断控制器、中断向量表、程序状态字、现场保护与恢复</strong>等关键组件和操作。</p>
<p><strong>中断机制是</strong>实现<strong>CPU与外部设备</strong>并行工作、提高系统效率、处理紧急事件<strong>的基础</strong>。</p>
<h3 id="专业回答-1"><a href="#专业回答-1" class="headerlink" title="专业回答"></a>专业回答</h3><p>计算机中断过程是一个精心设计的<strong>硬件与软件协同机制</strong>，其本质<strong>是CPU对外部或内部</strong>事件的异步响应。</p>
<p>整个过程始于<strong>中断源</strong>的产生，这些源可以是<strong>外部硬件</strong>设备如键盘敲击、网络包到达、定时器超时，也可以是<strong>内部软件</strong>条件如除<strong>零错误、页故障、系统调用</strong>请求。</p>
<p>当中断事件发生时，<strong>中断控制器</strong>接收并仲裁<strong>多个</strong>同时发生的中断，根据<strong>优先级</strong>决定<strong>哪个中断被首先服务</strong>，然后<strong>向CPU发出中断请求信号</strong>。</p>
<p>CPU在每条指令执行结束时检查是<strong>否有待处理的中断请求</strong>，如果<strong>中断允许且当前优先级允许响应，则进入中断响应周期</strong>：</p>
<p>首先<strong>完成当前指令的执行</strong>，然后保存关键的<strong>处理器状态</strong>，包括<strong>程序计数器</strong>（指向下一条要执行的指令）和<strong>程序状态字</strong>（包含条件码、中断允许位等标志），接着根据<strong>中断类型</strong>从<strong>中断向量表</strong>中获取对应的<strong>中断处理程序入口地址</strong>，同时<strong>自动关闭中断允许</strong>（防止嵌套中断干扰）。</p>
<p>随后<strong>CPU跳转到中断服务程序</strong>开始执行，此时进入中断处理阶段，<strong>处理程序首先保存被中断程序的完整现场</strong>，然后根据中断原因<strong>执行相应的处理逻辑</strong>，处理过程中可能<strong>需要与设备控制器交互以清除中断源</strong>。</p>
<p>处理完成后，中断服务程序恢复之前保存的现场，<strong>执行中断返回</strong>指令，该指令<strong>将恢复之前保存的程序状态字和程序计数器</strong>，CPU由此<strong>返回到被中断的程序继续执行</strong>，整个过程对原程序透明，仿佛从未被打断过。</p>
<p>中断机制的精妙之处在于它<strong>通过硬件自动化的状态保存与恢复</strong>，以及<strong>分层的中断优先级管理</strong>，实现了<strong>CPU计算与I&#x2F;O操作的高度重叠</strong>，极大<strong>提高了系统吞吐率和响应实时性</strong>。</p>
<ul>
<li>中断分类</li>
</ul>
<p><strong>硬中断</strong>：来自硬件设备（IRQ）</p>
<p><strong>软中断</strong>：由INT指令触发</p>
<p><strong>异常</strong>：执行指令时错误（除零、缺页）</p>
<p><strong>不可屏蔽中断（NMI）</strong>：必须立即处理</p>
<h1 id="同步与互斥"><a href="#同步与互斥" class="headerlink" title="同步与互斥"></a>同步与互斥</h1><h2 id="线程同步方式"><a href="#线程同步方式" class="headerlink" title="线程同步方式"></a>线程同步方式</h2><ol>
<li><strong>内核级线程（KLT）的同步方式</strong>：<ul>
<li><strong>互斥锁（Mutex）</strong>：确保同一时间只有一个线程进入临界区。<br>① <strong>实现</strong>：通过系统调用（如<code>pthread_mutex_lock</code>）获取锁，竞争失败的线程被阻塞并移出调度队列。<br>② <strong>优化</strong>：可配置为自适应自旋（如Linux的<code>futex</code>），减少上下文切换。<br>③ <strong>适用场景</strong>：长临界区（如文件操作），避免忙等待浪费CPU。</li>
<li><strong>信号量（Semaphore）</strong>：控制资源访问数量，支持计数和二进制模式。<br>① <strong>计数信号量</strong>：管理资源池（如数据库连接池）。<br>② <strong>二进制信号量</strong>：等价于互斥锁，但支持跨进程（<code>System V</code>信号量）。</li>
<li><strong>条件变量（Condition Variable）</strong>：用于线程间状态通知，基于特定条件阻塞或唤醒线程，需搭配互斥锁使用。<br>① <strong>实现</strong>：需与互斥锁配合，<code>wait()</code>释放锁并阻塞，<code>signal()</code>唤醒等待线程。<br>② <strong>典型场景</strong>：生产者-消费者模型中的缓冲区空&#x2F;满通知。</li>
<li><strong>读写锁（Read-Write Lock）</strong>：允许多读单写，提高读多写少场景的性能。<br>① <strong>实现</strong>：读锁共享（允许多线程同时读），写锁独占（仅允许单线程写）。<br>② <strong>适用场景</strong>：配置文件读取（多读）与更新（单写）。</li>
<li><strong>自旋锁（Spinlock）</strong>：忙等待锁，适用于短临界区。<br>① <strong>实现</strong>：线程循环检测锁状态（如<code>while(lock == BUSY);</code>），不主动让出CPU。<br>② <strong>适用场景</strong>：多核短临界区（如Linux内核中断处理）。<br>③ <strong>风险</strong>：长时间自旋浪费CPU，需配合抢占式调度使用。</li>
</ul>
</li>
<li><strong>用户级线程（ULT）的同步方式</strong>：<ul>
<li><strong>原子操作（Atomic）</strong>：通过原子指令如CAS（Compare-And-Swap），避免锁开销。<br>① <strong>实现</strong>：通过CPU指令（如<code>CAS</code>、<code>LL/SC</code>）直接操作内存，无需锁。<br>② <strong>适用场景</strong>：无锁计数器（如<code>atomic.AddInt32</code>）、标志位更新。<br>③ <strong>局限性</strong>：仅适用于简单操作，复杂逻辑仍需锁。</li>
<li><strong>协程同步原语</strong>：通过调度器协作式切换，如Go的<code>channel</code>，通过消息传递替代共享内存。<br>① <strong>实现</strong>：如Go的<code>channel</code>，通过通信传递数据所有权，避免共享内存。<br>② <strong>适用场景</strong>：高并发I&#x2F;O密集型任务（如网络服务器）。<br>② <strong>优势</strong>：无锁竞争，依赖调度器协作切换（非抢占式）。</li>
<li><strong>轻量级锁</strong>：用户态实现的互斥锁（如Java的偏向锁）。<br>① <strong>实现</strong>：先尝试用户态自旋，失败后升级为内核锁（如Java的锁膨胀）。<br>② <strong>优势</strong>：减少内核切换次数，平衡性能与公平性。</li>
<li><strong>非阻塞数据结构</strong>：用户态实现的互斥锁（如Java的偏向锁）。<br>① <strong>无锁队列</strong>：<strong>基于原子操作</strong>设计无锁队列、栈等数据结构（如Java的<code>ConcurrentLinkedQueue</code>）。</li>
<li><strong>协作式调度同步</strong>：<br>① <strong>示例</strong>：用户态调度器主动让出CPU（如协程<code>yield</code>），通过事件循环协调任务，避免竞态条件。<br>② <strong>缺点</strong>：依赖开发者手动控制切换时机，易引发死锁。</li>
</ul>
</li>
</ol>
<h3 id="知识拓展-5"><a href="#知识拓展-5" class="headerlink" title="知识拓展"></a>知识拓展</h3><ul>
<li><strong>自旋锁（Spinlock）的工作示意图</strong>，如下所示：<br><img src="https://file1.kamacoder.com/i/bagu/spinlock_workchart.jpg" alt="image"></li>
<li><strong>自旋与非自旋的流程图</strong>，如下图所示：<br><img src="https://file1.kamacoder.com/i/bagu/spinlock_flowchart.jpg" alt="image"></li>
<li><strong>面试官可能的追问1 — 用户级线程（如Go的Goroutine）如何避免内核级同步的开销？</strong>：<ul>
<li>Go通过<code>channel</code>通信替代锁，由调度器<strong>在用户态管理协程切换</strong>，仅在必要时（如系统调用）才进入内核态。</li>
</ul>
</li>
<li><strong>面试官可能的追问2 — 自旋锁在什么场景下会失效？如何优化？</strong>：<ul>
<li><strong>失效场景</strong>：单核环境（忙等待浪费CPU）、长临界区（自旋时间过长）。</li>
<li><strong>优化</strong>：设置最大自旋次数后转为阻塞锁，或结合自适应自旋（动态预测等待时间）。</li>
</ul>
</li>
<li><strong>面试官可能的追问3：自旋锁在内核线程和用户线程中的使用有何差异？</strong><ul>
<li><strong>内核线程</strong>：可禁用中断确保原子性，防止死锁；支持多核并发自旋。</li>
<li><strong>用户线程</strong>：依赖原子指令或<code>futex</code>；单核环境需主动让出CPU（如调用<code>sched_yield</code>）。</li>
</ul>
</li>
</ul>
<h2 id="典型锁"><a href="#典型锁" class="headerlink" title="典型锁"></a>典型锁</h2><ol>
<li><strong>互斥锁（Mutex Lock）</strong><ul>
<li><strong>功能</strong>：互斥锁用于保护临界区，确保<strong>同一时间只有一个线程可以访问</strong>共享资源。</li>
<li><strong>实现方式</strong>：当一个线程获取锁后，其他线程必须等待锁释放后才能获取。</li>
<li><strong>使用场景</strong>：适用于需要<strong>独占访问共享资源</strong>的场景，如修改全局变量、访问共享数据结构等。</li>
<li><strong>与条件变量的配合使用</strong>：条件变量通常与互斥锁配合使用，用于实现线程间的条件同步。例如，在生产者-消费者模型中，生产者线程在条件不满足时等待，消费者线程在条件满足时通知生产者线程。</li>
</ul>
</li>
<li><strong>读写锁（Read-Write Lock）</strong><ul>
<li><strong>功能</strong>：允许多个<strong>读线程同时访问</strong>共享资源，但<strong>写线程</strong>必须<strong>独占</strong>资源。</li>
<li><strong>实现方式</strong>：分为共享锁（读锁）和独占锁（写锁）。读锁可以被多个线程同时持有，写锁只能被一个线程持有。</li>
<li><strong>使用场景</strong>：适用于<strong>读多写少</strong>的场景，如缓存系统、数据库等。</li>
</ul>
</li>
<li><strong>自旋锁（Spin Lock）</strong><ul>
<li><strong>功能</strong>：线程在获取锁时，如果锁已被占用，会一直<strong>循环检查</strong>锁的状态（忙等待），直到锁可用。</li>
<li><strong>实现方式</strong>：通过忙等待（Busy-Waiting）实现，避免线程切换的开销。</li>
<li><strong>使用场景</strong>：适用于<strong>锁占用时间极短</strong>的场景，如内核中的临界区保护。</li>
</ul>
</li>
</ol>
<h3 id="知识拓展-6"><a href="#知识拓展-6" class="headerlink" title="知识拓展"></a>知识拓展</h3><h4 id="信号量、锁和条件变量的关系？"><a href="#信号量、锁和条件变量的关系？" class="headerlink" title="信号量、锁和条件变量的关系？"></a>信号量、锁和条件变量的关系？</h4><ol>
<li><strong>信号量</strong>：<ul>
<li>信号量是一种通用的同步机制，通过计数器来控制对资源的访问，信号量机制可以实现各种同步机制，包括锁。</li>
<li><strong>互斥锁</strong>是信号量的一个特例，通常是一个二进制信号量（只能取0或1）。</li>
<li>信号量可以用来实现各种同步问题，如生产者-消费者问题、读者-写者问题等。</li>
</ul>
</li>
<li><strong>锁</strong>：<ul>
<li><strong>锁</strong>是一个更高级别的概念，通常用于<strong>保护临界区</strong>，确保同一时间只有一个线程访问共享资源。</li>
<li><strong>互斥锁</strong>是锁的一种，通常与信号量相关联，但它本身是锁的一个具体实现。</li>
<li><strong>读写锁</strong>和<strong>自旋锁</strong>也是锁的类型，但它们并不属于信号量的特例。<ul>
<li><strong>读写锁</strong>允许多个读操作同时进行，但写操作是排他的。</li>
<li><strong>自旋锁</strong>是在获取锁失败时循环等待，而不是进入阻塞状态。</li>
</ul>
</li>
</ul>
</li>
<li><strong>条件变量</strong>：<ul>
<li>条件变量通常<strong>与互斥锁一起使用</strong>，用于线程之间的通信。</li>
<li>它允许线程在某个条件不满足时等待，直到其他线程发出<strong>通知</strong>。</li>
<li>条件变量本身不是信号量，但可以与信号量结合使用来实现更复杂的同步逻辑。</li>
</ul>
</li>
</ol>
<h4 id="为什么使用锁和条件变量而不是直接使用信号量？"><a href="#为什么使用锁和条件变量而不是直接使用信号量？" class="headerlink" title="为什么使用锁和条件变量而不是直接使用信号量？"></a>为什么使用锁和条件变量而不是直接使用信号量？</h4><ul>
<li><strong>抽象层次</strong>：<ul>
<li><strong>锁</strong>和<strong>条件变量</strong>提供了更高层次的抽象，使代码更易读、易写和易维护。</li>
<li><strong>信号量更底层</strong>，需要开发者手动管理计数器，容易出错。</li>
</ul>
</li>
<li><strong>功能专门化</strong>：<ul>
<li>锁专门用于保护临界区，条件变量专门用于线程间通信，它们组合使用可以更清晰地表达同步逻辑。</li>
<li>信号量虽然功能强大，但可能需要更多的代码来实现相同的功能，并且容易出现死锁或资源争用的问题。</li>
</ul>
</li>
</ul>
<h4 id="读写锁和自旋锁是否属于信号量的特例？"><a href="#读写锁和自旋锁是否属于信号量的特例？" class="headerlink" title="读写锁和自旋锁是否属于信号量的特例？"></a>读写锁和自旋锁是否属于信号量的特例？</h4><ul>
<li>虽然“互斥锁”属于信号量的特例，但<strong>读写锁</strong>和<strong>自旋锁</strong>不属于信号量的特例；它们是锁的不同类型，用于不同的场景：<ol>
<li><strong>读写锁</strong>允许并发读取，但 exclusive 写入。</li>
<li><strong>自旋锁</strong>适用于锁持有时间非常短的场景，避免线程切换的开销。</li>
</ol>
</li>
</ul>
]]></content>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG学习-文本分块</title>
    <url>/2026/04/13/RAG%E5%AD%A6%E4%B9%A0-%E6%96%87%E6%9C%AC%E5%88%86%E5%9D%97/</url>
    <content><![CDATA[<h2 id="技术原理"><a href="#技术原理" class="headerlink" title="技术原理"></a>技术原理</h2><p>主要分为检索、生成两个阶段</p>
<p>（1）<strong>检索阶段：寻找“非参数化知识”</strong></p>
<ul>
<li><strong>知识向量化</strong>：<strong>嵌入模型（Embedding Model）</strong> 充当了“连接器”的角色。它将外部知识库编码为向量索引（Index），存入<strong>向量数据库</strong>。</li>
<li><strong>语义召回</strong>：当用户发起查询时，检索模块利用同样的嵌入模型将问题向量化，并通过<strong>相似度搜索（Similarity Search）</strong>，从海量数据中精准锁定与问题最相关的文档片段。</li>
</ul>
<p>（2）<strong>生成阶段：融合两种知识</strong></p>
<ul>
<li><strong>上下文整合</strong>：<strong>生成模块</strong>接收检索阶段送来的相关文档片段以及用户的原始问题。</li>
<li><strong>指令引导生成</strong>：该模块会遵循预设的 <strong>Prompt</strong> 指令，将上下文与问题有效整合，并引导 LLM（如 DeepSeek）进行可控的、有理有据的文本生成。</li>
</ul>
<p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260413222349879.png?token=AR3LVDH3AOUKB6AGGTUOKU3J3T6LE" alt="image-20260413222347766"></p>
<h2 id="技术演进"><a href="#技术演进" class="headerlink" title="技术演进"></a>技术演进</h2><p>大致分为原生RAG、高级RAG、模块化RAG三个阶段。</p>
<p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260413222509770.png?token=AR3LVDCZYKD45IAKD5AT5QDJ3T6QE" alt="image-20260413222508575"></p>
<p>具体对比如下表：</p>
<table>
<thead>
<tr>
<th align="center"></th>
<th align="center">初级 RAG（Naive RAG）</th>
<th align="center">高级 RAG（Advanced RAG）</th>
<th align="center">模块化 RAG（Modular RAG）</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><strong>流程</strong></td>
<td align="center"><strong>离线:</strong> <code>索引</code> <strong>在线:</strong> <code>检索 → 生成</code></td>
<td align="center"><strong>离线:</strong> <code>索引</code> <strong>在线:</strong> <code>...→ 检索前 → ... → 检索后 → ...</code></td>
<td align="center">积木式可编排流程</td>
</tr>
<tr>
<td align="center"><strong>特点</strong></td>
<td align="center">基础线性流程</td>
<td align="center">增加<strong>检索前后</strong>的优化步骤</td>
<td align="center">模块化、可组合、可动态调整</td>
</tr>
<tr>
<td align="center"><strong>关键技术</strong></td>
<td align="center">基础向量检索</td>
<td align="center"><strong>查询重写（Query Rewrite）</strong> <strong>结果重排（Rerank）</strong></td>
<td align="center"><strong>动态路由（Routing）</strong> <strong>查询转换（Query Transformation）</strong> <strong>多路融合（Fusion）</strong></td>
</tr>
<tr>
<td align="center"><strong>局限性</strong></td>
<td align="center">效果不稳定，难以优化</td>
<td align="center">流程相对固定，优化点有限</td>
<td align="center">系统复杂性高</td>
</tr>
</tbody></table>
<h2 id="使用原因"><a href="#使用原因" class="headerlink" title="使用原因"></a>使用原因</h2><p>在选择具体的技术路径时，一个重要的考量是<strong>成本与效益的平衡</strong>。通常，我们应优先选择对模型改动最小、成本最低的方案，所以技术选型路径往往遵循的顺序是<strong>提示词工程（Prompt Engineering） -&gt; 检索增强生成 -&gt; 微调（Fine-tuning）</strong>。</p>
<p>我们可以从两个维度来理解这些技术的区别。如图 1-3 所示，<strong>横轴代表“LLM 优化”</strong>，即对模型本身进行多大程度的修改。从左到右，优化的程度越来越深，其中提示工程和 RAG 完全不改变模型权重，而微调则直接修改模型参数。<strong>纵轴代表“上下文优化”</strong>，是对输入给模型的信息进行多大程度的增强。从下到上，增强的程度越来越高，其中提示工程只是优化提问方式，而 RAG 则通过引入外部知识库，极大地丰富了上下文信息。</p>
<p>RAG 的出现填补了通用模型与专业领域之间的鸿沟，它在解决如表 1-2 所示 LLM 局限时尤其有效：</p>
<table>
<thead>
<tr>
<th align="center">问题</th>
<th align="center">RAG的解决方案</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><strong>静态知识局限</strong></td>
<td align="center">实时检索外部知识库，支持动态更新</td>
</tr>
<tr>
<td align="center"><strong>幻觉（Hallucination）</strong></td>
<td align="center">基于检索内容生成，错误率降低</td>
</tr>
<tr>
<td align="center"><strong>领域专业性不足</strong></td>
<td align="center">引入领域特定知识库（如医疗&#x2F;法律）</td>
</tr>
<tr>
<td align="center"><strong>数据隐私风险</strong></td>
<td align="center">本地化部署知识库，避免敏感数据泄露</td>
</tr>
</tbody></table>
<h1 id="文本分块"><a href="#文本分块" class="headerlink" title="文本分块"></a>文本分块</h1><h2 id="重要性"><a href="#重要性" class="headerlink" title="重要性"></a>重要性</h2><h3 id="满足模型上下文限制"><a href="#满足模型上下文限制" class="headerlink" title="满足模型上下文限制"></a>满足模型上下文限制</h3><p>将文本分块的首要原因，是为了适应 RAG 系统中两个核心组件的硬性限制：</p>
<ul>
<li><strong>嵌入模型 (Embedding Model)</strong>: 负责将文本块转换为向量。这类模型有严格的输入长度上限。例如，许多常用的嵌入模型（如 <code>bge-base-zh-v1.5</code>）的上下文窗口为512个token。任何超出此限制的文本块在输入时都会被截断，导致信息丢失，生成的向量也无法完整代表原文的语义。因此，<strong>文本块的大小必须小于等于嵌入模型的上下文窗口</strong>。</li>
<li><strong>大语言模型 (LLM)</strong>: 负责根据检索到的上下文生成答案。LLM同样有上下文窗口限制（尽管通常比嵌入模型大得多，从几千到上百万token不等）。检索到的所有文本块，连同用户问题和提示词，都必须能被放入这个窗口中。如果单个块过大，可能会导致只能容纳少数几个相关的块，限制了LLM回答问题时可参考的信息广度。</li>
</ul>
<p>因此，分块是确保文本能够被两个模型完整、有效处理的基础。</p>
<h3 id="为何“块”不是越大越好"><a href="#为何“块”不是越大越好" class="headerlink" title="为何“块”不是越大越好"></a>为何“块”不是越大越好</h3><p>假设嵌入模型最多能处理 8192 个 token，是否应该把块切得尽可能大（比如8000个token）呢？答案是否定的。<strong>块的大小并非越大越好</strong>，过大的块会严重影响RAG系统的性能。</p>
<h4 id="嵌入过程中的信息损失"><a href="#嵌入过程中的信息损失" class="headerlink" title="嵌入过程中的信息损失"></a>嵌入过程中的信息损失</h4><p>大多数嵌入模型都基于 Transformer 编码器。其工作流程大致如下：</p>
<ul>
<li><strong>分词 (Tokenization)</strong>: 将输入的文本块分解成一个个 token。</li>
<li><strong>向量化 (Vectorization)</strong>: Transformer 为<strong>每个 token</strong> 生成一个高维向量表示。</li>
<li><strong>池化 (Pooling)</strong>: 通过某种方法（如取 <code>[CLS]</code> 位的向量、对所有token向量求平均 <code>mean pooling</code> 等），将所有 token 的向量<strong>压缩</strong>成一个<strong>单一的向量</strong>，这个向量代表了整个文本块的语义。</li>
</ul>
<blockquote>
<p><code>[CLS]</code> 是BERT等Transformer模型在输入文本开头添加的特殊标记，它通过自注意力机制动态聚合整个序列的上下文信息，其最终向量被训练用作代表全局语义的嵌入。</p>
</blockquote>
<p>在这个<code>压缩</code>过程中，信息损失是不可避免的。一个768维的向量需要概括整个文本块的所有信息。<strong>文本块越长，包含的语义点越多，这个单一向量所承载的信息就越稀释</strong>，导致其表示变得笼统，关键细节被模糊化，从而降低了检索的精度。</p>
<h4 id="生成过程的“大海捞针”-Lost-in-the-Middle"><a href="#生成过程的“大海捞针”-Lost-in-the-Middle" class="headerlink" title="生成过程的“大海捞针” (Lost in the Middle)"></a>生成过程的“大海捞针” (Lost in the Middle)</h4><p>即使将检索到的多个大块文本都塞进LLM的长上下文窗口中，也会出现关键信息被“淹没”在大量无关内容里的问题。有研究表明 <a href="https://datawhalechina.github.io/all-in-rag/#fn-1">1</a>，当LLM处理非常长的、充满大量信息的上下文时，它倾向于更好地记住开头和结尾的信息，而忽略中间部分的内容。</p>
<p>如果提供给LLM的上下文块又大又杂，充满了与问题无关的噪音，模型就很难从中提取出最关键的信息来形成答案，从而导致回答质量下降或产生幻觉。</p>
<h4 id="主题稀释导致检索失败"><a href="#主题稀释导致检索失败" class="headerlink" title="主题稀释导致检索失败"></a>主题稀释导致检索失败</h4><p>一个好的文本块应该聚焦于一个明确、单一的主题。如果一个块包含太多不相关的主题，它的语义就会被稀释，导致在检索时无法被精确匹配。</p>
<p>假设有一个关于《王者荣耀》英雄鲁班七号的攻略文档。</p>
<ul>
<li><strong>糟糕的分块策略</strong>：将“技能介绍”、“推荐出装”和“背景故事”这三个完全不同主题的内容，全部放在一个巨大的文本块里。<ul>
<li>当玩家查询“鲁班七号怎么出装？”时，这个大块虽然包含了出装信息，但由于被技能说明和英雄故事等无关主题严重稀释，其整体的检索相关性得分可能会很低，导致无法被召回。</li>
</ul>
</li>
<li><strong>优秀的分块策略</strong>：将“技能”、“出装”和“故事”分别切分为三个独立的、主题聚焦的块。<ul>
<li>当玩家再次查询时，“推荐出装”这个块会因为与查询高度相关而获得极高的分数，从而被精准地检索出来。</li>
</ul>
</li>
</ul>
<p>通过合理分块，可以有效提升检索的信噪比，确保了后续生成环节能得到最优质、最相关的上下文。</p>
<h2 id="基础分块策略"><a href="#基础分块策略" class="headerlink" title="基础分块策略"></a>基础分块策略</h2><p>LangChain 提供了丰富且易于使用的文本分割器（Text Splitters），下面将介绍几种最核心的策略。</p>
<p>固定大小分块、递归字符分块、基于语义的分块、基于文档结构的分块</p>
<h3 id="固定大小分块"><a href="#固定大小分块" class="headerlink" title="固定大小分块"></a>固定大小分块</h3><p>这是最简单直接的分块方法。根据LangChain源码，这种方法的工作原理分为两个主要阶段：</p>
<p>（1）<strong>按段落分割</strong>：<code>CharacterTextSplitter</code> 采用默认分隔符 <code>&quot;\n\n&quot;</code>，使用正则表达式将文本按段落进行分割，通过 <code>_split_text_with_regex</code> 函数处理。</p>
<p>（2）<strong>智能合并</strong>：调用继承自父类的 <code>_merge_splits</code> 方法，将分割后的段落依次合并。该方法会监控累积长度，当超过 <code>chunk_size</code> 时形成新块，并通过重叠机制（<code>chunk_overlap</code>）保持上下文连续性，同时在必要时发出超长块的警告。</p>
<p>需要注意，<code>CharacterTextSplitter</code> 实际实现的并非严格的固定大小分块。根据 <code>_merge_splits</code> 源码逻辑，这种方法会：</p>
<ul>
<li><strong>优先保持段落完整性</strong>：只有当添加新段落会导致总长度超过 <code>chunk_size</code> 时，才会结束当前块</li>
<li><strong>处理超长段落</strong>：如果单个段落超过 <code>chunk_size</code>，系统会发出警告但仍将其作为完整块保留</li>
<li><strong>应用重叠机制</strong>：通过 <code>chunk_overlap</code> 参数在块之间保持内容重叠，确保上下文连续性</li>
</ul>
<p>所以，LangChain 的实现更准确地应该称为”段落感知的自适应分块”，块大小会根据段落边界动态调整。</p>
<h3 id="递归字符分块"><a href="#递归字符分块" class="headerlink" title="递归字符分块"></a>递归字符分块</h3><p>现在让我们深入了解 <code>RecursiveCharacterTextSplitter</code> 的实现。这种分块器通过分隔符层级递归处理，相对与固定大小分块，改善了超长文本的处理效果。</p>
<p><strong>算法流程</strong>： </p>
<p>（1）<strong>寻找有效分隔符</strong>: 从分隔符列表中从前到后遍历，找到第一个在当前文本中<strong>存在</strong>的分隔符。如果都不存在，使用最后一个分隔符（通常是空字符串 <code>&quot;&quot;</code>）。</p>
<p>（2）<strong>切分与分类处理</strong>: 使用选定的分隔符切分文本，然后遍历所有片段：</p>
<ul>
<li><strong>如果片段不超过块大小</strong>: 暂存到 <code>_good_splits</code> 中，准备合并</li>
<li><strong>如果片段超过块大小</strong>:<ul>
<li>首先，将暂存的合格片段通过 <code>_merge_splits</code> 合并成块</li>
<li>然后，检查是否还有剩余分隔符：<ul>
<li><strong>有剩余分隔符</strong>: 递归调用 <code>_split_text</code> 继续分割</li>
<li><strong>无剩余分隔符</strong>: 直接保留为超长块</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>（3）<strong>最终处理</strong>: 将剩余的暂存片段合并成最后的块</p>
<p><strong>实现细节</strong>：</p>
<ul>
<li><strong>批处理机制</strong>: 先收集所有合格片段（<code>_good_splits</code>），遇到超长片段时才触发合并操作。</li>
<li><strong>递归终止条件</strong>: 关键在于 <code>if not new_separators</code> 判断。当分隔符用尽时（<code>new_separators</code> 为空），停止递归，直接保留超长片段。确保算法不会无限递归。</li>
</ul>
<p><strong>与固定大小分块的关键差异</strong>：</p>
<ul>
<li>固定大小分块遇到超长段落时只能发出警告并保留。</li>
<li>递归分块会继续使用更细粒度的分隔符（句子→单词→字符）直到满足大小要求。</li>
</ul>
<p><strong>分隔符配置</strong>：</p>
<ul>
<li><strong>默认分隔符</strong>：<code>[&quot;\n\n&quot;, &quot;\n&quot;, &quot; &quot;, &quot;&quot;]</code></li>
<li><strong>多语言支持</strong>：对于无词边界语言（中文、日文、泰文），可添加：</li>
</ul>
<p><strong>编程语言特化支持</strong>：</p>
<p><code>RecursiveCharacterTextSplitter</code> 能够针对特定的编程语言（如Python, Java等）使用预设的、更符合代码结构的分隔符。它们通常包含语言的顶级语法结构（如类、函数定义）和次级结构（如控制流语句），以实现更符合代码逻辑的分割。</p>
<h3 id="语义分块"><a href="#语义分块" class="headerlink" title="语义分块"></a>语义分块</h3><p>语义分块（Semantic Chunking）是一种更智能的方法，这种方法不依赖于固定的字符数或预设的分隔符，而是尝试根据文本的语义内涵来切分。其核心是：<strong>在语义主题发生显著变化的地方进行切分</strong>。这使得每个分块都具有高度的内部语义一致性。LangChain 提供了 <code>langchain_experimental.text_splitter.SemanticChunker</code> 来实现这一功能。</p>
<p><strong>实现原理</strong></p>
<p>工作流程可以概括为以下几个步骤：</p>
<p>（1）**句子分割 (Sentence Splitting)**：首先，使用标准的句子分割规则（例如，基于句号、问号、感叹号）将输入文本拆分成一个句子列表。</p>
<p>（2）**上下文感知嵌入 (Context-Aware Embedding)**：这是 <code>SemanticChunker</code> 的一个关键设计。该分块器不是对每个句子独立进行嵌入，而是通过 <code>buffer_size</code> 参数（默认为1）来捕捉上下文信息。对于列表中的每一个句子，这种方法会将其与前后各 <code>buffer_size</code> 个句子组合起来，然后对这个临时的、更长的组合文本进行嵌入。这样，每个句子最终得到的嵌入向量就融入了其上下文的语义。</p>
<p>（3）<strong>计算语义距离 (Distance Calculation)：计算每对相邻</strong>句子的嵌入向量之间的余弦距离。这个距离值量化了两个句子之间的语义差异——距离越大，表示语义关联越弱，跳跃越明显。</p>
<p>（4）**识别断点 (Breakpoint Identification)**：<code>SemanticChunker</code> 会分析所有计算出的距离值，并根据一个统计方法（默认为 <code>percentile</code>）来确定一个动态阈值。例如，它可能会将所有距离中第95百分位的值作为切分阈值。所有距离大于此阈值的点，都被识别为语义上的“断点”。</p>
<p>（5）**合并成块 (Merging into Chunks)**：最后，根据识别出的所有断点位置，将原始的句子序列进行切分，并将每个切分后的部分内的所有句子合并起来，形成一个最终的、语义连贯的文本块。</p>
<p><strong>断点识别方法 (<code>breakpoint_threshold_type</code>)</strong></p>
<p>如何定义“显著的语义跳跃”是语义分块的关键。<code>SemanticChunker</code> 提供了几种基于统计的方法来识别断点：</p>
<ul>
<li><code>percentile</code> (百分位法 - <strong>默认方法</strong>):<ul>
<li><strong>逻辑</strong>: 计算所有相邻句子的语义差异值，并将这些差异值进行排序。当一个差异值超过某个百分位阈值时，就认为该差异值是一个断点。</li>
<li><strong>参数</strong>: <code>breakpoint_threshold_amount</code> (默认为 <code>95</code>)，表示使用第95个百分位作为阈值。这意味着，只有最显著的5%的语义差异点会被选为切分点。</li>
</ul>
</li>
<li><code>standard_deviation</code> (标准差法):<ul>
<li><strong>逻辑</strong>: 计算所有差异值的平均值和标准差。当一个差异值超过“平均值 + N * 标准差”时，被视为异常高的跳跃，即断点。</li>
<li><strong>参数</strong>: <code>breakpoint_threshold_amount</code> (默认为 <code>3</code>)，表示使用3倍标准差作为阈值。</li>
</ul>
</li>
<li><code>interquartile</code> (四分位距法):<ul>
<li><strong>逻辑</strong>: 使用统计学中的四分位距（IQR）来识别异常值。当一个差异值超过 <code>Q3 + N * IQR</code> 时，被视为断点。</li>
<li><strong>参数</strong>: <code>breakpoint_threshold_amount</code> (默认为 <code>1.5</code>)，表示使用1.5倍的IQR。</li>
</ul>
</li>
<li><code>gradient</code> (梯度法):<ul>
<li><strong>逻辑</strong>: 这是一种更复杂的方法。它首先计算差异值的变化率（梯度），然后对梯度应用百分位法。对于那些句子间语义联系紧密、差异值普遍较低的文本（如法律、医疗文档）特别有效，因为这种方法能更好地捕捉到语义变化的“拐点”。</li>
<li><strong>参数</strong>: <code>breakpoint_threshold_amount</code> (默认为 <code>95</code>)。</li>
</ul>
</li>
</ul>
<h3 id="基于文档结构的分块"><a href="#基于文档结构的分块" class="headerlink" title="基于文档结构的分块"></a>基于文档结构的分块</h3><p>对于具有明确结构标记的文档格式（如Markdown、HTML、LaTex），可以利用这些标记来实现更智能、更符合逻辑的分割。</p>
<h4 id="以Markdown-结构分块为例"><a href="#以Markdown-结构分块为例" class="headerlink" title="以Markdown 结构分块为例"></a>以Markdown 结构分块为例</h4><p>针对结构清晰的 Markdown 文档，利用其标题层级进行分块是一种高效且保留了丰富语义的方法。LangChain 提供了 <code>MarkdownHeaderTextSplitter</code> 来处理。</p>
<ul>
<li><strong>实现原理</strong>: 该分块器的主要逻辑是“先按标题分组，再按需细分”。<ol>
<li><strong>定义分割规则</strong>: 用户首先需要提供一个标题层级的映射关系，例如 <code>[ (&quot;#&quot;, &quot;Header 1&quot;), (&quot;##&quot;, &quot;Header 2&quot;) ]</code>，告诉分块器 <code>#</code> 是一级标题，<code>##</code> 是二级标题。</li>
<li><strong>内容聚合</strong>: 分块器会遍历整个文档，将每个标题下的所有内容（直到下一个同级或更高级别的标题出现前）聚合在一起。每个聚合后的内容块都会被赋予一个包含其完整标题路径的元数据。</li>
</ol>
</li>
<li><strong>元数据注入的优势</strong>: 这是此方法的主要特点。例如，对于一篇关于机器学习的文章，某个段落可能位于“第三章：模型评估”下的“3.2节：评估指标”中。经过分割后，这个段落形成的文本块，其元数据就会是 <code>&#123;&quot;Header 1&quot;: &quot;第三章：模型评估&quot;, &quot;Header 2&quot;: &quot;3.2节：评估指标&quot;&#125;</code>。这种元数据为每个块提供了精确的“地址”，极大地增强了上下文的准确性，让大模型能更好地理解信息片段的来源和背景。</li>
<li><strong>局限性与组合使用</strong>: 单纯按标题分割可能会导致一个问题：某个章节下的内容可能非常长，远超模型能处理的上下文窗口。为了解决这个问题，<code>MarkdownHeaderTextSplitter</code> 可以与其它分块器（如 <code>RecursiveCharacterTextSplitter</code>）<strong>组合使用</strong>。具体流程是：<ul>
<li>第一步，使用 <code>MarkdownHeaderTextSplitter</code> 将文档按标题分割成若干个大的、带有元数据的逻辑块。</li>
<li>第二步，对这些逻辑块再应用 <code>RecursiveCharacterTextSplitter</code>，将其进一步切分为符合 <code>chunk_size</code> 要求的小块。由于这个过程是在第一步之后进行的，所有最终生成的小块都会<strong>继承</strong>来自第一步的标题元数据。</li>
</ul>
</li>
<li><strong>RAG应用优势</strong>: 这种两阶段的分块方法，既保留了文档的宏观逻辑结构（通过元数据），又确保了每个块的大小适中，是处理结构化文档进行RAG的理想方案。</li>
</ul>
]]></content>
      <categories>
        <category>RAG</category>
        <category>文本分块</category>
      </categories>
      <tags>
        <tag>RAG</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker学习</title>
    <url>/2026/04/15/Docker%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>RAG-索引构建</title>
    <url>/2026/04/14/RAG%E5%AD%A6%E4%B9%A0-%E7%B4%A2%E5%BC%95%E6%9E%84%E5%BB%BA/</url>
    <content><![CDATA[<h1 id="向量嵌入"><a href="#向量嵌入" class="headerlink" title="向量嵌入"></a>向量嵌入</h1><h2 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h2><h3 id="什么是-Embedding"><a href="#什么是-Embedding" class="headerlink" title="什么是 Embedding"></a>什么是 Embedding</h3><p>向量嵌入（Embedding）是一种将真实世界中复杂、高维的数据对象（如文本、图像、音频、视频等）转换为数学上易于处理的、低维、稠密的连续数值向量的技术。</p>
<p>想象一下，我们将每一个词、每一段话、每一张图片都放在一个巨大的多维空间里，并给它一个独一无二的坐标。这个坐标就是一个向量，它“嵌入”了原始数据的所有关键信息。这个过程，就是 Embedding。</p>
<ul>
<li><strong>数据对象</strong>：任何信息，如文本“你好世界”，或一张猫的图片。</li>
<li><strong>Embedding 模型</strong>：一个深度学习模型，负责接收数据对象并进行转换。</li>
<li><strong>输出向量</strong>：一个固定长度的一维数组，例如 <code>[0.16, 0.29, -0.88, ...]</code>。这个向量的维度（长度）通常在几百到几千之间。</li>
</ul>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_1_1.webp" alt="Embedding 过程示意图"></p>
<h3 id="向量空间的语义表示"><a href="#向量空间的语义表示" class="headerlink" title="向量空间的语义表示"></a>向量空间的语义表示</h3><p>Embedding 的真正意义在于，它产生的向量不是随机数值的堆砌，而是对数据<strong>语义</strong>的数学编码。</p>
<ul>
<li><strong>核心原则</strong>：在 Embedding 构建的向量空间中，语义上相似的对象，其对应的向量在空间中的距离会更近；而语义上不相关的对象，它们的向量距离会更远。</li>
<li><strong>关键度量</strong>：我们通常使用以下数学方法来衡量向量间的“距离”或“相似度”：<ul>
<li><strong>余弦相似度 (Cosine Similarity)</strong> ：计算两个向量夹角的余弦值。值越接近 1，代表方向越一致，语义越相似。这是最常用的度量方式。</li>
<li><strong>点积 (Dot Product)</strong> ：计算两个向量的乘积和。在向量归一化后，点积等价于余弦相似度。</li>
<li><strong>欧氏距离 (Euclidean Distance)</strong> ：计算两个向量在空间中的直线距离。距离越小，语义越相似。</li>
</ul>
</li>
</ul>
<h2 id="Embedding-在-RAG-中的作用"><a href="#Embedding-在-RAG-中的作用" class="headerlink" title="Embedding 在 RAG 中的作用"></a>Embedding 在 RAG 中的作用</h2><p>在RAG流程中，Embedding 扮演着无可替代的重要角色。</p>
<h3 id="语义检索的基础"><a href="#语义检索的基础" class="headerlink" title="语义检索的基础"></a>语义检索的基础</h3><p>RAG 的“检索”环节通常以基于 Embedding 的语义搜索为核心。通用流程如下： </p>
<p>（1）<strong>离线索引构建</strong>：将知识库内文档切分后，使用 Embedding 模型将每个文档块（Chunk）转换为向量，存入专门的向量数据库中。</p>
<p>（2）<strong>在线查询检索</strong>：当用户提出问题时，使用<strong>同一个</strong> Embedding 模型将用户的问题也转换为一个向量。</p>
<p>（3）<strong>相似度计算</strong>：在向量数据库中，计算“问题向量”与所有“文档块向量”的相似度。</p>
<p>（4）<strong>召回上下文</strong>：选取相似度最高的 Top-K 个文档块，作为补充的上下文信息，与原始问题一同送给大语言模型（LLM）生成最终答案。</p>
<h3 id="决定检索质量的关键"><a href="#决定检索质量的关键" class="headerlink" title="决定检索质量的关键"></a>决定检索质量的关键</h3><p>Embedding 的质量直接决定了 RAG 检索召回内容的准确性与相关性。一个优秀的 Embedding 模型能够精准捕捉问题和文档之间的深层语义联系，即使用户的提问和原文的表述不完全一致。反之，一个劣质的 Embedding 模型可能会因为无法理解语义而召回不相关或错误的信息，从而“污染”提供给 LLM 的上下文，导致最终生成的答案质量低下。</p>
<h2 id="Embedding-技术发展"><a href="#Embedding-技术发展" class="headerlink" title="Embedding 技术发展"></a>Embedding 技术发展</h2><p>Embedding 技术的发展与自然语言处理（NLP）的进步紧密相连，尤其是在 RAG 框架出现后，对嵌入技术提出了新的要求。其演进路径大致可分为以下几个关键阶段。</p>
<h3 id="静态词嵌入：上下文无关的表示"><a href="#静态词嵌入：上下文无关的表示" class="headerlink" title="静态词嵌入：上下文无关的表示"></a>静态词嵌入：上下文无关的表示</h3><ul>
<li><strong>代表模型</strong>：Word2Vec (2013), GloVe (2014)</li>
<li><strong>主要原理</strong>：为词汇表中的每个单词生成一个固定的、与上下文无关的向量。例如，<code>Word2Vec</code> 通过 Skip-gram 和 CBOW 架构，利用局部上下文窗口学习词向量，并验证了向量运算的语义能力（如 <code>国王 - 男人 + 女人 ≈ 王后</code>）。<code>GloVe</code> 则融合了全局词-词共现矩阵的统计信息。</li>
<li><strong>局限性</strong>：无法处理一词多义问题。在“苹果公司发布了新手机”和“我吃了一个苹果”中，“苹果”的词向量是完全相同的，这限制了其在复杂语境下的语义表达能力。</li>
</ul>
<h3 id="动态上下文嵌入"><a href="#动态上下文嵌入" class="headerlink" title="动态上下文嵌入"></a>动态上下文嵌入</h3><p>2017年，<code>Transformer</code> 架构的诞生带来了自注意力机制（Self-Attention），它允许模型在生成一个词的向量时，动态地考虑句子中所有其他词的影响。基于此，2018年 <code>BERT</code> 模型利用 <code>Transformer</code> 的编码器，通过掩码语言模型（MLM）等自监督任务进行预训练，生成了深度上下文相关的嵌入。同一个词在不同语境中会生成不同的向量，这有效解决了静态嵌入的一词多义难题。</p>
<h3 id="RAG-对嵌入技术的新要求"><a href="#RAG-对嵌入技术的新要求" class="headerlink" title="RAG 对嵌入技术的新要求"></a>RAG 对嵌入技术的新要求</h3><p>在开篇我们就提到了 RAG 框架的提出，是为了解决大型语言模型 <strong>知识固化</strong>（内部知识难以更新）和 <strong>幻觉</strong>（生成的内容可能不符合事实且无法溯源）的问题。它通过“检索-生成”范式，动态地为 LLM 注入外部知识。这一过程的核心是 <strong>语义检索</strong>，很大程度上依赖于高质量的向量嵌入。</p>
<p>后续 RAG 的兴起对嵌入技术提出了更高、更具体的要求：</p>
<ul>
<li><strong>领域自适应能力</strong>：通用的嵌入模型在专业领域（如法律、医疗）往往表现不佳，这就要求嵌入模型具备领域自适应的能力，能够通过微调或使用指令（如 INSTRUCTOR 模型）来适应特定领域的术语和语义。</li>
<li><strong>多粒度与多模态支持</strong>：RAG 系统需要处理的不仅仅是短句，还可能包括长文档、代码，甚至是图像和表格。这就要求嵌入模型能够处理不同长度和类型的输入数据。</li>
<li><strong>检索效率与混合检索</strong>：嵌入向量的维度和模型大小直接影响存储成本和检索速度。同时，为了结合语义相似性（密集检索）和关键词匹配（稀疏检索）的优点，支持混合检索的嵌入模型（如 BGE-M3）应运而生，在某些任务中成为提升召回率的关键。</li>
</ul>
<h2 id="嵌入模型训练原理"><a href="#嵌入模型训练原理" class="headerlink" title="嵌入模型训练原理"></a>嵌入模型训练原理</h2><p>了解了嵌入模型的发展，我们来简单探究一下当前主流的嵌入模型（通常是基于 <code>BERT</code> 的变体）是如何通过训练获得强大的语义理解能力的。</p>
<p>现代嵌入模型的核心通常是 Transformer 的编码器（Encoder）部分，<code>BERT</code> 就是其中的典型代表。它通过堆叠多个 <code>Transformer Encoder</code> 层来构建一个深度的双向表示学习网络。</p>
<h3 id="主要训练任务"><a href="#主要训练任务" class="headerlink" title="主要训练任务"></a>主要训练任务</h3><p>BERT 的成功很大程度上归功于 <strong>自监督学习</strong> 策略，它允许模型从海量的、无标注的文本数据中学习知识。</p>
<h4 id="任务一：掩码语言模型-Masked-Language-Model-MLM"><a href="#任务一：掩码语言模型-Masked-Language-Model-MLM" class="headerlink" title="任务一：掩码语言模型 (Masked Language Model, MLM)"></a>任务一：掩码语言模型 (Masked Language Model, MLM)</h4><ul>
<li><strong>过程</strong>：<ul>
<li>随机地将输入句子中 15% 的词元（Token）替换为一个特殊的 <code>[MASK]</code> 标记。</li>
<li>让模型去预测这些被遮盖住的原始词元是什么。</li>
</ul>
</li>
<li><strong>目标</strong>：通过这个任务，模型被迫学习每个词元与其上下文之间的关系，从而掌握深层次的语境语义。</li>
</ul>
<h4 id="任务二：下一句预测-Next-Sentence-Prediction-NSP"><a href="#任务二：下一句预测-Next-Sentence-Prediction-NSP" class="headerlink" title="任务二：下一句预测 (Next Sentence Prediction, NSP)"></a>任务二：下一句预测 (Next Sentence Prediction, NSP)</h4><ul>
<li><strong>过程</strong>：<ul>
<li>构造训练样本，每个样本包含两个句子 A 和 B。</li>
<li>其中 50% 的样本，B 是 A 的真实下一句（IsNext）；另外 50% 的样本，B 是从语料库中随机抽取的句子（NotNext）。</li>
<li>让模型判断 B 是否是 A 的下一句。</li>
</ul>
</li>
<li><strong>目标</strong>：这个任务让模型学习句子与句子之间的逻辑关系、连贯性和主题相关性。</li>
<li><strong>重要说明</strong>：后续的研究（如 RoBERTa）发现<a href="https://datawhalechina.github.io/all-in-rag/#fn-2">2</a>，NSP 任务可能过于简单，甚至会损害模型性能。因此，许多现代的预训练模型（如 RoBERTa、SBERT）在预训练阶段移除了 NSP。</li>
</ul>
<blockquote>
<p>更多细节可查看 <a href="https://github.com/datawhalechina/base-nlp/blob/main/docs/chapter5/13_Bert.md">BERT 架构及应用</a></p>
</blockquote>
<h3 id="效果增强策略"><a href="#效果增强策略" class="headerlink" title="效果增强策略"></a>效果增强策略</h3><p>虽然 MLM 和 NSP 赋予了模型强大的基础语义理解能力，但为了在检索任务中表现更佳，现代嵌入模型通常会引入更具针对性的训练策略。</p>
<ul>
<li><strong>度量学习 (Metric Learning)</strong> ：<ul>
<li><strong>思想</strong>：直接以“相似度”作为优化目标。</li>
<li><strong>方法</strong>：收集大量相关的文本对（例如，（问题，答案）、（新闻标题，正文））。训练的目标是优化向量空间中的<strong>相对距离</strong>：让“正例对”的向量表示在空间中被“拉近”，而“负例对”的向量表示被“推远”。关键在于优化排序关系，而非追求绝对的相似度值（如 1 或 0），因为过度追求极端值可能导致模型过拟合。</li>
</ul>
</li>
<li><strong>对比学习 (Contrastive Learning)</strong> ：<ul>
<li><strong>思想</strong>：在向量空间中，将相似的样本“拉近”，将不相似的样本“推远”。</li>
<li><strong>方法</strong>：构建一个三元组（Anchor, Positive, Negative）。其中，Anchor 和 Positive 是相关的（例如，同一个问题的两种不同问法），Anchor 和 Negative 是不相关的。训练的目标是让 <code>distance(Anchor, Positive)</code> 尽可能小，同时让 <code>distance(Anchor, Negative)</code> 尽可能大。</li>
</ul>
</li>
</ul>
<h2 id="模型选型指南"><a href="#模型选型指南" class="headerlink" title="模型选型指南"></a>模型选型指南</h2><p>理论已经了解，那么该如何选择最适合你项目的嵌入模型？</p>
<h3 id="从-MTEB-排行榜开始"><a href="#从-MTEB-排行榜开始" class="headerlink" title="从 MTEB 排行榜开始"></a>从 MTEB 排行榜开始</h3><p><a href="https://huggingface.co/spaces/mteb/leaderboard"><strong>MTEB (Massive Text Embedding Benchmark)</strong></a> 是一个由 Hugging Face 维护的、全面的文本嵌入模型评测基准。它涵盖了分类、聚类、检索、排序等多种任务，并提供了公开的排行榜，为评估和选择嵌入模型提供了重要的参考依据。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_1_2.webp" alt="MTEB 排行榜"></p>
<p>下面这张图是网站中的模型评估图像，直观地展示了在选择开源嵌入模型时需要权衡的四个核心维度：</p>
<ul>
<li><strong>横轴 - 模型参数量 (Number of Parameters)</strong> ：代表了模型的大小。通常，参数量越大的模型（越靠右），其潜在能力越强，但对计算资源的要求也越高。</li>
<li><strong>纵轴 - 平均任务得分 (Mean Task Score)</strong> ：代表了模型的综合性能。这个分数是模型在分类、聚类、检索等一系列标准 NLP 任务上的平均表现。分数越高（越靠上），说明模型的通用语义理解能力越强。</li>
<li><strong>气泡大小 - 嵌入维度 (Embedding Size)</strong> ：代表了模型输出向量的维度。气泡越大，维度越高，理论上能编码更丰富的语义细节，但同时也会占用更多的存储和计算资源。</li>
<li><strong>气泡颜色 - 最大处理长度 (Max Tokens)</strong> ：代表了模型能处理的文本长度上限。颜色越深，表示模型能处理的 Token 数量越多，对长文本的适应性越好。</li>
</ul>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_1_3.webp" alt="MTEB 排行榜多维度评估示意图"></p>
<p>MTEB 榜单可以帮助我们快速筛选掉大量不合适的模型。但需要注意，榜单上的得分是在通用数据集上评测的，可能无法完全反映模型在你特定业务场景下的表现。</p>
<h3 id="关键评估维度"><a href="#关键评估维度" class="headerlink" title="关键评估维度"></a>关键评估维度</h3><p>在查看榜单时，除了分数，还需要关注以下几个关键维度：</p>
<ul>
<li><strong>任务 (Task)</strong> ：对于 RAG 应用，需要重点关注模型在 <code>Retrieval</code> (检索) 任务下的排名。</li>
<li><strong>语言 (Language)</strong> ：模型是否支持你的业务数据所使用的语言？对于中文 RAG，应选择明确支持中文或多语言的模型。</li>
<li><strong>模型大小 (Size)</strong> ：模型越大，通常性能越好，但对硬件（显存）的要求也越高，推理速度也越慢。需要根据你的部署环境和性能要求来权衡。</li>
<li><strong>维度 (Dimensions)</strong> ：向量维度越高，能编码的信息越丰富，但也会占用更多的存储空间和计算资源。</li>
<li><strong>最大 Token 数 (Max Tokens)</strong> ：这决定了模型能处理的文本长度上限。这个参数是你设计文本分块（Chunking）策略时必须考虑的重要依据，块大小不应超过此限制。</li>
<li><strong>得分与机构 (Score &amp; Publisher)</strong> ：结合模型的得分排名和其发布机构的声誉进行初步筛选。知名机构发布的模型通常质量更有保障。</li>
<li><strong>成本 (Cost)</strong> ：如果是使用 API 服务的模型，需要考虑其调用成本；如果是自部署开源模型，则需要评估其对硬件资源的消耗（如显存、内存）以及带来的运维成本。</li>
</ul>
<h3 id="迭代测试与优化"><a href="#迭代测试与优化" class="headerlink" title="迭代测试与优化"></a>迭代测试与优化</h3><blockquote>
<p>不要只依赖公开榜单做最终决定。</p>
</blockquote>
<p>（1）<strong>确定基线 (Baseline)</strong> ：根据上述维度，选择几个符合要求的模型作为你的初始基准模型。</p>
<p>（2）<strong>构建私有评测集</strong> ：根据真实业务数据，手动创建一批高质量的评测样本，每个样本包含一个典型用户问题和它对应的标准答案（或最相关的文档块）。</p>
<p>（3）<strong>迭代优化</strong> ： 使用基线模型在你的私有评测集上运行，评估其召回的准确率和相关性。 如果效果不理想，可以尝试更换模型，或者调整 RAG 流程的其他环节（如文本分块策略）。 通过几轮的对比测试和迭代优化，最终选出在你的特定场景下表现最佳的那个“心仪”模型。</p>
<h1 id="多模态嵌入"><a href="#多模态嵌入" class="headerlink" title="多模态嵌入"></a>多模态嵌入</h1><p>现代 AI 的一项重要突破，是将简单的词向量发展成了能统一理解图文、音视频的复杂系统。这一发展建立在<strong>注意力机制、Transformer 架构和对比学习</strong>等关键技术之上，它们解决了在共享向量空间中对齐不同数据模态的核心挑战。其发展环环相扣：Word2Vec 为 BERT 的上下文理解铺路，而 BERT 又为 CLIP 等模型的跨模态能力奠定了基础。</p>
<h2 id="为什么需要多模态嵌入？"><a href="#为什么需要多模态嵌入？" class="headerlink" title="为什么需要多模态嵌入？"></a>为什么需要多模态嵌入？</h2><p>前面的章节介绍了如何为文本创建向量嵌入。然而，仅有文本的世界是不完整的。现实世界的信息是多模态的，包含图像、音频、视频等。传统的文本嵌入无法理解“那张有红色汽车的图片”这样的查询，因为文本向量和图像向量处于相互隔离的空间，存在一堵“模态墙”。</p>
<p><strong>多模态嵌入 (Multimodal Embedding)</strong> 的目标正是为了打破这堵墙。其目的是将不同类型的数据（如图像和文本）映射到<strong>同一个共享的向量空间</strong>。在这个统一的空间里，一段描述“一只奔跑的狗”的文字，其向量会非常接近一张真实小狗奔跑的图片向量。</p>
<p>实现这一目标的关键，在于解决 <strong>跨模态对齐 (Cross-modal Alignment)</strong> 的挑战。以对比学习、视觉 Transformer (ViT) 等技术为代表的突破，让模型能够学习到不同模态数据之间的语义关联，最终催生了像 CLIP 这样的模型。</p>
<h2 id="CLIP-模型浅析"><a href="#CLIP-模型浅析" class="headerlink" title="CLIP 模型浅析"></a>CLIP 模型浅析</h2><p>在图文多模态领域，OpenAI 的 <strong>CLIP (Contrastive Language-Image Pre-training)</strong> 是一个很有影响力的模型，它为多模态嵌入定义了一个有效的范式。</p>
<p>CLIP 的架构清晰简洁。它采用**双编码器架构 (Dual-Encoder Architecture)**，包含一个图像编码器和一个文本编码器，分别将图像和文本映射到同一个共享的向量空间中。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_2_1.webp" alt="CLIP Architecture"> <em>图：CLIP 的工作流程。(1) 通过对比学习训练双编码器，对齐图文向量空间。(2)和(3) 展示了如何利用该空间，通过图文相似度匹配实现零样本预测。</em></p>
<p>为了让这两个编码器学会“对齐”不同模态的语义，CLIP 在训练时采用了<strong>对比学习 (Contrastive Learning)</strong> 策略。在处理一批图文数据时，模型的目标是：最大化正确图文对的向量相似度，同时最小化所有错误配对的相似度。通过这种“拉近正例，推远负例”的方式，模型从海量数据中学会了将语义相关的图像和文本在向量空间中拉近。</p>
<p>这种大规模的对比学习赋予了 CLIP 有效的<strong>零样本（Zero-shot）识别能力</strong>。它能将一个传统的分类任务，转化为一个“图文检索”问题——例如，要判断一张图片是不是猫，只需计算图片向量与“a photo of a cat”文本向量的相似度即可。这使得 CLIP 无需针对特定任务进行微调，就能实现对视觉概念的泛化理解。</p>
<h2 id="常用多模态嵌入模型-以bge-visualized-m3为例"><a href="#常用多模态嵌入模型-以bge-visualized-m3为例" class="headerlink" title="常用多模态嵌入模型(以bge-visualized-m3为例)"></a>常用多模态嵌入模型(以bge-visualized-m3为例)</h2><p>虽然 CLIP 为图文预训练提供了重要基础，但多模态领域的研究迅速发展，涌现了许多针对不同目标和场景进行优化的模型。例如，BLIP 系列专注于提升细粒度的图文理解与生成能力，而 ALIGN 则证明了利用海量噪声数据进行大规模训练的有效性。</p>
<p>在众多优秀的模型中，由北京智源人工智能研究院（BAAI）开发的 <strong>bge-visualized-m3（Visualized-BGE 的 M3 版本）</strong> 是一个很有代表性的现代多模态嵌入模型。它是在 <strong>BGE-M3</strong>（文本嵌入底座）的基础上引入图像能力而来，体现了当前技术向“更统一、更全面”发展的趋势。</p>
<p>bge-visualized-m3 的核心特性也可以概括为“M3”（主要继承自其文本底座 BGE-M3）：</p>
<ul>
<li>**多语言性 (Multi-Linguality)**：支持超过 100 种语言的文本表示，可用于跨语言的图文检索（文本侧）。</li>
<li>**多功能性 (Multi-Functionality)**：在文本检索场景下，可按需求使用密集检索（Dense Retrieval）、多向量检索（Multi-Vector Retrieval）等不同范式。</li>
<li>**多粒度性 (Multi-Granularity)**：文本侧可处理从短句到长达 8192 个 token 的长文档，覆盖更广泛的应用需求。</li>
</ul>
<p>在技术架构上，bge-visualized-m3 会先用视觉编码器提取图像的 <strong>patch token</strong>，再将其映射到与文本同维度的“图像 token”，与文本 token 一起送入 BGE 的 Transformer 编码器进行联合建模，最终得到可用于图文检索的统一向量表示。</p>
<h1 id="向量数据库"><a href="#向量数据库" class="headerlink" title="向量数据库"></a>向量数据库</h1><h2 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h2><p>在前面我们学习了如何使用嵌入模型将文本、图像等非结构化数据转换为高维向量。这些向量是 RAG 系统能够进行语义理解的基础。然而，当向量数量从几百个增长到数百万甚至数十亿时，一个核心问题随之而来：<strong>如何快速、准确地从海量向量中找到与用户查询最相似的那几个？</strong></p>
<h3 id="向量数据库主要功能"><a href="#向量数据库主要功能" class="headerlink" title="向量数据库主要功能"></a>向量数据库主要功能</h3><p>向量数据库的核心价值在于其高效处理海量高维向量的能力。其主要功能可以概括为以下几点：</p>
<ul>
<li><strong>高效的相似性搜索</strong>：这是向量数据库最重要的功能。它利用专门的索引技术（如 HNSW, IVF），能够在数十亿级别的向量中实现毫秒级的近似最近邻（ANN）查询，快速找到与给定查询最相似的数据。</li>
<li><strong>高维数据存储与管理</strong>：专门为存储高维向量（通常维度成百上千）而优化，支持对向量数据进行增、删、改、查等基本操作。</li>
<li><strong>丰富的查询能力</strong>：除了基本的相似性搜索，还支持按标量字段过滤查询（例如，在搜索相似图片的同时，指定<code>年份 &gt; 2023</code>）、范围查询和聚类分析等，满足复杂业务需求。</li>
<li><strong>可扩展与高可用</strong>：现代向量数据库通常采用分布式架构，具备良好的水平扩展能力和容错性，能够通过增加节点来应对数据量的增长，并确保服务的稳定可靠。</li>
<li><strong>数据与模型生态集成</strong>：与主流的 AI 框架（如 LangChain, LlamaIndex）和机器学习工作流无缝集成，简化了从模型训练到向量检索的应用开发流程。</li>
</ul>
<h3 id="向量数据库-vs-传统数据库"><a href="#向量数据库-vs-传统数据库" class="headerlink" title="向量数据库 vs 传统数据库"></a>向量数据库 vs 传统数据库</h3><p>传统的数据库（如 MySQL）擅长处理结构化数据的精确匹配查询（例如，<code>WHERE age = 25</code>），但它们并非为处理高维向量的相似性搜索而设计的。在庞大的向量集合中进行暴力、线性的相似度计算，其计算成本和时间延迟无法接受。<strong>向量数据库 (Vector Database)</strong> 很好的解决了这一问题，它是一种专门设计用于高效存储、管理和查询高维向量的数据库系统。在 RAG 流程中，它扮演着“知识库”的角色，是连接数据与大语言模型的关键桥梁。</p>
<p>向量数据库与传统数据库的主要差异如下：</p>
<table>
<thead>
<tr>
<th><strong>维度</strong></th>
<th align="center"><strong>向量数据库</strong></th>
<th align="center"><strong>传统数据库 (RDBMS)</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>核心数据类型</strong></td>
<td align="center">高维向量 (Embeddings)</td>
<td align="center">结构化数据 (文本、数字、日期)</td>
</tr>
<tr>
<td><strong>查询方式</strong></td>
<td align="center"><strong>相似性搜索</strong> (ANN)</td>
<td align="center"><strong>精确匹配</strong></td>
</tr>
<tr>
<td><strong>索引机制</strong></td>
<td align="center">HNSW, IVF, LSH 等 ANN 索引</td>
<td align="center">B-Tree, Hash Index</td>
</tr>
<tr>
<td><strong>主要应用场景</strong></td>
<td align="center">AI 应用、RAG、推荐系统、图像&#x2F;语音识别</td>
<td align="center">业务系统 (ERP, CRM)、金融交易、数据报表</td>
</tr>
<tr>
<td><strong>数据规模</strong></td>
<td align="center">轻松应对千亿级向量</td>
<td align="center">通常在千万到亿级行数据，更大规模需复杂分库分表</td>
</tr>
<tr>
<td><strong>性能特点</strong></td>
<td align="center">高维数据检索性能极高，计算密集型</td>
<td align="center">结构化数据查询快，高维数据查询性能呈指数级下降</td>
</tr>
<tr>
<td><strong>一致性</strong></td>
<td align="center">通常为最终一致性</td>
<td align="center">强一致性 (ACID 事务)</td>
</tr>
</tbody></table>
<p>向量数据库和传统数据库并非相互替代的关系，而是<strong>互补关系</strong>。在构建现代 AI 应用时，通常会将两者结合使用：利用传统数据库存储业务元数据和结构化信息，而向量数据库则专门负责处理和检索由 AI 模型产生的海量向量数据。</p>
<h2 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h2><p>向量数据库的核心是高效处理高维向量的相似性搜索。向量数据库通常采用四层架构，通过<strong>存储层、索引层、查询层和服务层</strong>的协同工作来实现高效相似性搜索，其中存储层负责存储向量数据和元数据，优化存储效率并支持分布式存储；索引层维护索引算法（HNSW、LSH、PQ等），负责索引的创建与优化，并支持索引调整；查询层处理查询请求，支持混合查询并实现查询优化；服务层管理客户端连接，提供监控和日志能力，并实现安全管理。</p>
<p>主要技术手段包括：</p>
<ul>
<li><strong>基于树的方法</strong>：如 Annoy 使用的随机投影树，通过树形结构实现对数复杂度的搜索</li>
<li><strong>基于哈希的方法</strong>：如 LSH（局部敏感哈希），通过哈希函数将相似向量映射到同一“桶”</li>
<li><strong>基于图的方法</strong>：如 HNSW（分层可导航小世界图），通过多层邻近图结构实现快速搜索</li>
<li><strong>基于量化的方法</strong>：如 Faiss 的 IVF 和 PQ，通过聚类和量化压缩向量</li>
</ul>
<h2 id="主流向量数据库"><a href="#主流向量数据库" class="headerlink" title="主流向量数据库"></a>主流向量数据库</h2><p>当前主流的向量数据库产品包括：</p>
<p><a href="https://www.pinecone.io/"><strong>Pinecone</strong> </a>是一款完全托管的向量数据库服务，采用Serverless架构设计。它提供存储计算分离、自动扩展和负载均衡等企业级特性，并保证99.95%的SLA。Pinecone支持多种语言SDK，提供极高可用性和低延迟搜索（&lt;100ms），特别适合企业级生产环境、高并发场景和大规模部署。</p>
<p><a href="https://github.com/milvus-io/milvus"><strong>Milvus</strong> </a>是一款开源的分布式向量数据库，采用分布式架构设计，支持GPU加速和多种索引算法。它能够处理亿级向量检索，提供高性能GPU加速和完善的生态系统。Milvus特别适合大规模部署、高性能要求的场景，以及需要自定义开发的开源项目。</p>
<p><a href="https://github.com/qdrant/qdrant"><strong>Qdrant</strong> </a>是一款高性能的开源向量数据库，采用Rust开发，支持二进制量化技术。它提供多种索引策略和向量混合搜索功能，能够实现极高的性能（RPS&gt;4000）和低延迟搜索。Qdrant特别适合性能敏感应用、高并发场景以及中小规模部署。</p>
<p><a href="https://github.com/weaviate/weaviate"><strong>Weaviate</strong> </a>是一款支持GraphQL的AI集成向量数据库，提供20+AI模块和多模态支持。它采用GraphQL API设计，支持RAG优化，特别适合AI开发、多模态处理和快速开发场景。Weaviate具有活跃的社区支持和易于集成的特点。</p>
<p><a href="https://github.com/chroma-core/chroma"><strong>Chroma</strong> </a>是一款轻量级的开源向量数据库，采用本地优先设计，无依赖。它提供零配置安装、本地运行和低资源消耗等特性，特别适合原型开发、教育培训和小规模应用。Chroma的部署简单，适合快速原型开发。</p>
<p><strong>选择建议</strong>：</p>
<ul>
<li><strong>新手入门&#x2F;小型项目</strong>：从 <code>ChromaDB</code> 或 <code>FAISS</code> 开始是最佳选择。它们与 LangChain&#x2F;LlamaIndex 紧密集成，几行代码就能运行，且能满足基本的存储和检索需求。</li>
<li><strong>生产环境&#x2F;大规模应用</strong>：当数据量超过百万级，或需要高并发、实时更新、复杂元数据过滤时，应考虑更专业的解决方案，如 <code>Milvus</code>、<code>Weaviate</code> 或云服务 <code>Pinecone</code>。</li>
</ul>
<p><strong>索引创建实现细节</strong>： 通过深入 LangChain 源码，可以发现索引创建是一个分层、解耦的过程，主要涉及以下几个方法的嵌套调用：</p>
<ol>
<li><strong><code>from_documents</code> (封装层)</strong>:<ul>
<li>这是我们直接调用的方法。它的职责很简单：从输入的 <code>Document</code> 对象列表中提取出纯文本内容 (<code>page_content</code>) 和元数据 (<code>metadata</code>)。</li>
<li>然后，它将这些提取出的信息传递给核心的 <code>from_texts</code> 方法。</li>
</ul>
</li>
<li><strong><code>from_texts</code> (向量化入口)</strong>:<ul>
<li>这个方法是面向用户的入口。它接收文本列表，并执行关键的第一步：调用 <code>embedding.embed_documents(texts)</code>，将所有文本批量转换为向量。</li>
<li>完成向量化后，它并不直接处理索引构建，而是将生成的向量和其他所有信息（文本、元数据等）传递给一个内部的辅助方法 <code>__from</code>。</li>
</ul>
</li>
<li><strong><code>__from</code> (构建索引框架)</strong>:<ul>
<li>一个内部方法，负责搭建 FAISS 向量存储的“空框架”。</li>
<li>它会根据指定的距离策略（默认为 L2 欧氏距离）初始化一个空的 FAISS 索引结构（如 <code>faiss.IndexFlatL2</code>）。</li>
<li>同时，它也准备好了用于存储文档原文的 <code>docstore</code> 和用于连接 FAISS 索引与文档的 <code>index_to_docstore_id</code> 映射。</li>
<li>最后，它调用另一个内部方法 <code>__add</code> 来完成数据的填充。</li>
</ul>
</li>
<li><strong><code>__add</code> (填充数据)</strong>:<ul>
<li>真正执行数据添加操作的核心。它接收到向量、文本和元数据后，执行以下关键操作：<ul>
<li><strong>添加向量</strong>: 将向量列表转换为 FAISS 需要的 <code>numpy</code> 数组，并调用 <code>self.index.add(vector)</code> 将其批量添加到 FAISS 索引中。</li>
<li><strong>存储文档</strong>: 将文本和元数据打包成 <code>Document</code> 对象，存入 <code>docstore</code>。</li>
<li><strong>建立映射</strong>: 更新 <code>index_to_docstore_id</code> 字典，建立起 FAISS 内部的整数 ID（如 0, 1, 2…）到我们文档唯一 ID 的映射关系。</li>
</ul>
</li>
</ul>
</li>
</ol>
<h1 id="Milvus向量数据库"><a href="#Milvus向量数据库" class="headerlink" title="Milvus向量数据库"></a>Milvus向量数据库</h1><h2 id="Milvus安装"><a href="#Milvus安装" class="headerlink" title="Milvus安装"></a>Milvus安装</h2><h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><ul>
<li><strong>安装 Docker 与 Docker Compose</strong>: 确保系统中已安装并正在运行 Docker 和 Docker Compose。</li>
</ul>
<h3 id="下载并启动-Milvus"><a href="#下载并启动-Milvus" class="headerlink" title="下载并启动 Milvus"></a>下载并启动 Milvus</h3><p>在你选定的工作目录下，打开终端（Terminal）或命令行工具（PowerShell），执行以下步骤：</p>
<p><strong>第一步：下载配置文件</strong></p>
<p>使用以下命令下载官方的 <code>docker-compose.yml</code> 文件。这个文件定义了 Milvus Standalone 及其运行所需的两个核心依赖服务：<code>etcd</code> 用于存储元数据，<code>MinIO</code> 用于对象存储（更多架构细节请参考<a href="https://milvus.io/docs/architecture_overview.md">官方文档</a>）。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># macOS / Linux (使用 wget)</span></span><br><span class="line">wget https://github.com/milvus-io/milvus/releases/download/v2.5.14/milvus-standalone-docker-compose.yml -O docker-compose.ymlCopy to clipboardErrorCopied</span><br><span class="line"><span class="comment"># Windows (使用 PowerShell)</span></span><br><span class="line">Invoke-WebRequest -Uri <span class="string">&quot;https://github.com/milvus-io/milvus/releases/download/v2.5.14/milvus-standalone-docker-compose.yml&quot;</span> -OutFile <span class="string">&quot;docker-compose.yml&quot;</span>Copy to clipboardErrorCopied</span><br></pre></td></tr></table></figure>

<h3 id="第二步：启动-Milvus-服务"><a href="#第二步：启动-Milvus-服务" class="headerlink" title="第二步：启动 Milvus 服务"></a><strong>第二步：启动 Milvus 服务</strong></h3><p>在 <code>docker-compose.yml</code> 文件所在的目录中，运行以下命令以后台模式启动 Milvus：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose up -dCopy to clipboardErrorCopied</span><br></pre></td></tr></table></figure>

<p>Docker 将会自动拉取所需的镜像并启动三个容器：<code>milvus-standalone</code>, <code>milvus-minio</code>, 和 <code>milvus-etcd</code>。这个过程可能需要几分钟，具体取决于你的网络状况。</p>
<h3 id="验证安装"><a href="#验证安装" class="headerlink" title="验证安装"></a>验证安装</h3><p>可以通过以下方式验证 Milvus 是否成功启动：</p>
<ul>
<li><strong>查看 Docker 容器</strong>: 在终端运行 <code>docker ps</code> 命令 (Linux)，确认三个 Milvus 相关容器（<code>milvus-standalone</code>, <code>milvus-minio</code>, <code>milvus-etcd</code>）都处于 <code>running</code> 或 <code>up</code> 状态。</li>
<li><strong>检查服务端口</strong>: Milvus Standalone 默认通过 <code>19530</code> 端口提供服务，这是后续代码连接时需要用到的地址。</li>
</ul>
<h3 id="常用管理命令"><a href="#常用管理命令" class="headerlink" title="常用管理命令"></a>常用管理命令</h3><ul>
<li><p><strong>停止服务</strong>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose downCopy to clipboardErrorCopied</span><br></pre></td></tr></table></figure>

<p>此命令会停止并移除容器，但保留存储的数据卷。</p>
</li>
<li><p><strong>彻底清理 (停止并删除数据)</strong>: 如果想彻底删除所有数据（包括向量、元数据等），可以执行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose down -v</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="核心组件"><a href="#核心组件" class="headerlink" title="核心组件"></a>核心组件</h2><h3 id="Collection-集合"><a href="#Collection-集合" class="headerlink" title="Collection (集合)"></a>Collection (集合)</h3><p>可以用一个图书馆的比喻来理解 Collection：</p>
<ul>
<li><strong>Collection (集合)</strong>: 相当于一个<strong>图书馆</strong>，是所有数据的顶层容器。一个 Collection 可以包含多个 Partition，每个 Partition 可以包含多个 Entity。</li>
<li><strong>Partition (分区)</strong>: 相当于图书馆里的<strong>不同区域</strong>（如“小说区”、“科技区”），将数据物理隔离，让检索更高效。</li>
<li><strong>Schema (模式)</strong>: 相当于图书馆的<strong>图书卡片规则</strong>，定义了每本书（数据）必须登记哪些信息（字段）。</li>
<li><strong>Entity (实体)</strong>: 相当于<strong>一本具体的书</strong>，是数据本身。</li>
<li><strong>Alias (别名)</strong>: 相当于一个<strong>动态的推荐书单</strong>（如“本周精选”），它可以指向某个具体的 Collection，方便应用层调用，实现数据更新时的无缝切换。</li>
</ul>
<p><strong>Collection</strong> 是 Milvus 中最基本的数据组织单位，类似于关系型数据库中的一张**表 (Table)**。是我们存储、管理和查询向量及相关元数据的容器。所有的数据操作，如插入、删除、查询等，都是围绕 Collection 展开的。</p>
<p>一个 Collection 由其 <strong>Schema</strong> 定义，并包含以下重要的子概念和特性：</p>
<h4 id="Schema"><a href="#Schema" class="headerlink" title="Schema"></a>Schema</h4><p>在创建 Collection 之前，必须先定义它的 <strong>Schema</strong>。 <code>Schema</code> 规定了 Collection 的数据结构，定义了其中包含的所有<strong>字段 (Field)</strong> 及其属性。一个设计良好的 Schema 是能够保证数据一致性并提升查询性能。</p>
<p>Schema 通常包含以下几类字段：</p>
<ul>
<li><strong>主键字段 (Primary Key Field)</strong>: 每个 Collection 必须有且仅有一个主键字段，用于唯一标识每一条数据（实体）。它的值必须是唯一的，通常是整数或字符串类型。</li>
<li><strong>向量字段 (Vector Field)</strong>: 用于存储核心的向量数据。一个 Collection 可以有一个或多个向量字段，以满足多模态等复杂场景的需求。</li>
<li><strong>标量字段 (Scalar Field)</strong>: 用于存储除向量之外的元数据，如字符串、数字、布尔值、JSON 等。这些字段可以用于过滤查询，实现更精确的检索。</li>
</ul>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_4_1.webp" alt="Schema 设计剖析"></p>
<p>上图以一篇新闻文章为例，展示了一个典型的多模态、混合向量 Schema 设计。它将一篇文章拆解为：唯一的 <code>Article (ID)</code>、文本元数据（如 <code>Title</code>、<code>Author Info</code>）、图像信息（<code>Image URL</code>），并为图像和摘要内容分别生成了密集向量（<code>Image Embedding</code>, <code>Summary Embedding</code>）和稀疏向量（<code>Summary Sparse Embedding</code>）。</p>
<h4 id="Partition-分区"><a href="#Partition-分区" class="headerlink" title="Partition (分区)"></a>Partition (分区)</h4><p><strong>Partition</strong> 是 Collection 内部的一个逻辑划分。每个 Collection 在创建时都会有一个名为 <code>_default</code> 的默认分区。我们可以根据业务需求创建更多的分区，将数据按特定规则（如类别、日期等）存入不同分区。</p>
<p><strong>为什么使用分区？</strong></p>
<ul>
<li><strong>提升查询性能</strong>: 在查询时，可以指定只在一个或几个分区内进行搜索，从而大幅减少需要扫描的数据量，显著提升检索速度。</li>
<li><strong>数据管理</strong>: 便于对部分数据进行批量操作，如加载&#x2F;卸载特定分区到内存，或者删除整个分区的数据。</li>
</ul>
<p>一个 Collection 最多可以有 1024 个分区。合理利用分区是 Milvus 性能优化的重要手段之一。</p>
<h4 id="Alias-别名"><a href="#Alias-别名" class="headerlink" title="Alias (别名)"></a>Alias (别名)</h4><p><strong>Alias</strong> (别名) 是为 Collection 提供的一个“昵称”。通过为一个 Collection 设置别名，我们可以在应用程序中使用这个别名来执行所有操作，而不是直接使用真实的 Collection 名称。</p>
<p><strong>为什么使用别名？</strong></p>
<ul>
<li><strong>安全地更新数据</strong>：想象一下，你需要对一个在线服务的 Collection 进行大规模的数据更新或重建索引。直接在原 Collection 上操作风险很高。正确的做法是：<ol>
<li>创建一个新的 Collection (<code>collection_v2</code>) 并导入、索引好所有新数据。</li>
<li>将指向旧 Collection (<code>collection_v1</code>) 的别名（例如 <code>my_app_collection</code>）原子性地切换到新 Collection (<code>collection_v2</code>) 上。</li>
</ol>
</li>
<li><strong>代码解耦</strong>：整个切换过程对上层应用完全透明，无需修改任何代码或重启服务，实现了数据的平滑无缝升级。</li>
</ul>
<h3 id="索引-Index"><a href="#索引-Index" class="headerlink" title="索引 (Index)"></a>索引 (Index)</h3><p>如果说 Collection 是 Milvus 的骨架，那么<strong>索引 (Index)</strong> 就是其加速检索的神经系统。从宏观上看，索引本身就是一种<strong>为了加速查询而设计的复杂数据结构</strong>。对向量数据创建索引后，Milvus 可以极大地提升向量相似性搜索的速度，代价是会占用额外的存储和内存资源。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_4_2.webp" alt="Milvus 索引结构与工作原理"></p>
<p>上图清晰地展示了 Milvus 向量索引的内部组件及其工作流程：</p>
<ul>
<li><strong>数据结构</strong>：这是索引的骨架，定义了向量的组织方式（如 HNSW 中的图结构）。</li>
<li><strong>量化</strong>(可选)：数据压缩技术，通过降低向量精度来减少内存占用和加速计算。</li>
<li><strong>结果精炼</strong>(可选)：在找到初步候选集后，进行更精确的计算以优化最终结果。</li>
</ul>
<p>Milvus 支持对标量字段和向量字段分别创建索引。</p>
<ul>
<li><strong>标量字段索引</strong>：主要用于加速元数据过滤，常用的有 <code>INVERTED</code>、<code>BITMAP</code> 等。通常使用推荐的索引类型即可。</li>
<li><strong>向量字段索引</strong>：这是 Milvus 的核心。选择合适的向量索引是在查询性能、召回率和内存占用之间做出权衡的艺术。</li>
</ul>
<h4 id="主要向量索引类型"><a href="#主要向量索引类型" class="headerlink" title="主要向量索引类型"></a>主要向量索引类型</h4><p>Milvus 提供了多种向量索引算法，以适应不同的应用场景。以下是几种最核心的类型：</p>
<ul>
<li><strong>FLAT (精确查找)</strong><ul>
<li><strong>原理</strong>：暴力搜索（Brute-force Search）。它会计算查询向量与集合中所有向量之间的实际距离，返回最精确的结果。</li>
<li><strong>优点</strong>：100% 的召回率，结果最准确。</li>
<li><strong>缺点</strong>：速度慢，内存占用大，不适合海量数据。</li>
<li><strong>适用场景</strong>：对精度要求极高，且数据规模较小（百万级以内）的场景。</li>
</ul>
</li>
<li><strong>IVF 系列 (倒排文件索引)</strong><ul>
<li><strong>原理</strong>：类似于书籍的目录。它首先通过聚类将所有向量分成多个“桶”(<code>nlist</code>)，查询时，先找到最相似的几个“桶”，然后只在这几个桶内进行精确搜索。<code>IVF_FLAT</code>、<code>IVF_SQ8</code>、<code>IVF_PQ</code> 是其不同变体，主要区别在于是否对桶内向量进行了压缩（量化）。</li>
<li><strong>优点</strong>：通过缩小搜索范围，极大地提升了检索速度，是性能和效果之间很好的平衡。</li>
<li><strong>缺点</strong>：召回率不是100%，因为相关向量可能被分到了未被搜索的桶中。</li>
<li><strong>适用场景</strong>：通用场景，尤其适合需要高吞吐量的大规模数据集。</li>
</ul>
</li>
<li><strong>HNSW (基于图的索引)</strong><ul>
<li><strong>原理</strong>：构建一个多层的邻近图。查询时从最上层的稀疏图开始，快速定位到目标区域，然后在下层的密集图中进行精确搜索。</li>
<li><strong>优点</strong>：检索速度极快，召回率高，尤其擅长处理高维数据和低延迟查询。</li>
<li><strong>缺点</strong>：内存占用非常大，构建索引的时间也较长。</li>
<li><strong>适用场景</strong>：对查询延迟有严格要求（如实时推荐、在线搜索）的场景。</li>
</ul>
</li>
<li><strong>DiskANN (基于磁盘的索引)</strong><ul>
<li><strong>原理</strong>：一种为在 SSD 等高速磁盘上运行而优化的图索引。</li>
<li><strong>优点</strong>：支持远超内存容量的海量数据集（十亿级甚至更多），同时保持较低的查询延迟。</li>
<li><strong>缺点</strong>：相比纯内存索引，延迟稍高。</li>
<li><strong>适用场景</strong>：数据规模巨大，无法全部加载到内存的场景。</li>
</ul>
</li>
</ul>
<h4 id="如何选择索引？"><a href="#如何选择索引？" class="headerlink" title="如何选择索引？"></a>如何选择索引？</h4><p>选择索引没有唯一的“最佳答案”，需要根据业务场景在<strong>数据规模、内存限制、查询性能和召回率</strong>之间进行权衡。</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>推荐索引</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>数据可完全载入内存，追求低延迟</td>
<td><strong>HNSW</strong></td>
<td>内存占用较大，但查询性能和召回率都很优秀。</td>
</tr>
<tr>
<td>数据可完全载入内存，追求高吞吐</td>
<td><strong>IVF_FLAT &#x2F; IVF_SQ8</strong></td>
<td>性能和资源消耗的平衡之选。</td>
</tr>
<tr>
<td>数据量巨大，无法载入内存</td>
<td><strong>DiskANN</strong></td>
<td>在 SSD 上性能优异，专为海量数据设计。</td>
</tr>
<tr>
<td>追求 100% 准确率，数据量不大</td>
<td><strong>FLAT</strong></td>
<td>暴力搜索，确保结果最精确。</td>
</tr>
</tbody></table>
<p>在实际应用中，通常需要通过测试来找到最适合自己数据和查询模式的索引类型及其参数。</p>
<h3 id="检索"><a href="#检索" class="headerlink" title="检索"></a>检索</h3><h4 id="基础向量检索-ANN-Search"><a href="#基础向量检索-ANN-Search" class="headerlink" title="基础向量检索 (ANN Search)"></a>基础向量检索 (ANN Search)</h4><p>拥有了数据容器 (Collection) 和检索引擎 (Index) 后，最后一步就是从海量数据中高效地检索信息。这是 Milvus 的核心功能之一，<strong>近似最近邻 (Approximate Nearest Neighbor, ANN) 检索</strong>。与需要计算全部数据的暴力检索（Brute-force Search）不同，ANN 检索利用预先构建好的索引，能够极速地从海量数据中找到与查询向量最相似的 Top-K 个结果。这是一种在速度和精度之间取得极致平衡的策略。</p>
<ul>
<li><strong>主要参数</strong>:<ul>
<li><code>anns_field</code>: 指定要在哪个向量字段上进行检索。</li>
<li><code>data</code>: 传入一个或多个查询向量。</li>
<li><code>limit</code> (或 <code>top_k</code>): 指定需要返回的最相似结果的数量。</li>
<li><code>search_params</code>: 指定检索时使用的参数，例如距离计算方式 (<code>metric_type</code>) 和索引相关的查询参数。</li>
</ul>
</li>
</ul>
<h4 id="增强检索"><a href="#增强检索" class="headerlink" title="增强检索"></a>增强检索</h4><p>在基础的 ANN 检索之上，Milvus 提供了多种增强检索功能，以满足更复杂的业务需求。</p>
<p><strong>过滤检索 (Filtered Search)</strong></p>
<p>在实际应用中，我们很少只进行单纯的向量检索。更常见的需求是“在满足特定条件的向量中，查找最相似的结果”，这就是过滤检索。它将<strong>向量相似性检索</strong>与<strong>标量字段过滤</strong>结合在一起。</p>
<ul>
<li><strong>工作原理</strong>：先根据提供的过滤表达式 (<code>filter</code>) 筛选出符合条件的实体，然后仅在这个子集内执行 ANN 检索。这极大地提高了查询的精准度。</li>
<li><strong>应用示例</strong>：<ul>
<li><strong>电商</strong>：”检索与这件红色连衣裙最相似的商品，但只看价格低于500元且有库存的。”</li>
<li><strong>知识库</strong>：”查找与‘人工智能’相关的文档，但只从‘技术’分类下、且发布于2023年之后的文章中寻找。”</li>
</ul>
</li>
</ul>
<p><strong>范围检索 (Range Search)</strong></p>
<p>有时我们关心的不是最相似的 Top-K 个结果，而是“所有与查询向量的相似度在特定范围内的结果”。</p>
<ul>
<li><strong>工作原理</strong>：范围检索允许定义一个距离（或相似度）的阈值范围。Milvus 会返回所有与查询向量的距离落在这个范围内的实体。</li>
<li><strong>应用示例</strong>：<ul>
<li><strong>人脸识别</strong>：”查找所有与目标人脸相似度超过 0.9 的人脸”，用于身份验证。</li>
<li><strong>异常检测</strong>：”查找所有与正常样本向量距离过大的数据点”，用于发现异常。</li>
</ul>
</li>
</ul>
<p><strong>多向量混合检索 (Hybrid Search)</strong></p>
<p>这是 Milvus 提供的一种极其强大的高级检索模式，它允许在一个请求中同时检索<strong>多个向量字段</strong>，并将结果智能地融合在一起。</p>
<ul>
<li><strong>工作原理</strong>：<ol>
<li><strong>并行检索</strong>：应用针对不同的向量字段（如一个用于文本语义的密集向量，一个用于关键词匹配的稀疏向量，一个用于图像内容的多模态向量）分别发起 ANN 检索请求。</li>
<li>**结果融合 (Rerank)**：Milvus 使用一个重排策略（Reranker）将来自不同检索流的结果合并成一个统一的、更高质量的排序列表。常用的策略有 <code>RRFRanker</code>（平衡各方结果）和 <code>WeightedRanker</code>（可为特定字段结果加权）。</li>
</ol>
</li>
<li><strong>应用示例</strong>：<ul>
<li><strong>多模态商品检索</strong>：用户输入文本“安静舒适的白色耳机”，系统可以同时检索商品的<strong>文本描述向量</strong>和<strong>图片内容向量</strong>，返回最匹配的商品。</li>
<li><strong>增强型 RAG</strong>: 结合<strong>密集向量</strong>（捕捉语义）和<strong>稀疏向量</strong>（精确匹配关键词），实现比单一向量更精准的文档检索效果。</li>
</ul>
</li>
</ul>
<p><strong>分组检索 (Grouping Search)</strong></p>
<p>分组检索解决了一个常见的痛点：检索结果多样性不足。想象一下，你检索“机器学习”，返回的前10篇文章都来自同一本教科书不同章节。这显然不是理想的结果。</p>
<ul>
<li><strong>工作原理</strong>：分组检索允许指定一个字段（如 <code>document_id</code>）对结果进行分组。Milvus 会在检索后，确保返回的结果中每个组（每个 <code>document_id</code>）只出现一次（或指定的次数），且返回的是该组内与查询最相似的那个实体。</li>
<li><strong>应用示例</strong>：<ul>
<li><strong>视频检索</strong>：检索“可爱的猫咪”，确保返回的视频来自不同的博主。</li>
<li><strong>文档检索</strong>：检索“数据库索引”，确保返回的结果来自不同的书籍或来源。</li>
</ul>
</li>
</ul>
<p>通过这些灵活的检索功能组合，开发者可以构建出满足各种复杂业务需求的向量检索应用。</p>
<h1 id="索引优化"><a href="#索引优化" class="headerlink" title="索引优化"></a>索引优化</h1><h2 id="上下文扩展"><a href="#上下文扩展" class="headerlink" title="上下文扩展"></a>上下文扩展</h2><p>在RAG系统中，常常面临一个权衡问题：使用小块文本进行检索可以获得更高的精确度，但小块文本缺乏足够的上下文，可能导致大语言模型（LLM）无法生成高质量的答案；而使用大块文本虽然上下文丰富，却容易引入噪音，降低检索的相关性。为了解决这一矛盾，LlamaIndex 提出了一种实用的索引策略——<strong>句子窗口检索（Sentence Window Retrieval）</strong><a href="https://datawhalechina.github.io/all-in-rag/#fn-2">2</a>。该技术巧妙地结合了两种方法的优点：它在检索时聚焦于高度精确的单个句子，在送入LLM生成答案前，又智能地将上下文扩展回一个更宽的“窗口”，从而同时保证检索的准确性和生成的质量。</p>
<h3 id="主要思路"><a href="#主要思路" class="headerlink" title="主要思路"></a>主要思路</h3><p>句子窗口检索的思想可以概括为：<strong>为检索精确性而索引小块，为上下文丰富性而检索大块</strong>。</p>
<p>其工作流程如下：</p>
<p>（1）<strong>索引阶段</strong>：在构建索引时，文档被分割成<strong>单个句子</strong>。每个句子都作为一个独立的“节点（Node）”存入向量数据库。同时，每个句子节点都会在元数据（metadata）中存储其<strong>上下文窗口</strong>，即该句子原文中的前N个和后N个句子。这个窗口内的文本不会被索引，仅仅是作为元数据存储。</p>
<p>（2）<strong>检索阶段</strong>：当用户发起查询时，系统会在所有<strong>单一句子节点</strong>上执行相似度搜索。因为句子是表达完整语义的最小单位，所以这种方式可以非常精确地定位到与用户问题最相关的核心信息。</p>
<p>（3）<strong>后处理阶段</strong>：在检索到最相关的句子节点后，系统会使用一个名为 <code>MetadataReplacementPostProcessor</code> 的后处理模块。该模块会读取到检索到的句子节点的元数据，并用元数据中存储的<strong>完整上下文窗口</strong>来替换节点中原来的单一句子内容。</p>
<p>（4）<strong>生成阶段</strong>：最后，这些被替换了内容的、包含丰富上下文的节点被传递给LLM，用于生成最终的答案。</p>
<h1 id="结构化索引"><a href="#结构化索引" class="headerlink" title="结构化索引"></a>结构化索引</h1><p>随着知识库的规模不断扩大（例如，包含数百个PDF文件），传统的RAG方法（即对所有文本块进行top-k相似度搜索）会遇到瓶颈。当一个查询可能只与其中一两个文档相关时，在整个文档库中进行无差别的向量搜索，不仅效率低下，还容易被不相关的文本块干扰，导致检索结果不精确。</p>
<p>为了解决这个问题，一个有效的方法是利用<strong>结构化索引</strong>。其原理是在索引文本块的同时，为其附加结构化的<strong>元数据（Metadata）</strong>。这些元数据可以是任何有助于筛选和定位信息的标签，例如：</p>
<ul>
<li>文件名</li>
<li>文档创建日期</li>
<li>章节标题</li>
<li>作者</li>
<li>任何自定义的分类标签</li>
</ul>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter3/images/3_5_1.webp" alt="结构化索引"></p>
<p>实际上，RAG-文本分块中介绍的<strong>基于文档结构的分块</strong>方法，就是实现结构化索引的一种前置步骤。例如，在使用 <code>MarkdownHeaderTextSplitter</code> 时，分块器会自动将Markdown文档的各级标题（如 <code>Header 1</code>, <code>Header 2</code> 等）提取并存入每个文本块的元数据中。这些标题信息就是非常有价值的结构化数据，可以直接用于后续的元数据过滤。</p>
<p>通过这种方式，可以在检索时实现“元数据过滤”和“向量搜索”的结合。例如，当用户查询“请总结一下2023年第二季度财报中关于AI的论述”时，系统可以：</p>
<p>（1）<strong>元数据预过滤</strong>：首先通过元数据筛选，只在 <code>document_type == &#39;财报&#39;</code>、<code>year == 2023</code> 且 <code>quarter == &#39;Q2&#39;</code> 的文档子集中进行搜索。</p>
<p>（2）<strong>向量搜索</strong>：然后，在经过滤的、范围更小的文本块集合中，执行针对查询“关于AI的论述”的向量相似度搜索。</p>
<p>这种“先过滤，再搜索”的策略，能够极大地缩小检索范围，显著提升大规模知识库场景下RAG应用的检索效率和准确性。LlamaIndex 提供了包括“自动检索”（Auto-Retrieval）在内的多种工具来支持这种结构化的检索范式。</p>
]]></content>
      <categories>
        <category>RAG</category>
        <category>索引构建</category>
      </categories>
  </entry>
  <entry>
    <title>RAG-检索优化</title>
    <url>/2026/04/16/RAG-%E6%A3%80%E7%B4%A2%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="混合检索"><a href="#混合检索" class="headerlink" title="混合检索"></a>混合检索</h1><p>混合检索（Hybrid Search）是一种结合了 <strong>稀疏向量（Sparse Vectors）</strong> 和 <strong>密集向量（Dense Vectors）</strong> 优势的先进搜索技术。旨在同时利用稀疏向量的关键词精确匹配能力和密集向量的语义理解能力，以克服单一向量检索的局限性，从而在各种搜索场景下提供更准确、更鲁棒的检索结果。</p>
<h2 id="稀疏向量"><a href="#稀疏向量" class="headerlink" title="稀疏向量"></a>稀疏向量</h2><p>稀疏向量，也常被称为“词法向量”，是基于词频统计的传统信息检索方法的数学表示。它通常是一个维度极高（与词汇表大小相当）但绝大多数元素为零的向量。它采用精准的“词袋”匹配模型，将文档视为一堆词的集合，不考虑其顺序和语法，其中向量的每一个维度都直接对应一个具体的词，非零值则代表该词在文档中的重要性（权重）。这类向量的经典权重计算方法是 TF-IDF。在信息检索领域，BM25 则是基于这种稀疏表示的成功且应用广泛的排序算法之一，其核心公式如下：</p>
<p>$$<br>Score(Q,D)&#x3D;\sum_{i&#x3D;1}^n IDF(q_i)\cdot\frac{f(q_i,D)+k_1⋅(1−b+b\cdot\frac{avgdl}{∣D∣})}{f(q_i,D)\cdot(k_1+1)}<br>$$<br>其中：</p>
<ul>
<li>$IDF(q_i)$: 查询词 $q_i$的逆文档频率，用于衡量一个词的普遍程度。越常见的词，IDF值越低。</li>
<li>$f(q_i,D)$: 查询词 $q_i$在文档 D<em>D</em> 中的词频。</li>
<li>$∣D∣$: 文档 D的长度。</li>
<li>$avgdl$: 集合中所有文档的平均长度。</li>
<li>$k_1,b$: 可调节的超参数。 $k_i$ 用于控制词频饱和度（一个词在文档中出现10次和100次，其重要性增长并非线性）， $b$用于控制文档长度归一化的程度。</li>
</ul>
<p>这种方法的优点是可解释性极强（每个维度都代表一个确切的词），无需训练，能够实现关键词的精确匹配，对于专业术语和特定名词的检索效果好。主要缺点是无法理解语义，例如它无法识别“汽车”和“轿车”是同义词，存在“词汇鸿沟”。</p>
<p><strong>稀疏向量表示:</strong></p>
<p>稀疏向量的核心思想是只存储非零值。例如，一个8维的向量 <code>[0, 0, 0, 5, 0, 0, 0, 9]</code>，其大部分元素都是零。用稀疏格式表示，可以极大地节约空间。常见的稀疏表示法有两种：</p>
<ol>
<li><p><strong>字典 &#x2F; 键值对 (Dictionary &#x2F; Key-Value):</strong> 这种方式将非零元素的 <code>索引</code> (0-based) 作为键，<code>值</code> 作为值。上面的向量可以表示为：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// &#123;索引: 值&#125;</span></span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;3&quot;</span><span class="punctuation">:</span> <span class="number">5</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;7&quot;</span><span class="punctuation">:</span> <span class="number">9</span></span><br><span class="line"><span class="punctuation">&#125;</span>Copy to clipboardErrorCopied</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>坐标列表 (Coordinate list - COO):</strong> 这种方式通常用一个元组 <code>(维度, [索引列表], [值列表])</code> 来表示。上面的向量可以表示为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(8, [3, 7], [5, 9])Copy to clipboardErrorCopied</span><br></pre></td></tr></table></figure>

<p>这种格式在 <code>SciPy</code> 等科学计算库中非常常见。</p>
</li>
</ol>
<p>假设在一个包含5万个词的词汇表中，“西红柿”在第88位，“炒”在第666位，“蛋”在第999位，它们的BM25权重分别是1.2、0.8、1.5。那么它的稀疏表示（采用字典格式）就是：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// &#123;索引: 权重&#125;</span></span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;88&quot;</span><span class="punctuation">:</span> <span class="number">1.2</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;666&quot;</span><span class="punctuation">:</span> <span class="number">0.8</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;999&quot;</span><span class="punctuation">:</span> <span class="number">1.5</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>如果采用坐标列表（COO）格式，它会是这样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(50000, [88, 666, 999], [1.2, 0.8, 1.5])</span><br></pre></td></tr></table></figure>

<p>这两种格式都清晰地记录了文档的关键信息，但它们的局限性也很明显：如果我们搜索“番茄炒鸡蛋”，由于“番茄”和“西红柿”是不同的词条（索引不同），模型将无法理解它们的语义相似性。</p>
<h2 id="密集向量"><a href="#密集向量" class="headerlink" title="密集向量"></a>密集向量</h2><p>密集向量，也常被称为“语义向量”，是通过深度学习模型学习到的数据（如文本、图像）的<strong>低维、稠密</strong>的浮点数表示。这些向量旨在将原始数据映射到一个连续的、充满意义的“语义空间”中来捕捉“语义”或“概念”。在理想的语义空间中，向量之间的距离和方向代表了它们所表示概念之间的关系。一个经典的例子是 <code>vector(&#39;国王&#39;) - vector(&#39;男人&#39;) + vector(&#39;女人&#39;)</code> 的计算结果在向量空间中非常接近 <code>vector(&#39;女王&#39;)</code>，这表明模型学会了“性别”和“皇室”这两个维度的抽象概念。它的代表包括 Word2Vec、GloVe、以及所有基于 Transformer 的模型（如 BERT、GPT）生成的嵌入（Embeddings）。</p>
<p>其主要优点是能够理解同义词、近义词和上下文关系，泛化能力强，在语义搜索任务中表现卓越。但缺点也同样明显：可解释性差（向量中的每个维度通常没有具体的物理意义），需要大量数据和算力进行模型训练，且对于未登录词（OOV）的处理相对困难。</p>
<p><strong>密集向量表示:</strong></p>
<p>与稀疏向量不同，密集向量的所有维度都有值，因此使用<strong>数组 <code>[]</code></strong> 来表示是最直接的方式。一个预训练好的语义模型在读取“西红柿炒蛋”后，会输出一个低维的密集向量：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是一个低维（比如1024维）的浮点数向量</span></span><br><span class="line"><span class="comment">// 向量的每个维度没有直接的、可解释的含义</span></span><br><span class="line"><span class="punctuation">[</span><span class="number">0.89</span><span class="punctuation">,</span> <span class="number">-0.12</span><span class="punctuation">,</span> <span class="number">0.77</span><span class="punctuation">,</span> ...<span class="punctuation">,</span> <span class="number">-0.45</span><span class="punctuation">]</span></span><br></pre></td></tr></table></figure>

<p>这个向量本身难以解读，但它在语义空间中的位置可能与“番茄鸡蛋面”、“洋葱炒鸡蛋”等菜肴的向量非常接近，因为模型理解了它们共享“鸡蛋类菜肴”、“家常菜”、“酸甜口味”等核心概念。因此，当我们搜索“蛋白质丰富的家常菜”时，即使查询中没有出现任何原文关键词，密集向量也很有可能成功匹配到这份菜谱。</p>
<blockquote>
<p>[!NOTE]</p>
<p><strong>OOV（Out-of-Vocabulary）未登录词</strong>：指在模型训练时没有出现在词汇表中，但在实际使用时遇到的新词汇。传统的稀疏向量方法（如BM25）对OOV词汇会完全忽略，而现代的密集向量方法通过子词分割（如BPE、WordPiece）可以更好地处理OOV问题。</p>
</blockquote>
<h2 id="混合检索-1"><a href="#混合检索-1" class="headerlink" title="混合检索"></a>混合检索</h2><p>稀疏向量和密集向量各有千秋，那么将它们结合起来，实现优势互补，就成了一个不错的选择。混合检索便是基于这个思路，通过结合多种搜索算法（最常见的是稀疏与密集检索）来提升搜索结果相关性和召回率。</p>
<ul>
<li><strong>主要目标</strong>：解决单一检索技术的局限性。例如，关键词检索无法理解语义，而向量检索则可能忽略掉必须精确匹配的关键词（如产品型号、函数名等）。混合检索旨在同时利用稀疏向量的<strong>精确性</strong>和密集向量的<strong>泛化性</strong>，以应对复杂多变的搜索需求。</li>
</ul>
<h3 id="技术原理与融合方法"><a href="#技术原理与融合方法" class="headerlink" title="技术原理与融合方法"></a>技术原理与融合方法</h3><p>混合检索通常并行执行两种检索算法，然后将两组异构的结果集融合成一个统一的排序列表。以下是两种主流的融合策略：</p>
<h4 id="倒数排序融合-Reciprocal-Rank-Fusion-RRF"><a href="#倒数排序融合-Reciprocal-Rank-Fusion-RRF" class="headerlink" title="倒数排序融合 (Reciprocal Rank Fusion, RRF)"></a>倒数排序融合 (Reciprocal Rank Fusion, RRF)</h4><p>RRF 不关心不同检索系统的原始得分，只关心每个文档在各自结果集中的<strong>排名</strong>。其思想是：一个文档在不同检索系统中的排名越靠前，它的最终得分就越高。</p>
<p>其计分公式为：<br>$$<br>RRF_{score}(d) &#x3D; \sum_{i&#x3D;1}^k \frac{1}{rank_i(d) + c}<br>$$<br>其中：</p>
<ul>
<li>$d$ 是待评分的文档。</li>
<li>$k$ 是检索系统的数量（这里是2，即稀疏和密集）。</li>
<li>$rank_i(d)$是文档 d<em>d</em> 在第 $i$ 个检索系统中的排名。</li>
<li>$c$是一个常数（通常设为60），用于降低排名靠前文档的相对权重，实现更稳健的排名融合。</li>
</ul>
<h4 id="加权线性组合"><a href="#加权线性组合" class="headerlink" title="加权线性组合"></a>加权线性组合</h4><p>这种方法需要先将不同检索系统的得分进行归一化（例如，统一到 0-1 区间），然后通过一个权重参数 $\alpha$ 来进行线性组合。<br>$$<br>Hybrid_{score} &#x3D; \alpha \cdot Dense_{score} + (1 - \alpha) \cdot Sparse_{score}<br>$$<br>通过调整 $\alpha$ 的值，可以灵活地控制语义相似性与关键词匹配在最终排序中的贡献比例。例如，在电商搜索中，可以调高关键词的权重；而在智能问答中，则可以侧重于语义。</p>
<h3 id="优势与局限"><a href="#优势与局限" class="headerlink" title="优势与局限"></a>优势与局限</h3><table>
<thead>
<tr>
<th align="left">优势</th>
<th align="left">局限</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>召回率与准确率高</strong>：能同时捕获关键词和语义，显著优于单一检索。</td>
<td align="left"><strong>计算资源消耗大</strong>：需要同时维护和查询两套索引。</td>
</tr>
<tr>
<td align="left"><strong>灵活性强</strong>：可通过融合策略和权重调整，适应不同业务场景。</td>
<td align="left"><strong>参数调试复杂</strong>：融合权重等超参数需要反复实验调优。</td>
</tr>
<tr>
<td align="left"><strong>容错性好</strong>：关键词检索可部分弥补向量模型对拼写错误或罕见词的敏感性。</td>
<td align="left"><strong>可解释性仍是挑战</strong>：融合后的结果排序理由难以直观分析。</td>
</tr>
</tbody></table>
<h1 id="查询构建"><a href="#查询构建" class="headerlink" title="查询构建"></a>查询构建</h1><p>在实际应用中，我们常常需要处理更加复杂和多样化的数据，包括结构化数据（如SQL数据库）、半结构化数据（如带有元数据的文档）以及图数据。用户的查询也可能不仅仅是简单的语义匹配，而是包含复杂的过滤条件、聚合操作或关系查询。</p>
<p><strong>查询构建（Query Construction）</strong><a href="https://datawhalechina.github.io/all-in-rag/#fn-1">1</a> 正是应对这一挑战的关键技术。它利用大语言模型（LLM）的强大理解能力，将用户的自然语言查询“翻译”成针对特定数据源的结构化查询语言或带有过滤条件的请求。这使得RAG系统能够无缝地连接和利用各种类型的数据，从而极大地扩展了其应用场景和能力。</p>
<p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260418122703299.png?token=AR3LVDCJZ4IEMTVPL3AOZHDJ4MEFG" alt="image-20260418122648264"></p>
<h2 id="文本到元数据过滤器"><a href="#文本到元数据过滤器" class="headerlink" title="文本到元数据过滤器"></a>文本到元数据过滤器</h2><p>在构建向量索引时，常常会为文档块（Chunks）附加元数据（Metadata），例如文档来源、发布日期、作者、章节、类别等。这些元数据为我们提供了在语义搜索之外进行精确过滤的可能。</p>
<p><strong>自查询检索器（Self-Query Retriever）</strong> 是LangChain中实现这一功能的核心组件。它的工作流程如下：</p>
<ol>
<li><strong>定义元数据结构</strong>：首先，需要向LLM清晰地描述文档内容和每个元数据字段的含义及类型。</li>
<li><strong>查询解析</strong>：当用户输入一个自然语言查询时，自查询检索器会调用LLM，将查询分解为两部分：<ul>
<li><strong>查询字符串（Query String）</strong>：用于进行语义搜索的部分。</li>
<li><strong>元数据过滤器（Metadata Filter）</strong>：从查询中提取出的结构化过滤条件。</li>
</ul>
</li>
<li><strong>执行查询</strong>：检索器将解析出的查询字符串和元数据过滤器发送给向量数据库，执行一次同时包含语义搜索和元数据过滤的查询。</li>
</ol>
<p>例如，对于查询“关于2022年发布的机器学习的论文”，自查询检索器会将其解析为：</p>
<ul>
<li><strong>查询字符串</strong>: “机器学习的论文”</li>
<li><strong>元数据过滤器</strong>: <code>year == 2022</code></li>
</ul>
<h2 id="文本到Cypher"><a href="#文本到Cypher" class="headerlink" title="文本到Cypher"></a>文本到Cypher</h2><p>除了处理扁平化的元数据，查询构建技术还能应用于更复杂的数据结构，如图数据库。</p>
<h3 id="什么是-Cypher？"><a href="#什么是-Cypher？" class="headerlink" title="什么是 Cypher？"></a>什么是 Cypher？</h3><p>Cypher 是图数据库（如 Neo4j）中最常用的查询语言，其地位类似于 SQL 之于关系数据库。它采用一种直观的方式来匹配图中的模式和关系，例如 <code>(:Person &#123;name:&quot;Tomaz&quot;&#125;)-[:LIVES_IN]-&gt;(:Country &#123;name:&quot;Slovenia&quot;&#125;)</code> 描述了一个人和一个国家以及他们之间的“居住在”关系。</p>
<h3 id="文本到Cypher”的原理"><a href="#文本到Cypher”的原理" class="headerlink" title="文本到Cypher”的原理"></a>文本到Cypher”的原理</h3><p>与“文本到元数据过滤器”类似，“文本到Cypher”技术利用大语言模型（LLM）将用户的自然语言问题直接翻译成一句精准的 Cypher 查询语句。LangChain 提供了相应的工具链（如 <code>GraphCypherQAChain</code>），其工作流程通常是：</p>
<ol>
<li>接收用户的自然语言问题。</li>
<li>LLM 根据预先提供的图谱模式（Schema），将问题转换为 Cypher 查询。</li>
<li>在图数据库上执行该查询，获取精确的结构化数据。</li>
<li>(可选)将查询结果再次交由 LLM，生成通顺的自然语言答案。</li>
</ol>
<p>由于生成有效的 Cypher 查询是一项复杂的任务，通常使用性能较强的 LLM 来确保转换的准确性。通过这种方式，用户可以用最自然的方式与高度结构化的图数据进行交互，极大地降低了数据查询的门槛。</p>
<h1 id="文本到SQL"><a href="#文本到SQL" class="headerlink" title="文本到SQL"></a>文本到SQL</h1><p>在数据世界中，除了向量数据库能够处理的非结构化数据，关系型数据库（如 MySQL, PostgreSQL, SQLite）同样是存储和管理结构化数据的重点。<strong>文本到SQL（Text-to-SQL）</strong>正是为了打破人与结构化数据之间的语言障碍而生。它利用大语言模型（LLM）将用户的自然语言问题，直接翻译成可以在数据库上执行的SQL查询语句。</p>
<p><img src="https://raw.githubusercontent.com/Guailper/MyPicGo/master/images/20260418130229381.png?token=AR3LVDCDOH4T4V2OVC3SPU3J4MIKA" alt="image-20260418130227803"></p>
<h2 id="挑战"><a href="#挑战" class="headerlink" title="挑战"></a>挑战</h2><ul>
<li><strong>“幻觉”问题</strong>：LLM 可能会“想象”出数据库中不存在的表或字段，导致生成的SQL语句无效。</li>
<li><strong>对数据库结构理解不足</strong>：LLM 需要准确理解表的结构、字段的含义以及表与表之间的关联关系，才能生成正确的 <code>JOIN</code> 和 <code>WHERE</code> 子句。</li>
<li><strong>处理用户输入的模糊性</strong>：用户的提问可能存在拼写错误或不规范的表达（例如，“上个月的销售冠军是谁？”），模型需要具备一定的容错和推理能力。</li>
</ul>
<h2 id="优化策略"><a href="#优化策略" class="headerlink" title="优化策略"></a>优化策略</h2><ol>
<li><strong>提供精确的数据库模式</strong>：这是最基础也是最关键的一步。我们需要向LLM提供数据库中相关表的 <code>CREATE TABLE</code> 语句。这就像是给了LLM一张地图，让它了解数据库的结构，包括表名、列名、数据类型和外键关系。</li>
<li><strong>提供少量高质量的示例</strong>：在提示（Prompt）中加入一些“问题-SQL”的示例对，可以极大地提升LLM生成查询的准确性。这相当于给了LLM几个范例，让它学习如何根据相似的问题构建查询。</li>
<li><strong>利用RAG增强上下文</strong>：这是更进一步的策略。我们可以像RAGFlow一样，为数据库构建一个专门的“知识库”<a href="https://datawhalechina.github.io/all-in-rag/#fn-2">2</a>，其中不仅包含表的DDL（数据定义语言），还可以包含：<ul>
<li><strong>表和字段的详细描述</strong>：用自然语言解释每个表是做什么的，每个字段代表什么业务含义。</li>
<li><strong>同义词和业务术语</strong>：例如，将用户的“花费”映射到数据库的 <code>cost</code> 字段。</li>
<li><strong>复杂的查询示例</strong>：提供一些包含 <code>JOIN</code>、<code>GROUP BY</code> 或子查询的复杂问答对。 当用户提问时，系统首先从这个知识库中检索最相关的信息（如相关的表结构、字段描述、相似的Q&amp;A），然后将这些信息和用户的问题一起组合成一个内容更丰富的提示，交给LLM生成最终的SQL查询。这种方式极大地降低了“幻觉”的风险，提高了查询的准确度。</li>
</ul>
</li>
<li>**错误修正与反思 (Error Correction and Reflection)**：在生成SQL后，系统会尝试执行它。如果数据库返回错误，可以将错误信息反馈给LLM，让它“反思”并修正SQL语句，然后重试。这个迭代过程可以显著提高查询的成功率。</li>
</ol>
<h1 id="查询重构与分发"><a href="#查询重构与分发" class="headerlink" title="查询重构与分发"></a>查询重构与分发</h1><p>用户的原始问题往往不是最优的检索输入。它可能过于复杂、包含歧义，或者与文档的实际措辞存在偏差。为了解决这些问题，需要在检索之前对用户的查询进行“预处理”，这就是<strong>查询重构与分发</strong>。</p>
<p>两个关键技术：</p>
<ol>
<li><strong>查询翻译（Query Translation）</strong>：将用户的原始问题转换成一个或多个更适合检索的形式。</li>
<li><strong>查询路由（Query Routing）</strong>：根据问题的性质，将其智能地分发到最合适的数据源或检索器。</li>
</ol>
<h2 id="查询翻译"><a href="#查询翻译" class="headerlink" title="查询翻译"></a>查询翻译</h2><p>减弱用户自然语言提问与文档库中存储信息之间的“语义差距”。通过<strong>重写、分解或扩展</strong>查询，我们可以显著提升检索的准确率。</p>
<h3 id="提示工程"><a href="#提示工程" class="headerlink" title="提示工程"></a>提示工程</h3><p>最直接的查询重构方法。通过精心设计的提示词（Prompt），引导 LLM 将用户的原始查询改写得更清晰、更具体，或者转换成一种更利于检索的叙述风格。</p>
<p>在第二节查询构建的代码示例中，我们发现 <code>SelfQueryRetriever</code> 无法正确处理“时间最短的视频”这类需要排序或进行比较的查询。</p>
<p>为了解决这个问题，可以采用一种更高级的提示工程技巧：<strong>让 LLM 直接构建出查询指令</strong>。</p>
<p>这种方法的思路是，要求 LLM 直接分析用户的意图，并生成一个结构化（例如 JSON 格式）的指令，告诉我们的代码应该如何操作。对于“时间最短的视频”这个问题，我们期望 LLM 能直接告诉我们：“请按‘时长’字段进行升序排序，并返回第一条结果”。</p>
<p>下面，来看看如何修改代码来实现这一思路。我们不再使用 <code>SelfQueryRetriever</code>，而是直接与 LLM 交互，并根据其返回的指令在代码中执行排序逻辑。</p>
<h3 id="多查询分解-Multi-query"><a href="#多查询分解-Multi-query" class="headerlink" title="多查询分解 (Multi-query)"></a>多查询分解 (Multi-query)</h3><p>当用户提出一个复杂的问题时，直接用整个问题去检索可能效果不佳，因为它可能包含多个子主题或意图。分解技术的核心思想是将这个复杂问题拆分成多个更简单、更具体的子问题。然后，系统分别对每个子问题进行检索，最后将所有检索到的结果合并、去重，形成一个更全面的上下文，再交给 LLM 生成最终答案。</p>
<p><strong>示例</strong>：</p>
<ul>
<li><strong>原始问题</strong>：“在《流浪地球》中，刘慈欣对人工智能和未来社会结构有何看法？”</li>
<li><strong>分解后的子问题</strong>：<ul>
<li>“《流浪地球》中描述的人工智能技术有哪些？”</li>
<li>“《流浪地球》中描绘的未来社会是怎样的？”</li>
<li>“刘慈欣关于人工智能的观点是什么？”</li>
</ul>
</li>
</ul>
<h3 id="退步提示（Step-Back-Prompting）"><a href="#退步提示（Step-Back-Prompting）" class="headerlink" title="退步提示（Step-Back Prompting）"></a>退步提示（Step-Back Prompting）</h3><p>退步提示是由 Google DeepMind 团队提出的一种旨在提升大语言模型推理能力的提示工程技巧<a href="https://datawhalechina.github.io/all-in-rag/#fn-2">2</a>。当面对一个细节繁多或过于具体的问题时，模型直接作答（即便是使用思维链）也容易出错。退步提示通过引导模型“退后一步”来解决这个问题。</p>
<p>其核心流程分为两步：</p>
<p>（1）<strong>抽象化</strong>：首先，引导 LLM 从用户的原始具体问题中，生成一个更高层次、更概括的“退步问题”（Step-back Question）。这个退步问题旨在探寻原始问题背后的通用原理或核心概念。</p>
<p>（2）<strong>推理</strong>：接着，系统会先获取“退步问题”的答案（例如，一个物理定律、一段历史背景等），然后将这个通用原理作为上下文，再结合原始的具体问题，进行推理并生成最终答案。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_4_1.webp" alt="“退步提示”与“思维链”对比图"></p>
<p><strong>示例</strong>：</p>
<ul>
<li><strong>原始问题</strong>：“如果理想气体的温度增加2倍，体积增加8倍，其压力会如何变化？”</li>
<li><strong>退步问题</strong>：“这个问题背后的物理原理是什么？”</li>
<li><strong>推理过程</strong>：首先回答退步问题，得到“理想气体定律 PV&#x3D;nRT”。然后基于这个定律，代入具体数值进行计算，最终得出压力变为原来的1&#x2F;4。</li>
</ul>
<p>通过先检索或生成高层知识，再进行具体推理，退步提示能够帮助模型构建一个更坚实的逻辑基础，从而提高在复杂问答场景下的准确性。</p>
<h3 id="假设性文档嵌入-HyDE"><a href="#假设性文档嵌入-HyDE" class="headerlink" title="假设性文档嵌入 (HyDE)"></a>假设性文档嵌入 (HyDE)</h3><p>假设性文档嵌入（Hypothetical Document Embeddings, HyDE）是一种无需微调即可显著提升向量检索质量的查询改写技术，由 Luyu Gao 等人在其论文中首次提出<a href="https://datawhalechina.github.io/all-in-rag/#fn-3">3</a>。其核心是解决一个普遍存在于检索任务中的难题：用户的查询（Query）通常简短、关键词有限，而数据库中存储的文档则内容详实、上下文丰富，两者在语义向量空间中可能存在“鸿沟”，导致直接用查询向量进行搜索效果不佳。Zilliz 的一篇技术博客<a href="https://datawhalechina.github.io/all-in-rag/#fn-4">4</a>也对该技术进行了深入浅出的解读。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_4_2.webp" alt="HyDE"></p>
<p>HyDE 通过一种巧妙的方式来“绕过”这个问题：它不直接使用用户的原始查询，而是先利用一个生成式大语言模型（LLM）来生成一个“假设性”的、能够完美回答该查询的文档。然后，HyDE 将这个内容详实的假设性文档进行向量化，用其生成的向量去数据库中寻找与之最相似的真实文档。HyDE 的工作流程可以分为三个步骤：</p>
<p>（1）<strong>生成</strong>：当接收到用户查询时，首先调用一个生成式 LLM（例如，GPT-3.5）。提示该模型根据查询生成一个详细的、可能是理想答案的文档。这个文档不必完全符合事实，但它必须在语义上与一个好的答案高度相关。</p>
<p>（2）<strong>编码</strong>：将上一步生成的假设性文档输入到一个对比编码器（如 Contriever）中，将其转换为一个高维向量嵌入。这个向量在语义上代表了一个“理想答案”的位置。</p>
<p>（3）<strong>检索</strong>：使用这个假设性文档的向量，在向量数据库中执行相似性搜索，找出与这个“理想答案”最接近的真实文档。这些被检索出的文档将作为最终的上下文信息。</p>
<p>通过这种方式，HyDE 将困难的“查询到文档”的匹配问题，转化为了一个相对容易的“文档到文档”的匹配问题，从而提升检索的准确率。</p>
<h2 id="查询路由"><a href="#查询路由" class="headerlink" title="查询路由"></a>查询路由</h2><p><strong>查询路由（Query Routing）</strong> 是用于优化复杂 RAG 系统的一项关键技术。当系统接入了多个不同的数据源或具备多种处理能力时，就需要一个“智能调度中心”来分析用户的查询，并动态选择最合适的处理路径。其本质是替代硬编码规则，通过语义理解将查询分发至最匹配的数据源、处理组件或提示模板，从而提升系统的效率与答案的准确性。</p>
<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><p>查询路由的应用场景十分广泛。</p>
<ol>
<li><strong>数据源路由</strong>：这是最常见的场景。根据查询意图，将其路由到不同的知识库。例如：<ul>
<li>查询“最新的 iPhone 有什么功能？” -&gt; 路由到<strong>产品文档向量数据库</strong>。</li>
<li>查询“我上次订购了什么？” -&gt; 路由到<strong>用户历史SQL数据库</strong>（执行Text-to-SQL）。</li>
<li>查询“A公司和B公司的投资关系是怎样的？” -&gt; 路由到<strong>企业知识图谱数据库</strong>。</li>
</ul>
</li>
<li><strong>组件路由</strong>：根据问题的复杂性，将其分配给不同的处理组件，以平衡成本和效果。<ul>
<li>简单FAQ → 直接进行向量检索，速度快、成本低。</li>
<li>复杂操作或需要与外部API交互 → 调用 Agent 来执行任务。</li>
</ul>
</li>
<li><strong>提示模板路由</strong>：为不同类型的任务动态选择最优的提示词模板，以优化生成效果。<ul>
<li>数学问题 → 选用包含分步思考（Step-by-Step）逻辑的提示模板。</li>
<li>代码生成 → 选用专门为代码优化过的提示模板。</li>
</ul>
</li>
</ol>
<h3 id="实现方法"><a href="#实现方法" class="headerlink" title="实现方法"></a>实现方法</h3><h4 id="基于LLM的意图识别"><a href="#基于LLM的意图识别" class="headerlink" title="基于LLM的意图识别"></a>基于LLM的意图识别</h4><p>通过设计一个包含路由选项的提示词，让大模型直接对用户的查询进行分类，并输出一个代表路由选择的标签。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_4_3.webp" alt="逻辑路由"></p>
<ul>
<li><strong>实现流程</strong>：<ol>
<li>定义清晰的路由选项（例如，数据源名称、功能分类）。</li>
<li>LLM 分析查询并输出决策标签。</li>
<li>代码根据标签调用相应的检索器或工具。</li>
</ol>
</li>
</ul>
<p>该方法的核心在于构建一个“分类-分发”的流水线。这里以一个菜谱问答为例，系统需要根据用户提问的菜系（川菜、粤菜或其他）调用不同的专家模型。</p>
<h4 id="嵌入相似性路由"><a href="#嵌入相似性路由" class="headerlink" title="嵌入相似性路由"></a>嵌入相似性路由</h4><p>这种方法不依赖 LLM 进行分类，延迟更低。它通过计算用户查询与预设的“路由示例语句”之间的向量嵌入相似度来做出决策。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_4_4.webp" alt="语义路由"></p>
<p><strong>第一步：定义路由描述并向量化</strong></p>
<p>为每个路由创建一个详细的文本描述，并使用嵌入模型将其转换为向量，供后续相似度计算使用。</p>
<p><strong>第二步：定义目标链</strong></p>
<p>创建路由最终要分发到的目标处理链，并用一个字典route_map将路由名称和链对应起来。</p>
<p>第三步：定义路由函数</p>
<p>定义一个route函数，接收用户问题，计算与各路由描述的相似度，选择最相似的路由并调用相应的处理链。</p>
<p>第四步：组合并调用</p>
<p>最后，将route函数包装成一个RunnableLambda，形成一个完整的、可执行的路由链。</p>
<h1 id="检索进阶"><a href="#检索进阶" class="headerlink" title="检索进阶"></a>检索进阶</h1><p>在基础的 RAG 流程中，依赖向量相似度从知识库中检索信息。不过，这种方法存在一些固有的局限性，例如最相关的文档不总是在检索结果的顶端，以及语义理解的偏差等。为了构建更强大、更精准的生产级 RAG 应用，需要引入更高级的检索技术。</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_5_1.webp" alt="retrieval"></p>
<h2 id="重排序-Re-ranking"><a href="#重排序-Re-ranking" class="headerlink" title="重排序(Re-ranking)"></a>重排序(Re-ranking)</h2><h3 id="RRF"><a href="#RRF" class="headerlink" title="RRF"></a>RRF</h3><p>一种简单而有效的零样本重排方法，不依赖于任何模型训练，而是纯粹基于文档在多个检索器（例如，一个稀疏检索器和一个密集检索器）结果列表中的排名俩计算最终分数。</p>
<p>如果文档在多个检索器结果中都排名靠前，那它可能更重要。RRF通过计算排名的倒数（$\frac1{rank}$）来为文档打分，有效融合了不同检索策略的优势。但如果只考虑排名信息，会忽略原始的相似度分数。</p>
<h3 id="RankLLM-LLM-based-Reranker"><a href="#RankLLM-LLM-based-Reranker" class="headerlink" title="RankLLM &#x2F; LLM-based Reranker"></a>RankLLM &#x2F; LLM-based Reranker</h3><p>ankLLM 代表了一类直接利用大型语言模型本身来进行重排的方法<a href="https://datawhalechina.github.io/all-in-rag/#fn-1">1</a>。其基本逻辑非常直观：既然 LLM 最终要负责根据上下文来生成答案，那么为什么不直接让它来判断哪些上下文最相关呢？</p>
<p>这种方法通过一个精心设计的提示词来实现。该提示词会包含用户的查询和一系列候选文档（通常是文档的摘要或关键部分），然后要求 LLM 以特定格式（如 JSON）输出一个排序后的文档列表，并给出每个文档的相关性分数。</p>
<p>提示词示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">以下是一个文档列表，每个文档都有一个编号和摘要。同时提供一个问题。请根据问题，按相关性顺序列出您认为需要查阅的文档编号，并给出相关性分数（1-10分）。请不要包含与问题无关的文档。</span><br><span class="line"></span><br><span class="line">示例格式:</span><br><span class="line">文档 1: &lt;文档1的摘要&gt;</span><br><span class="line">文档 2: &lt;文档2的摘要&gt;</span><br><span class="line">...</span><br><span class="line">文档 10: &lt;文档10的摘要&gt;</span><br><span class="line"></span><br><span class="line">问题: &lt;用户的问题&gt;</span><br><span class="line"></span><br><span class="line">回答:</span><br><span class="line">Doc: 9, Relevance: 7</span><br><span class="line">Doc: 3, Relevance: 4</span><br><span class="line">Doc: 7, Relevance: 3</span><br></pre></td></tr></table></figure>

<h3 id="Cross-Encoder-重排"><a href="#Cross-Encoder-重排" class="headerlink" title="Cross-Encoder 重排"></a>Cross-Encoder 重排</h3><p>交叉编码器能提供出色的重排精度。工作原理是将查询（Query）和每个候选文档（Document）<strong>拼接</strong>成一个单一的输入（例如，<code>[CLS] query [SEP] document [SEP]</code>），然后将这个整体输入到一个预训练的 Transformer 模型（如 BERT）中，模型最终会输出一个单一的分数（通常在 0 到 1 之间），这个分数直接代表了文档与查询的<strong>相关性</strong>。</p>
<blockquote>
<p>[!NOTE]</p>
<p>交叉编码器会将两个输入句子拼接后一起编码&amp;]，直接输出一个相关性或分类分数，而不是分别生成独立的向量再计算相似度。这种方式能捕捉句子间的细粒度交互，因此在分类、重排序等高精度任务中表现更好，但推理速度较慢、计算成本高。</p>
<p><strong>[SEP]</strong> 是在 BERT 这类基于 Transformer 架构的模型中，用于分隔不同文本片段（如查询和文档）的特殊标记。</p>
</blockquote>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_5_3.svg" alt="cross-encoder"></p>
<p>上图清晰地展示了 Cross-Encoder 的工作流程：</p>
<ol>
<li><strong>初步检索</strong>：搜索引擎首先从知识库中召回一个初始的文档列表（例如，前 50 篇）。</li>
<li><strong>逐一评分</strong>：对于列表中的<strong>每一篇</strong>文档，系统都将其与原始查询<strong>配对</strong>，然后发送给 Cross-Encoder 模型。</li>
<li><strong>独立推理</strong>：模型对每个“查询-文档”对进行一次完整的、独立的推理计算，得出一个精确的相关性分数。</li>
<li><strong>返回重排结果</strong>：系统根据这些新的分数对文档列表进行重新排序，并将最终结果返回给用户。</li>
</ol>
<p>这个流程凸显了其高精度的来源（同时分析查询和文档），也解释了其高延迟的原因（需要N次独立的模型推理）。</p>
<p>常见的 Cross-Encoder 模型包括 <code>ms-marco-MiniLM-L-12-v2</code>、<code>ms-marco-TinyBERT-L-2-v2</code> 等。</p>
<h3 id="ColBERT-重排"><a href="#ColBERT-重排" class="headerlink" title="ColBERT 重排"></a>ColBERT 重排</h3><p>ColBERT（Contextualized Late Interaction over BERT）是一种创新的重排模型，它在 Cross-Encoder 的高精度和双编码器（Bi-Encoder）的高效率之间取得了平衡<a href="https://datawhalechina.github.io/all-in-rag/#fn-3">3</a>。采用了一种“<strong>后期交互</strong>”机制。</p>
<p>其工作流程如下：</p>
<ol>
<li><strong>独立编码</strong>：ColBERT 分别为查询（Query）和文档（Document）中的每个 Token 生成上下文相关的嵌入向量。这一步是独立完成的，可以预先计算并存储文档的向量，从而加快查询速度。</li>
<li><strong>后期交互</strong>：在查询时，模型会计算查询中每个 Token 的向量与文档中每个 Token 向量之间的最大相似度（MaxSim）。</li>
<li><strong>分数聚合</strong>：最后，将查询中所有 Token 得到的最大相似度分数相加，得到最终的相关性总分。</li>
</ol>
<p>通过这种方式，ColBERT 避免了将查询和文档拼接在一起进行昂贵的联合编码，同时又比单纯比较单个 <code>[CLS]</code> 向量的双编码器模型捕捉了更细粒度的词汇级交互信息。</p>
<h3 id="重排方法对比"><a href="#重排方法对比" class="headerlink" title="重排方法对比"></a>重排方法对比</h3><p>为了更直观地理解不同重排方法的特点和适用场景，下表对讨论过的几种主流方法进行了总结：</p>
<table>
<thead>
<tr>
<th>特性</th>
<th>RRF</th>
<th>RankLLM</th>
<th>Cross-Encoder</th>
<th>ColBERT</th>
</tr>
</thead>
<tbody><tr>
<td><strong>核心机制</strong></td>
<td>融合多个排名</td>
<td>LLM 推理，生成排序列表</td>
<td>联合编码查询与文档，计算单一相关分</td>
<td>独立编码，后期交互</td>
</tr>
<tr>
<td><strong>计算成本</strong></td>
<td>低（简单数学计算）</td>
<td>中 (API 费用与延迟)</td>
<td>高（N次模型推理）</td>
<td>中（向量点积计算）</td>
</tr>
<tr>
<td><strong>交互粒度</strong></td>
<td>无（仅排名）</td>
<td>概念&#x2F;语义级</td>
<td>句子级（Query-Doc Pair）</td>
<td>Token 级</td>
</tr>
<tr>
<td><strong>适用场景</strong></td>
<td>多路召回结果融合</td>
<td>高价值语义理解场景</td>
<td>Top-K 精排</td>
<td>Top-K 重排</td>
</tr>
</tbody></table>
<h2 id="压缩-Compression"><a href="#压缩-Compression" class="headerlink" title="压缩(Compression)"></a>压缩(Compression)</h2><p>“压缩”技术旨在解决一个常见问题：初步检索到的文档块（Chunks）虽然整体上与查询相关，但可能包含大量无关的“噪音”文本。将这些未经处理的、冗长的上下文直接提供给 LLM，不仅会增加 API 调用的成本和延迟，还可能因为信息过载而降低最终生成答案的质量。</p>
<p>压缩的目标就是对检索到的内容进行“压缩”和“提炼”，只保留与用户查询最直接相关的信息。这可以通过两种主要方式实现：</p>
<ol>
<li><strong>内容提取</strong>：从文档中只抽出与查询相关的句子或段落。</li>
<li><strong>文档过滤</strong>：完全丢弃那些虽然被初步召回，但经过更精细判断后认为不相关的整个文档。</li>
</ol>
<h3 id="LangChain-的-ContextualCompressionRetriever"><a href="#LangChain-的-ContextualCompressionRetriever" class="headerlink" title="LangChain 的 ContextualCompressionRetriever"></a>LangChain 的 ContextualCompressionRetriever</h3><p>LangChain 提供了一个强大的组件 <code>ContextualCompressionRetriever</code> 来实现上下文压缩<a href="https://datawhalechina.github.io/all-in-rag/#fn-4">4</a>。它像一个包装器，包裹在基础的检索器（如 <code>FAISS.as_retriever()</code>）之上。当基础检索器返回文档后，<code>ContextualCompressionRetriever</code> 会使用一个指定的 <code>DocumentCompressor</code> 对这些文档进行处理，然后再返回给调用者。</p>
<p>LangChain 内置了多种 <code>DocumentCompressor</code>：</p>
<ul>
<li><code>LLMChainExtractor</code>: 这是最直接的压缩方式。它会遍历每个文档，并利用一个 LLM Chain 来判断并提取出其中与查询相关的部分。这是一种“内容提取”。</li>
<li><code>LLMChainFilter</code>: 这种压缩器同样使用 LLM，但它做的是“文档过滤”。它会判断整个文档是否与查询相关，如果相关，则保留整个文档；如果不相关，则直接丢弃。</li>
<li><code>EmbeddingsFilter</code>: 这是一种更快速、成本更低的过滤方法。它会计算查询和每个文档的嵌入向量之间的相似度，只保留那些相似度超过预设阈值的文档。</li>
</ul>
<h3 id="自定义重排器与压缩管道"><a href="#自定义重排器与压缩管道" class="headerlink" title="自定义重排器与压缩管道"></a>自定义重排器与压缩管道</h3><p>在前面我们就提到根据实际应用，需要自己进行一些功能的实现。这里以 ColBERT 为例，展示如何集成未被官方支持的功能。</p>
<p>整个探索和实现过程如下：</p>
<ol>
<li><strong>从官方文档出发</strong>：首先，通过 LangChain 官方文档，了解到可以通过 <code>DocumentCompressorPipeline</code> 来组合多个压缩器和文档转换器。</li>
<li><strong>需求缺口</strong>：希望使用 ColBERT 模型进行重排，但发现 LangChain 并没有内置的 <code>ColBERT</code> 重排器。</li>
<li><strong>分析示例与源码</strong>：回头分析 <code>ContextualCompressionRetriever</code> 的用法和源码。我们发现，其处理逻辑分为两步：首先使用 <code>base_retriever</code> 获取原始文档，然后将这些文档交给 <code>base_compressor</code> 进行压缩或重排。这说明，实现自定义后处理（如重排）功能的关键在于 <code>base_compressor</code>。</li>
<li><strong>定位核心基类</strong>：通过f12查看源码，确定 <code>base_compressor</code> 参数接收的是 <code>BaseDocumentCompressor</code> 类型的对象。这就是实现自定义功能的核心切入点。</li>
<li><strong>参考与实现</strong>：最后，参考 LangChain 中其他重排器的实现方式，通过继承 <code>BaseDocumentCompressor</code> 基类并实现其关键方法，创建自己的 <code>ColBERTReranker</code> 类。</li>
</ol>
<blockquote>
<p>PS：如果代码基础薄弱，想借助大模型帮你完成 <code>ColBERTReranker</code> ，需要提供给大模型的关键信息：<code>BaseDocumentCompressor</code> 的源码和 <code>ContextualCompressionRetriever</code> 的源码及其使用示例、你的明确目标（实现 ColBERT 重排逻辑）、以及 LangChain 中其他重排器的代码作为参考。信息越充分，模型生成的代码越准确。</p>
</blockquote>
<h3 id="LlamaIndex-中的检索压缩"><a href="#LlamaIndex-中的检索压缩" class="headerlink" title="LlamaIndex 中的检索压缩"></a>LlamaIndex 中的检索压缩</h3><p>LlamaIndex 同样提供了封装好的压缩功能，其代表是 <code>SentenceEmbeddingOptimizer</code><a href="https://datawhalechina.github.io/all-in-rag/#fn-5">5</a>。它也是一个后处理器（Node Postprocessor），工作在检索之后。</p>
<p>它的工作原理是，对于每个检索到的文档，将其分解成句子。然后计算每个句子与用户查询的嵌入相似度，最后只保留那些相似度最高的句子，从而“优化”文档，去除无关信息。</p>
<h2 id="校正-Correcting"><a href="#校正-Correcting" class="headerlink" title="校正(Correcting)"></a>校正(Correcting)</h2><p>传统的 RAG 流程有一个隐含的假设：检索到的文档总是与问题相关且包含正确答案。然而在现实世界中，检索系统可能会失败，返回不相关、过时或甚至完全错误的文档。如果将这些“有毒”的上下文直接喂给 LLM，就可能导致幻觉（Hallucination）或产生错误的回答。</p>
<p><strong>校正检索（Corrective-RAG, C-RAG）</strong> 正是为解决这一问题而提出的一种策略<a href="https://datawhalechina.github.io/all-in-rag/#fn-6">6</a>。思路是引入一个“自我反思”或“自我修正”的循环，在生成答案之前，对检索到的文档质量进行评估，并根据评估结果采取不同的行动。</p>
<p>C-RAG 的工作流程可以概括为 <strong>“检索-评估-行动”</strong> 三个阶段：</p>
<p><img src="https://datawhalechina.github.io/all-in-rag/chapter4/images/4_5_4.webp" alt="C-RAG"></p>
<ol>
<li><strong>检索 (Retrieve)</strong> ：与标准 RAG 一样，首先根据用户查询从知识库中检索一组文档。</li>
<li><strong>评估 (Assess)</strong> ：这是 C-RAG 的关键步骤。如图所示，一个“检索评估器 (Retrieval Evaluator)”会判断每个文档与查询的相关性，并给出“正确 (Correct)”、“不正确 (Incorrect)”或“模糊 (Ambiguous)”的标签。</li>
<li><strong>行动 (Act)</strong> ：根据评估结果，系统会进入不同的知识修正与获取流程：<ul>
<li><strong>如果评估为“正确”</strong>：系统会进入“知识精炼 (Knowledge Refinement)”环节。如图，它会将原始文档分解成更小的知识片段 (strips)，过滤掉无关部分，然后重新组合成更精准、更聚焦的上下文，再送给大模型生成答案。</li>
<li><strong>如果评估为“不正确”</strong>：系统认为内部知识库无法回答问题，此时会触发“知识搜索 (Knowledge Searching)”。它会先对原始查询进行“查询重写 (Query Rewriting)”，生成一个更适合搜索引擎的查询，然后进行 Web 搜索，用外部信息来回答问题。</li>
<li><strong>如果评估为“模糊”</strong>：同样会触发“知识搜索”，但通常会直接使用原始查询进行 Web 搜索，以获取额外信息来辅助生成答案。</li>
</ul>
</li>
</ol>
<p>通过这种方式，C-RAG 极大地增强了 RAG 系统的鲁棒性。不再盲目信任检索结果，而是增加了一个“事实核查”层，能够在检索失败时主动寻求外部帮助，从而有效减少幻觉，提升答案的准确性和可靠性。</p>
<p>在 LangChain 的 <code>langgraph</code> 库中，可以利用其图结构来灵活地构建这种带有条件判断和循环的复杂 RAG 流程。</p>
]]></content>
      <categories>
        <category>RAG</category>
        <category>检索优化</category>
      </categories>
      <tags>
        <tag>RAG</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG-系统评估</title>
    <url>/2026/04/16/RAG-%E7%B3%BB%E7%BB%9F%E8%AF%84%E4%BC%B0/</url>
    <content><![CDATA[<h1 id="RAG评估"><a href="#RAG评估" class="headerlink" title="RAG评估"></a>RAG评估</h1><p>构建好一个RAG系统后，应该如何评判这个系统的好坏？如何快速定位问题根源？</p>
<h2 id="典型失败模式"><a href="#典型失败模式" class="headerlink" title="典型失败模式"></a>典型失败模式</h2><ul>
<li>检索器问题：检索结果与需求不相关（检索了完全不相关的文档或数据）、遗漏关键信息（Top-K没有包揽核心论点）、刻舟求剑（返回了历史过期版本的信息）</li>
<li>生成器问题：忽略正确的检索上下文信息、脱离上下文，产生知识幻觉、答非所问（回答形式或语气不符合要求）</li>
<li>协同问题：检索正确但被LLM歪曲理解、检索到错误信息LLm无法调和</li>
</ul>
<h2 id="评估维度"><a href="#评估维度" class="headerlink" title="评估维度"></a>评估维度</h2><p>该架构包含以下三个维度，并在 <strong>TruLens</strong> 等工具中有深入的应用：</p>
<p>（1）<strong>上下文相关性 (Context Relevance)</strong></p>
<p>即检索层评估（Retrieval Layer Evaluation），核心指标有召回率、精度、平均倒数排名（MRR）、归一化折损累积增益（NDCG）、Hit Rate@K等，评估重点为检索器能否准确找到相关文档并正确排序。</p>
<ul>
<li><strong>评估目标：</strong> 检索器（Retriever）的性能。</li>
<li><strong>核心问题：</strong> 检索到的上下文内容，是否与用户的查询（Query）高度相关？</li>
<li><strong>重要性：</strong> 检索是RAG应用在响应用户查询时的第一步。如果检索回来的上下文充满了噪声或无关信息，那么无论后续的生成模型多么强大，都没法做出正确答案。</li>
</ul>
<p>（2）<strong>忠实度 &#x2F; 可信度 (Faithfulness &#x2F; Groundedness)</strong></p>
<p>即生成层评估，核心指标有忠实度、答案相关性、完整性、流畅性等，评估重点为生成器能否基于检索内容生成正确、相关的答案。</p>
<ul>
<li><strong>评估目标：</strong> 生成器的可靠性。</li>
<li><strong>核心问题：</strong> 生成的答案是否完全基于所提供的上下文信息？</li>
<li><strong>重要性：</strong> 这个维度主要在于量化LLM的“幻觉”程度。一个高忠实度的回答意味着模型严格遵守了上下文，没有捏造或歪曲事实。如果忠实度得分低，说明LLM在回答时“自由发挥”过度，引入了外部知识或不实信息。</li>
</ul>
<p>（3）<strong>答案相关性 (Answer Relevance)</strong></p>
<p>即系统层评估，核心指标有整体准确性、用户满意度、业务指标等，评估重点为端到端的系统性能和业务价值。</p>
<ul>
<li><strong>评估目标：</strong> 系统的端到端（End-to-End）表现。</li>
<li><strong>核心问题：</strong> 最终生成的答案是否直接、完整且有效地回答了用户的原始问题？</li>
<li><strong>重要性：</strong> 这是用户最直观的感受。一个答案可能完全基于上下文（高忠实度），但如果它答非所问，或者只回答了问题的一部分，那么这个答案的相关性就很低。例如，当用户问“法国在哪里，首都是哪里？”，如果答案只是“法国在西欧”，那么虽然忠实度高，但答案相关性很低。</li>
</ul>
<blockquote>
<p>你可能觉得忠实度和答案相关性很相似，但它们的侧重点是不同的。忠实度更关注模型是否严格遵循了上下文，而答案相关性则更关注模型是否直接、完整且有效地回答了问题。</p>
</blockquote>
<p>通过对这三个维度进行评估，可以对RAG系统的表现有一个全面而细致的了解，并能准确定位问题所在：是检索出了问题，还是生成环节有待改进。</p>
<h2 id="评估工作流"><a href="#评估工作流" class="headerlink" title="评估工作流"></a>评估工作流</h2><p>虽然上面把评估分成了三个部分，但实际上可以把评估过程拆解为两个主要环节：<strong>检索评估</strong>和<strong>响应评估</strong>。</p>
<h3 id="检索评估"><a href="#检索评估" class="headerlink" title="检索评估"></a>检索评估</h3><p>检索评估聚焦于RAG三元组中的 **上下文相关性 (Context Relevance)*<em>，本质上是一次*<em>白盒测试</em></em> <a href="https://datawhalechina.github.io/all-in-rag/#fn-2">2</a>。此阶段的评估需要一个标注数据集，其中包含一系列查询以及每个查询对应的真实相关文档。</p>
<p>这项评估借鉴了信息检索领域的多个经典指标：</p>
<ul>
<li><p><strong>上下文精确率 (Context Precision):</strong> 衡量检索结果的准确性。计算在检索到的前 <strong>k</strong> 个文档中相关文档所占的比例，其中 <strong>k</strong> 是一个预设的数字（例如，k&#x3D;3或k&#x3D;5），代表评估的范围。高精确率意味着检索结果的噪声较少。<br>$$<br>Precision@k&#x3D;\frac{检索到的k个结果中的相关文档数}k<br>$$</p>
</li>
<li><p><strong>上下文召回率 (Context Recall):</strong> 衡量检索结果的完整性。计算在检索到的前 <strong>k</strong> 个文档中，找到的相关文档占所有真实相关文档总数的比例。高召回率意味着系统能够成功找回大部分关键信息。<br>$$<br>Recall@k&#x3D;\frac{检索到的k个结果中的相关文档数}{数据集中所有相关的文档总数}<br>$$<br><strong>F1分数 (F1-Score):</strong> F1分数是精确率和召回率的调和平均数，它同时兼顾了这两个指标，在它们之间寻求平衡。当精确率和召回率都高时，F1分数也高。<br>$$<br>F1&#x3D;2\frac{Precision×Recall}{Precision+Recall}<br>$$</p>
</li>
<li><p><strong>平均倒数排名 (MRR - Mean Reciprocal Rank):</strong> 评估系统将第一个相关文档排在靠前位置的能力。对于一个查询，倒数排名是第一个相关文档排名的倒数。MRR是所有查询的倒数排名的平均值。该指标适用于用户通常只关心第一个正确答案的场景。<br>$$<br>MRR&#x3D;\frac1{∣Q∣}∑_{q&#x3D;1}^{∣Q∣}\frac1{rankq}<br>$$<br>其中 <code>|Q|</code> 是查询总数，<code>rank_q</code> 是第 <code>q</code> 个查询的第一个相关文档的排名。</p>
</li>
<li><p><strong>平均准确率均值 (MAP - Mean Average Precision):</strong> MAP是一个综合性指标，同时评估了检索结果的精确率和相关文档的排名。它先计算每个查询的平均精确率（AP），然后对所有查询的AP取平均值。AP本身是基于每个相关文档被检索到时的精确率计算的。<br>$$<br>MAP&#x3D;\frac1{∣Q∣}∑_{q&#x3D;1}^{∣Q∣}AP(q)<br>$$<br>其中 <code>|Q|</code> 是查询总数，<code>AP(q)</code> 是第 <code>q</code> 个查询的平均精确率（Average Precision）。</p>
</li>
</ul>
<p>要计算上述所有指标，前提是拥有一个高质量的标注数据集，其中包含了查询和每个查询对应的“真实”相关文档。</p>
<h3 id="响应评估"><a href="#响应评估" class="headerlink" title="响应评估"></a>响应评估</h3><p>响应评估覆盖了RAG三元组中的 <strong>忠实度</strong> 和 <strong>答案相关性</strong>。此环节通常采用 <strong>端到端</strong> 的评估范式，因为它直接衡量用户感知的最终输出质量。无论采用何种评估方法，都主要围绕以下两个核心维度展开。</p>
<h4 id="评估维度-1"><a href="#评估维度-1" class="headerlink" title="评估维度"></a>评估维度</h4><p>（1）<strong>忠实度 &#x2F; 可信度</strong>：衡量生成的答案在多大程度上可以由给定的上下文所证实。一个完全忠实的答案，其所有内容都必须能在上下文中找到依据，以此避免模型产生“幻觉”。</p>
<p>（2）<strong>答案相关性</strong>：衡量生成的答案与用户原始查询的对齐程度。一个高相关性的答案必须是直接的、切题的，并且不包含与问题无关的冗余信息。</p>
<h4 id="主要评估方法"><a href="#主要评估方法" class="headerlink" title="主要评估方法"></a>主要评估方法</h4><p>针对上述维度，目前主要有两类评估方法：</p>
<p>（1）<strong>基于大语言模型的评估</strong></p>
<p>这是一种强大的评估方法，能够提供更深度的语义评估，正逐渐成为主流选择。利用一个高性能、中立的llm作为“评估者”，对上述维度进行深度的语义理解和打分。</p>
<ul>
<li><strong>忠实度评估：</strong> 首先，将生成的答案分解为一系列独立的声明或断言（Claims）。然后，对于每一个断言，在提供的上下文中进行验证，判断其真伪。最终的忠实度分数是所有被上下文证实的断言所占的比例。</li>
<li><strong>答案相关性评估：</strong> 评估者需要同时分析用户查询和生成的答案。评分时会惩罚那些答非所问、信息不完整或包含过多无关细节的答案。</li>
</ul>
<p>（2）<strong>基于词汇重叠的经典指标</strong></p>
<p>这类指标需要在数据集中包含一个或多个“标准答案”。它们通过计算生成答案与标准答案之间 n-gram（连续的n个词）的重叠程度来评估质量。</p>
<ul>
<li><p><strong>ROUGE (Recall-Oriented Understudy for Gisting Evaluation):</strong> ROUGE关注的重点是 <strong>召回率</strong>，即标准答案中的词语有多少被生成答案所覆盖，因此常用于评估内容的 <strong>完整性</strong>。其常用变体包括计算n-gram的 <code>ROUGE-N</code> 和计算最长公共子序列的 <code>ROUGE-L</code>。<br>$$<br>ROUGE-N&#x3D;\frac{匹配的 n-gram 数量}{参考答案中 n-gram 的总数}<br>$$</p>
</li>
<li><p><strong>BLEU (Bilingual Evalu ation Understudy):</strong> BLEU侧重于评估 <strong>精确率</strong>，衡量生成的答案中有多少词是有效的（即在标准答案中出现过）。它还引入了长度惩罚机制，避免模型生成过短的句子，因此更适合评估答案的 <strong>流畅度和准确性</strong>。<br>$$<br>BLEU&#x3D;BP×\exp⁡(∑_{n&#x3D;1}^Nw_n\log⁡p_n)<br>$$<br>其中，<code>BP</code> 是长度惩罚因子，<code>p_n</code> 是修正后的n-gram精确率。</p>
</li>
<li><p><strong>METEOR (Metric for Evaluation of Translation with Explicit ORdering):</strong> 作为BLEU的改进版，METEOR同时考量 <strong>精确率和召回率</strong> 的调和平均，并通过词干和同义词匹配（如将’boat’和’ship’视为相关）来更好地捕捉语义相似性。其评估结果通常被认为与人类判断的相关性更高。<br>$$<br>Fmean&#x3D;\frac{P×R}{αP+(1−α)R}<br>$$</p>
<p>$$<br>METEOR&#x3D;F_{mean}×(1−Penalty)<br>$$</p>
<p>其中 <code>P</code> 是精确率，<code>R</code> 是召回率，<code>Penalty</code> 是基于语序的惩罚项。</p>
</li>
</ul>
<blockquote>
<p>为了更直观地理解三者的区别，来看一个简单的例子。</p>
<p><strong>假设：</strong></p>
<ul>
<li><strong>参考答案：</strong> <code>狗 在 床 上面</code> (共5个词)</li>
<li><strong>生成答案：</strong> <code>狗 在 床 上</code> (共4个词)</li>
</ul>
<p><strong>评估分析：</strong></p>
<ul>
<li><strong>ROUGE (召回率导向):</strong> 从召回率的角度出发：“参考答案里的5个词，生成答案覆盖了多少？”——覆盖了4个。因此，它的召回率很高（ROUGE-1 为 4&#x2F;5），得分会不错。ROUGE更关心“说全了没”。</li>
<li><strong>BLEU (精确率导向):</strong> 从精确率的角度进行评判：“生成答案里的4个词，有多少是有效的（在参考答案里）？”——全部有效，精确率很高。但它会发现生成答案比参考答案短，于是通过 <strong>长度惩罚(Brevity Penalty)</strong> 进行扣分。BLEU更关心“说对了没，以及长度是否合适”。</li>
<li><strong>METEOR (综合平衡):</strong> 同时计算精确率和召回率，并取一个调和平均。在这个例子里，词序是完全正确的，惩罚项为0。METEOR会在“说全”和“说对”之间找到一个最佳平衡点。</li>
</ul>
</blockquote>
<h4 id="方法对比和总结"><a href="#方法对比和总结" class="headerlink" title="方法对比和总结"></a>方法对比和总结</h4><p>基于LLM的评估更注重<strong>语义和逻辑</strong>，评估质量高，但成本也更高且存在评估者偏见。基于词汇重叠的指标<strong>客观、计算快、成本低</strong>，但无法理解语义，可能误判同义词或释义。在实践中，可以将两者结合，使用经典指标进行快速、大规模的初步筛选，再利用LLM进行更精细的评估。</p>
<h2 id="常用评估工具"><a href="#常用评估工具" class="headerlink" title="常用评估工具"></a>常用评估工具</h2><h3 id="LlamaIndex-Evaluation"><a href="#LlamaIndex-Evaluation" class="headerlink" title="LlamaIndex Evaluation"></a>LlamaIndex Evaluation</h3><p><code>LlamaIndex Evaluation</code> 是<strong>深度集成于LlamaIndex框架内的评估模块</strong>，专为使用该框架构建的RAG应用提供无缝的评估能力。作为RAG开发框架的原生组件，其核心定位是<strong>为开发者在开发、调试和迭代周期中提供快速、灵活的嵌入式评估解决方案</strong>。它强调与开发流程的紧密结合，允许开发者在构建过程中即时验证和对比不同RAG策略的性能<a href="https://datawhalechina.github.io/all-in-rag/#fn-1">1</a>。</p>
<blockquote>
<p><strong>适用场景</strong>：对于深度使用 <code>LlamaIndex</code> 框架构建RAG应用的开发者而言，其内置评估模块是无缝集成的首选，提供了一站式的开发与评估体验。</p>
</blockquote>
<h4 id="核心理念与工作流"><a href="#核心理念与工作流" class="headerlink" title="核心理念与工作流"></a>核心理念与工作流</h4><p><code>LlamaIndex</code> 的评估理念是利用LLM作为“裁判”，以自动化的方式对RAG系统的各个环节进行打分。这种方法在很多场景下无需预先准备“标准答案”，大大降低了评估门槛。其典型工作流如下：</p>
<ol>
<li><strong>准备评估数据集</strong>：通过 <code>DatasetGenerator</code> 从文档中自动生成问题-答案对（<code>QueryResponseDataset</code>），或加载一个已有的数据集。为了效率，通常会将生成的数据集保存到本地，避免重复生成。</li>
<li><strong>构建查询引擎</strong>：搭建一个或多个需要被评估的RAG查询引擎（<code>QueryEngine</code>）。这是进行对比实验的基础。</li>
<li><strong>初始化评估器</strong>：根据评估维度，选择并初始化一个或多个评估器，如 <code>FaithfulnessEvaluator</code>（忠实度）和 <code>RelevancyEvaluator</code>（相关性）。</li>
<li><strong>执行批量评估</strong>：使用 <code>BatchEvalRunner</code> 来管理整个评估过程。它能够高效地（可并行）将查询引擎应用于数据集中的所有问题，并调用所有评估器进行打分。</li>
<li><strong>分析结果</strong>：从评估运行器返回的结果中，计算各项指标的平均分，从而量化地对比不同RAG策略的优劣。</li>
</ol>
<h3 id="RAGAS"><a href="#RAGAS" class="headerlink" title="RAGAS"></a>RAGAS</h3><p>RAGAS（RAG Assessment）是一个<strong>独立的、专注于RAG的开源评估框架</strong>。提供了一套全面的指标来量化RAG管道的检索和生成两大核心环节的性能。其最显著的特色是支持<strong>无参考评估</strong>，即在许多场景下无需人工标注的“标准答案”即可进行评估，极大地降低了评估成本。现对RAG管道的持续监控和改进。如果你需要一个轻量级、与具体RAG实现解耦、能够快速对核心指标进行量化评估的工具时，<code>RAGAS</code> 是一个理想的选择。</p>
<h4 id="设计理念"><a href="#设计理念" class="headerlink" title="设计理念"></a>设计理念</h4><p><code>RAGAS</code> 的核心思想是通过分析问题（<code>question</code>）、生成的答案（<code>answer</code>）和检索到的上下文（<code>context</code>）三者之间的关系，来综合评估RAG系统的性能。它将复杂的评估问题分解为几个简单、可量化的维度。</p>
<h4 id="工作流程与核心指标"><a href="#工作流程与核心指标" class="headerlink" title="工作流程与核心指标"></a>工作流程与核心指标</h4><p>RAGAS的评估流程非常简洁，通常遵循以下步骤：</p>
<p>（1）<strong>准备数据集</strong>：根据官方文档，一个标准的评估数据集应包含 <code>question</code>（问题）、<code>answer</code>（RAG系统生成的答案）、<code>contexts</code>（检索到的上下文）以及 <code>ground_truth</code>（标准参考答案）这四列。不过，<code>ground_truth</code> 对于计算 <code>context_recall</code> 等指标是必需的，但对于 <code>faithfulness</code> 等指标则是可选的。</p>
<p>（2）<strong>运行评估</strong>：调用 <code>ragas.evaluate()</code> 函数，传入准备好的数据集和需要评估的指标列表。</p>
<p>（3）<strong>分析结果</strong>：获取一个包含各项指标量化分数的评估报告。</p>
<p>其核心评估指标包括：</p>
<ul>
<li><code>faithfulness</code>: 衡量生成的答案中有多少比例的信息是可以由检索到的上下文所支持的。</li>
<li><code>context_recall</code>: 衡量检索到的上下文与标准答案（<code>ground_truth</code>）的对齐程度，即标准答案中的信息是否被上下文完全“召回”。</li>
<li><code>context_precision</code>: 衡量检索到的上下文中，信噪比如何，即有多少是真正与回答问题相关的。</li>
<li><code>answer_relevancy</code>: 评估答案与问题的相关程度。此指标不评估事实准确性，只关注答案是否切题。</li>
</ul>
<h3 id="Phoenix-Arize-Phoenix"><a href="#Phoenix-Arize-Phoenix" class="headerlink" title="Phoenix (Arize Phoenix)"></a>Phoenix (Arize Phoenix)</h3><p>Phoenix (现由Arize维护) 是一个<strong>开源的LLM可观测性与评估平台</strong>。在RAG评估生态中，它主要扮演<strong>生产环境中的可视化分析与故障诊断引擎</strong>的角色。它通过捕获LLM应用的轨迹（Traces），提供强大的可视化、切片和聚类分析能力，帮助开发者理解线上真实数据的表现。Phoenix 的核心价值在于<strong>从海量生产数据中发现问题、监控性能漂移并进行深度诊断</strong>，是连接线下评估与线上运维的关键桥梁。它不仅提供评估指标，更强调对LLM应用进行追踪（Tracing）和可视化分析，从而快速定位问题<a href="https://datawhalechina.github.io/all-in-rag/#fn-3">3</a>。</p>
<h4 id="核心理念"><a href="#核心理念" class="headerlink" title="核心理念"></a>核心理念</h4><p><code>Phoenix</code> 的核心是“AI可观测性”，它通过追踪RAG系统内部的每一步调用（如检索、生成等），将整个流程可视化。这使得开发者可以直观地看到每个环节的输入、输出和耗时，并在此基础上进行深入的评估和调试。</p>
<h4 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h4><p><code>Phoenix</code> 的工作流程是先通过基于开放标准 <strong>OpenTelemetry</strong> 的<strong>代码插桩（<code>Instrumentation</code>）</strong>，在 RAG 应用中集成追踪功能，自动捕获 LLM 调用、函数执行等事件；随后在应用运行过程中持续生成<strong>追踪数据（<code>Traces</code>）</strong>，记录完整的执行链路；接着在本地启动 <code>Phoenix</code> 的 Web 界面，加载并可视化这些追踪数据；最后在 UI 中对失败案例或表现不佳的查询进行筛选、钻取，并借助内置的<strong>评估器（<code>Evals</code>）</strong>完成深入的评估与调试。</p>
<p>特色功能：</p>
<ul>
<li><strong>可视化追踪</strong>: 将RAG的执行流程、数据和评估结果进行可视化展示，极大地方便了问题定位。</li>
<li><strong>根本原因分析</strong>: 通过可视化的界面，可以轻松地对表现不佳的查询进行切片和钻取。</li>
<li><strong>安全护栏 (<code>Guardrails</code>)</strong>: 允许为应用添加保护层，防止恶意或错误的输入输出，保障生产环境安全。</li>
<li><strong>数据探索与标注</strong>: 提供数据探索、清洗和标注工具，帮助开发者利用生产数据反哺模型和系统优化。</li>
<li><strong>与Arize平台集成</strong>: <code>Phoenix</code> 可以与Arize的商业平台无缝对接，实现生产环境中对RAG系统的持续监控。</li>
</ul>
<h3 id="对比建议"><a href="#对比建议" class="headerlink" title="对比建议"></a>对比建议</h3><table>
<thead>
<tr>
<th><strong>工具</strong></th>
<th><strong>核心机制</strong></th>
<th><strong>独特技术</strong></th>
<th><strong>典型应用场景</strong></th>
</tr>
</thead>
<tbody><tr>
<td>RAGAS</td>
<td>LLM驱动评估</td>
<td>合成数据生成、无参考评估架构</td>
<td>对比不同RAG策略、版本迭代后的性能回归测试</td>
</tr>
<tr>
<td>LlamaIndex</td>
<td>嵌入式评估</td>
<td>异步评估引擎、模块化BaseEvaluator</td>
<td>开发过程中快速验证单个组件或完整管道的效果</td>
</tr>
<tr>
<td>Phoenix</td>
<td>追踪分析型</td>
<td>分布式追踪、向量聚类分析算法</td>
<td>生产环境监控、Bad Case分析、数据漂移检测</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>RAG</category>
        <category>系统评估</category>
      </categories>
      <tags>
        <tag>RAG</tag>
      </tags>
  </entry>
  <entry>
    <title>智能体通信协议</title>
    <url>/2026/04/26/%E6%99%BA%E8%83%BD%E4%BD%93%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<p><strong>如何让智能体与外部世界高效交互？如何让多个智能体相互协作？</strong>这就需要使用到智能体通信协议了。</p>
<p>目前主要有三种通信协议，这三种协议共同构成了智能体通信的基础设施层。</p>
<ul>
<li><strong>MCP（Model Context Protocol）</strong>用于智能体与工具的标准化通信；</li>
<li><strong>A2A（Agent-to-Agent Protocol）</strong>用于智能体间的点对点协作；</li>
<li><strong>ANP（Agent Network Protocol）</strong>用于构建大规模智能体网络。</li>
</ul>
<h1 id="设计理念比较"><a href="#设计理念比较" class="headerlink" title="设计理念比较"></a>设计理念比较</h1><p>智能体通信协议并非单一的解决方案，而是针对不同通信场景设计的一系列标准。在本章以目前业界主流的三种协议 MCP、A2A 和 ANP 为例进行实践，下面是一个总览的比较。</p>
<p><strong>（1）MCP：智能体与工具的桥梁</strong></p>
<p>MCP（Model Context Protocol）由 Anthropic 团队提出[1]，其核心设计理念是<strong>标准化智能体与外部工具&#x2F;资源的通信方式</strong>。想象一下，你的智能体需要访问文件系统、数据库、GitHub、Slack 等各种服务。传统做法是为每个服务编写专门的适配器，这不仅工作量大，而且难以维护。MCP 通过定义统一的协议规范，让所有服务都能以相同的方式被访问。</p>
<p>MCP 的设计哲学是”上下文共享”。它不仅仅是一个 RPC（远程过程调用）协议，更重要的是它允许智能体和工具之间共享丰富的上下文信息。如图 10.1 所示，当智能体访问一个代码仓库时，MCP 服务器不仅能提供文件内容，还能提供代码结构、依赖关系、提交历史等上下文信息，让智能体能够做出更智能的决策。</p>
<p><img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/10-figures/10-1.png" alt="img"></p>
<p><strong>（2）A2A：智能体间的对话</strong></p>
<p>A2A（Agent-to-Agent Protocol）协议由 Google 团队提出2，其核心设计理念是<strong>实现智能体之间的点对点通信</strong>。与 MCP 关注智能体与工具的通信不同，A2A 关注的是智能体之间如何相互协作。这种设计让智能体能够像人类团队一样进行对话、协商和协作。</p>
<p>A2A 的设计哲学是”对等通信”。如图 10.2 所示，在 A2A 网络中，每个智能体既是服务提供者，也是服务消费者。智能体可以主动发起请求，也可以响应其他智能体的请求。这种对等的设计避免了中心化协调器的瓶颈，让智能体网络更加灵活和可扩展。</p>
<p><img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/10-figures/10-2.png" alt="img"></p>
<p><strong>（3）ANP：智能体网络的基础设施</strong></p>
<p>ANP（Agent Network Protocol）是一个概念性的协议框架3，目前由开源社区维护，还没有成熟的生态，其核心设计理念是<strong>构建大规模智能体网络的基础设施</strong>。如果说 MCP 解决的是”如何访问工具”，A2A 解决的是”如何与其他智能体对话”，那么 ANP 解决的是”如何在大规模网络中发现和连接智能体”。</p>
<p>ANP 的设计哲学是”去中心化服务发现”。在一个包含成百上千个智能体的网络中，如何让智能体能够找到它需要的服务？如图 10.3 所示，ANP 提供了服务注册、发现和路由机制，让智能体能够动态地发现网络中的其他服务，而不需要预先配置所有的连接关系。</p>
<p><img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/10-figures/10-3.png" alt="img"></p>
<p>图 10.3 ANP 设计思想</p>
<p>最后在表 10.1 中，让我们通过一个对比表格来更清晰地理解这三种协议的差异：</p>
<p>表 10.1 三种协议对比</p>
<p><img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/10-figures/10-table-1.png" alt="img"></p>
<p><strong>（4）如何选择合适的协议？</strong></p>
<p>目前的协议还处于发展早期，MCP 的生态相对成熟，不过各种工具的时效性取决于维护者，更推荐选择大公司背书的 MCP 工具。</p>
<p>选择协议的关键在于理解你的需求：</p>
<ul>
<li>如果你的智能体需要访问外部服务（文件、数据库、API），选择<strong>MCP</strong></li>
<li>如果你需要多个智能体相互协作完成任务，选择<strong>A2A</strong></li>
<li>如果你要构建大规模的智能体生态系统，考虑<strong>ANP</strong></li>
</ul>
<h1 id="MCP"><a href="#MCP" class="headerlink" title="MCP"></a>MCP</h1>]]></content>
      <categories>
        <category>Agent</category>
        <category>通信协议</category>
        <category>MCP A2A ANP</category>
      </categories>
      <tags>
        <tag>Agent</tag>
      </tags>
  </entry>
  <entry>
    <title>C++面经</title>
    <url>/2025/12/20/C-%E9%9D%A2%E7%BB%8F/</url>
    <content><![CDATA[<p>1.头文件创建一个函数但源文件未实现该函数，会出现bug吗？</p>
<ul>
<li><p>函数未被使用：编译和链接时都不报错</p>
</li>
<li><p>函数被使用：编译不报错，链接时报错（特殊情况：函数为内联函数时，编译和链接都报错）</p>
</li>
<li><p>若为模板函数（需要在头文件中声明和定义），需要在编译期进行模板实例化，会在编译期报错</p>
</li>
</ul>
<p>2.头文件循环包含问题</p>
<ul>
<li>当类&#x2F;结构体仅被用作指针和引用时，不用包含完整定义，用前置声明替代头文件包含，示例如下：</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//A.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>;<span class="comment">// 前置声明</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>&#123;</span><br><span class="line">    B* b;<span class="comment">// 或B &amp;b;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//class A;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>&#123;</span><br><span class="line">    A* a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但在源文件中需要包含具体头文件</p>
<ul>
<li>解决重复包含问题：使用 头文件保护宏 或 #pragma once</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//A.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> A_H</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> A_H</span></span><br><span class="line">···</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<ul>
<li>解决依赖扩散问题：使用Pimpl解决</li>
</ul>
]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2025/12/18/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
<span id="more"></span>
]]></content>
  </entry>
  <entry>
    <title>FastAPI学习</title>
    <url>/2026/04/25/FastAPI%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h2 id="常见项目结构"><a href="#常见项目结构" class="headerlink" title="常见项目结构"></a>常见项目结构</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastapi-project/</span><br><span class="line">├── app</span><br><span class="line">│   ├── main.py                 # 项目全局入口</span><br><span class="line">│   ├── core                    # 核心配置模块</span><br><span class="line">│   │   ├── config.py           # 全局配置、环境变量读取</span><br><span class="line">│   │   └── security.py         # JWT令牌、密码加密</span><br><span class="line">│   ├── api                     # 路由控制层</span><br><span class="line">│   │   └── v1</span><br><span class="line">│   │       └── user.py         # 用户相关接口</span><br><span class="line">│   ├── schemas                 # Pydantic数据校验、序列化层</span><br><span class="line">│   ├── crud                    # 业务逻辑层</span><br><span class="line">│   ├── models                  # ORM数据库模型层</span><br><span class="line">│   ├── db                      # 数据库连接层</span><br><span class="line">│   └── utils                   # 公共工具类</span><br><span class="line">├── .env                        # 环境变量配置文件</span><br><span class="line">└── requirements.txt             # 项目依赖包</span><br></pre></td></tr></table></figure>

<h3 id="main-py-项目入口层"><a href="#main-py-项目入口层" class="headerlink" title="main.py 项目入口层"></a>main.py 项目入口层</h3><ul>
<li>初始化 FastAPI 应用实例</li>
<li>配置全局跨域中间件</li>
<li>统一注册所有业务路由</li>
<li>程序启动唯一入口</li>
<li>自动创建数据库数据表</li>
<li>管理项目全局配置</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> fastapi.middleware.cors <span class="keyword">import</span> CORSMiddleware</span><br><span class="line"><span class="keyword">from</span> app.api.v1.user <span class="keyword">import</span> router <span class="keyword">as</span> user_router</span><br><span class="line"><span class="keyword">from</span> app.db.database <span class="keyword">import</span> engine, Base</span><br><span class="line"></span><br><span class="line"><span class="comment"># 程序启动时自动根据ORM模型创建数据表</span></span><br><span class="line">Base.metadata.create_all(bind=engine)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建FastAPI主应用</span></span><br><span class="line">app = FastAPI(</span><br><span class="line">    title=<span class="string">&quot;FastAPI分层后端项目&quot;</span>,</span><br><span class="line">    version=<span class="string">&quot;1.0.0&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置跨域中间件，解决前端跨域请求问题</span></span><br><span class="line">app.add_middleware(</span><br><span class="line">    CORSMiddleware,</span><br><span class="line">    allow_origins=[<span class="string">&quot;http://localhost:5173&quot;</span>],</span><br><span class="line">    allow_credentials=<span class="literal">True</span>,</span><br><span class="line">    allow_methods=[<span class="string">&quot;GET&quot;</span>, <span class="string">&quot;POST&quot;</span>, <span class="string">&quot;PUT&quot;</span>, <span class="string">&quot;DELETE&quot;</span>],</span><br><span class="line">    allow_headers=[<span class="string">&quot;*&quot;</span>]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 注册所有业务路由</span></span><br><span class="line">app.include_router(user_router)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 根路径测试接口</span></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;msg&quot;</span>:<span class="string">&quot;项目启动成功&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<h3 id="api-路由控制层-Controller"><a href="#api-路由控制层-Controller" class="headerlink" title="api 路由控制层 Controller"></a>api 路由控制层 Controller</h3><ul>
<li>接收前端所有 HTTP 请求</li>
<li>匹配请求路径、请求方式</li>
<li>获取请求参数、请求头 Token</li>
<li>做登录鉴权依赖注入</li>
<li>调用业务层方法，<strong>不写业务逻辑、不操作数据库</strong></li>
<li>接收业务结果，返回统一数据</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> APIRouter, Depends, Header, HTTPException</span><br><span class="line"><span class="keyword">from</span> sqlalchemy.orm <span class="keyword">import</span> Session</span><br><span class="line"><span class="keyword">from</span> jose <span class="keyword">import</span> jwt, JWTError</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.db.database <span class="keyword">import</span> get_db</span><br><span class="line"><span class="keyword">from</span> app.schemas.user <span class="keyword">import</span> UserCreate, TokenResponse</span><br><span class="line"><span class="keyword">from</span> app.crud.user <span class="keyword">import</span> create_user, get_user_by_username, verify_login</span><br><span class="line"><span class="keyword">from</span> app.core.security <span class="keyword">import</span> create_access_token</span><br><span class="line"><span class="keyword">from</span> app.utils.response <span class="keyword">import</span> success, fail</span><br><span class="line"><span class="keyword">from</span> app.core.config <span class="keyword">import</span> settings</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建用户模块路由对象</span></span><br><span class="line">router = APIRouter(</span><br><span class="line">    prefix=<span class="string">&quot;/user&quot;</span>,</span><br><span class="line">    tags=[<span class="string">&quot;用户管理模块&quot;</span>]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户注册接口</span></span><br><span class="line"><span class="meta">@router.post(<span class="params"><span class="string">&quot;/register&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">register</span>(<span class="params">user: UserCreate, db: Session = Depends(<span class="params">get_db</span>)</span>):</span><br><span class="line">    <span class="comment"># 判断用户名是否已经存在</span></span><br><span class="line">    exist_user = get_user_by_username(db, user.username)</span><br><span class="line">    <span class="keyword">if</span> exist_user:</span><br><span class="line">        <span class="keyword">return</span> fail(msg=<span class="string">&quot;用户名已存在&quot;</span>)</span><br><span class="line">    <span class="comment"># 调用业务层完成注册</span></span><br><span class="line">    create_user(db, user)</span><br><span class="line">    <span class="keyword">return</span> success(msg=<span class="string">&quot;注册成功&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户登录接口</span></span><br><span class="line"><span class="meta">@router.post(<span class="params"><span class="string">&quot;/login&quot;</span>, response_model=TokenResponse</span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">login</span>(<span class="params">user: UserCreate, db: Session = Depends(<span class="params">get_db</span>)</span>):</span><br><span class="line">    <span class="comment"># 校验账号密码</span></span><br><span class="line">    login_user = verify_login(db, user.username, user.password)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> login_user:</span><br><span class="line">        <span class="keyword">raise</span> HTTPException(status_code=<span class="number">400</span>, detail=<span class="string">&quot;账号或密码错误&quot;</span>)</span><br><span class="line">    <span class="comment"># 生成登录令牌</span></span><br><span class="line">    token = create_access_token(login_user.username)</span><br><span class="line">    <span class="keyword">return</span> success(data=&#123;<span class="string">&quot;token&quot;</span>: token&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 需要登录才能访问的个人信息接口</span></span><br><span class="line"><span class="meta">@router.get(<span class="params"><span class="string">&quot;/info&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user_info</span>(<span class="params">token: <span class="built_in">str</span> = Header(<span class="params"><span class="literal">None</span></span>), db: Session = Depends(<span class="params">get_db</span>)</span>):</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 解密token获取用户信息</span></span><br><span class="line">        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])</span><br><span class="line">        username = payload.get(<span class="string">&quot;sub&quot;</span>)</span><br><span class="line">        user = get_user_by_username(db, username)</span><br><span class="line">        <span class="keyword">return</span> success(data=user)</span><br><span class="line">    <span class="keyword">except</span> JWTError:</span><br><span class="line">        <span class="keyword">return</span> fail(msg=<span class="string">&quot;Token失效，请重新登录&quot;</span>, code=<span class="number">401</span>)</span><br></pre></td></tr></table></figure>

<h3 id="schemas-数据模型层-Pydantic"><a href="#schemas-数据模型层-Pydantic" class="headerlink" title="schemas 数据模型层 Pydantic"></a>schemas 数据模型层 Pydantic</h3><ul>
<li>自动校验前端传入参数类型、是否必填</li>
<li>自动解析 JSON 请求体</li>
<li>格式化、过滤返回给前端的数据</li>
<li>配合框架<strong>自动生成接口文档</strong></li>
<li>提前拦截非法参数，减少业务代码判断</li>
<li>实现 ORM 对象与 JSON 数据转换</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户注册、登录 请求参数模型</span></span><br><span class="line"><span class="comment"># 自动校验参数类型、必填项</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserCreate</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    username: <span class="built_in">str</span></span><br><span class="line">    password: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户信息返回模型，规范返回给前端的数据字段</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserInfo</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    <span class="built_in">id</span>: <span class="built_in">int</span></span><br><span class="line">    username: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 开启ORM映射，支持直接读取数据库对象</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">Config</span>:</span><br><span class="line">        from_attributes = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 登录返回token数据模型</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TokenResponse</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    token: <span class="built_in">str</span></span><br></pre></td></tr></table></figure>

<h3 id="crud-业务逻辑层-Service"><a href="#crud-业务逻辑层-Service" class="headerlink" title="crud 业务逻辑层 Service"></a>crud 业务逻辑层 Service</h3><ul>
<li><p>编写项目所有业务逻辑</p>
</li>
<li><p>数据判断、逻辑处理、业务规则判断</p>
</li>
<li><p>调用数据库模型完成增删改查</p>
</li>
<li><p>项目真正处理业务的核心代码层</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sqlalchemy.orm <span class="keyword">import</span> Session</span><br><span class="line"><span class="keyword">from</span> app.models.user <span class="keyword">import</span> User</span><br><span class="line"><span class="keyword">from</span> app.schemas.user <span class="keyword">import</span> UserCreate</span><br><span class="line"><span class="keyword">from</span> app.core.security <span class="keyword">import</span> get_password_hash, verify_password</span><br><span class="line"></span><br><span class="line"><span class="comment"># 根据用户名查询用户</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user_by_username</span>(<span class="params">db: Session, username: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="keyword">return</span> db.query(User).<span class="built_in">filter</span>(User.username == username).first()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户注册业务逻辑</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_user</span>(<span class="params">db: Session, user: UserCreate</span>):</span><br><span class="line">    <span class="comment"># 密码加密处理</span></span><br><span class="line">    hash_pwd = get_password_hash(user.password)</span><br><span class="line">    <span class="comment"># 创建数据库用户对象</span></span><br><span class="line">    db_user = User(username=user.username, password=hash_pwd)</span><br><span class="line">    <span class="comment"># 添加数据</span></span><br><span class="line">    db.add(db_user)</span><br><span class="line">    <span class="comment"># 提交事务保存到数据库</span></span><br><span class="line">    db.commit()</span><br><span class="line">    <span class="comment"># 刷新对象数据</span></span><br><span class="line">    db.refresh(db_user)</span><br><span class="line">    <span class="keyword">return</span> db_user</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户登录账号密码校验</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">verify_login</span>(<span class="params">db: Session, username: <span class="built_in">str</span>, password: <span class="built_in">str</span></span>):</span><br><span class="line">    user = get_user_by_username(db, username)</span><br><span class="line">    <span class="comment"># 用户不存在直接返回空</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> user:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">    <span class="comment"># 密码不一致返回空</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> verify_password(password, user.password):</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">    <span class="keyword">return</span> user</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="models-ORM-数据库模型层"><a href="#models-ORM-数据库模型层" class="headerlink" title="models ORM 数据库模型层"></a>models ORM 数据库模型层</h3><ul>
<li>映射 MySQL 真实数据表</li>
<li>定义数据表字段、类型、主键、备注</li>
<li>使用对象操作代替原生 SQL 语句</li>
<li>防止 SQL 注入，简化数据库操作</li>
<li>与数据库一一对应</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sqlalchemy <span class="keyword">import</span> Column, Integer, String, DateTime</span><br><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"><span class="keyword">from</span> app.db.database <span class="keyword">import</span> Base</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用户数据表ORM模型，对应mysql user表</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">User</span>(<span class="title class_ inherited__">Base</span>):</span><br><span class="line">    <span class="comment"># 数据库真实表名</span></span><br><span class="line">    __tablename__ = <span class="string">&quot;user&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 用户主键id</span></span><br><span class="line">    <span class="built_in">id</span> = Column(Integer, primary_key=<span class="literal">True</span>, index=<span class="literal">True</span>, comment=<span class="string">&quot;用户ID&quot;</span>)</span><br><span class="line">    <span class="comment"># 用户名，唯一不可重复</span></span><br><span class="line">    username = Column(String(<span class="number">50</span>), unique=<span class="literal">True</span>, nullable=<span class="literal">False</span>, comment=<span class="string">&quot;用户名&quot;</span>)</span><br><span class="line">    <span class="comment"># 加密后的用户密码</span></span><br><span class="line">    password = Column(String(<span class="number">255</span>), nullable=<span class="literal">False</span>, comment=<span class="string">&quot;用户密码&quot;</span>)</span><br><span class="line">    <span class="comment"># 账号创建时间</span></span><br><span class="line">    create_time = Column(DateTime, default=datetime.now, comment=<span class="string">&quot;创建时间&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="db-数据库连接层"><a href="#db-数据库连接层" class="headerlink" title="db 数据库连接层"></a>db 数据库连接层</h3><ul>
<li>创建数据库连接引擎</li>
<li>管理数据库会话 Session</li>
<li>统一创建、关闭数据库连接</li>
<li>提供全局数据库依赖，供所有接口使用</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sqlalchemy <span class="keyword">import</span> create_engine</span><br><span class="line"><span class="keyword">from</span> sqlalchemy.ext.declarative <span class="keyword">import</span> declarative_base</span><br><span class="line"><span class="keyword">from</span> sqlalchemy.orm <span class="keyword">import</span> sessionmaker</span><br><span class="line"><span class="keyword">from</span> app.core.config <span class="keyword">import</span> settings</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拼接完整mysql数据库连接地址</span></span><br><span class="line">SQL_URL = (</span><br><span class="line">    <span class="string">f&quot;mysql+pymysql://<span class="subst">&#123;settings.DB_USER&#125;</span>:<span class="subst">&#123;settings.DB_PASSWORD&#125;</span>&quot;</span></span><br><span class="line">    <span class="string">f&quot;@<span class="subst">&#123;settings.DB_HOST&#125;</span>:<span class="subst">&#123;settings.DB_PORT&#125;</span>/<span class="subst">&#123;settings.DB_NAME&#125;</span>?charset=utf8mb4&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建数据库引擎，建立数据库基础连接</span></span><br><span class="line">engine = create_engine(</span><br><span class="line">    SQL_URL,</span><br><span class="line">    pool_pre_ping=<span class="literal">True</span>  <span class="comment"># 自动检测连接有效性，防止断开报错</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建数据库会话工厂，用来生成数据库操作对象</span></span><br><span class="line">SessionLocal = sessionmaker(autocommit=<span class="literal">False</span>, autoflush=<span class="literal">False</span>, bind=engine)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ORM模型基类，所有数据表模型都继承此类</span></span><br><span class="line">Base = declarative_base()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 数据库依赖函数：每次接口请求自动获取一次数据库连接</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_db</span>():</span><br><span class="line">    db = SessionLocal()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">yield</span> db</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="comment"># 请求结束自动关闭连接，释放资源</span></span><br><span class="line">        db.close()</span><br></pre></td></tr></table></figure>

<h3 id="core-核心配置层"><a href="#core-核心配置层" class="headerlink" title="core 核心配置层"></a>core 核心配置层</h3><ul>
<li>读取数据库密钥、JWT 密钥、过期时间</li>
<li>密码加密、密码校验工具封装</li>
<li>JWT 令牌生成、解密、身份认证逻辑</li>
</ul>
<p><strong>配置文件</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入环境变量读取工具</span></span><br><span class="line"><span class="keyword">from</span> pydantic_settings <span class="keyword">import</span> BaseSettings</span><br><span class="line"></span><br><span class="line"><span class="comment"># 全局配置类，自动读取.env配置文件</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Settings</span>(<span class="title class_ inherited__">BaseSettings</span>):</span><br><span class="line">    <span class="comment"># 数据库配置</span></span><br><span class="line">    DB_HOST: <span class="built_in">str</span></span><br><span class="line">    DB_PORT: <span class="built_in">int</span></span><br><span class="line">    DB_USER: <span class="built_in">str</span></span><br><span class="line">    DB_PASSWORD: <span class="built_in">str</span></span><br><span class="line">    DB_NAME: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># JWT令牌配置</span></span><br><span class="line">    SECRET_KEY: <span class="built_in">str</span></span><br><span class="line">    ALGORITHM: <span class="built_in">str</span></span><br><span class="line">    ACCESS_TOKEN_EXPIRE_MINUTES: <span class="built_in">int</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 指定配置文件路径</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">Config</span>:</span><br><span class="line">        env_file = <span class="string">&quot;.env&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 全局唯一配置实例</span></span><br><span class="line">settings = Settings()</span><br></pre></td></tr></table></figure>

<p><strong>JWT</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime, timedelta</span><br><span class="line"><span class="keyword">from</span> jose <span class="keyword">import</span> jwt</span><br><span class="line"><span class="keyword">from</span> passlib.context <span class="keyword">import</span> CryptContext</span><br><span class="line"><span class="keyword">from</span> app.core.config <span class="keyword">import</span> settings</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建密码加密上下文，使用bcrypt加密算法</span></span><br><span class="line">pwd_context = CryptContext(schemes=[<span class="string">&quot;bcrypt&quot;</span>], deprecated=<span class="string">&quot;auto&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 密码加密：明文密码 -&gt; 加密密码</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_password_hash</span>(<span class="params">password: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="keyword">return</span> pwd_context.<span class="built_in">hash</span>(password)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 密码校验：明文密码 和 加密密码比对</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">verify_password</span>(<span class="params">plain_password: <span class="built_in">str</span>, hashed_password: <span class="built_in">str</span></span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">    <span class="keyword">return</span> pwd_context.verify(plain_password, hashed_password)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成JWT登录令牌</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_access_token</span>(<span class="params">subject: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="comment"># 设置token过期时间</span></span><br><span class="line">    expire_time = datetime.utcnow() + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)</span><br><span class="line">    <span class="comment"># 封装令牌载荷数据</span></span><br><span class="line">    payload = &#123;</span><br><span class="line">        <span class="string">&quot;sub&quot;</span>: subject,</span><br><span class="line">        <span class="string">&quot;exp&quot;</span>: expire_time</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment"># 加密生成token字符串</span></span><br><span class="line">    token = jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)</span><br><span class="line">    <span class="keyword">return</span> token</span><br></pre></td></tr></table></figure>

<h3 id="utils-工具公共层"><a href="#utils-工具公共层" class="headerlink" title="utils 工具公共层"></a>utils 工具公共层</h3><ul>
<li>封装统一接口返回格式</li>
<li>统一成功、失败返回体</li>
<li>通用工具方法、格式化处理</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi.responses <span class="keyword">import</span> JSONResponse</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统一成功返回格式</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">success</span>(<span class="params">data=<span class="literal">None</span>, msg=<span class="string">&quot;请求成功&quot;</span>, code=<span class="number">200</span></span>):</span><br><span class="line">    <span class="keyword">return</span> JSONResponse(content=&#123;</span><br><span class="line">        <span class="string">&quot;code&quot;</span>: code,</span><br><span class="line">        <span class="string">&quot;msg&quot;</span>: msg,</span><br><span class="line">        <span class="string">&quot;data&quot;</span>: data</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统一失败返回格式</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">fail</span>(<span class="params">msg=<span class="string">&quot;请求失败&quot;</span>, code=<span class="number">400</span></span>):</span><br><span class="line">    <span class="keyword">return</span> JSONResponse(content=&#123;</span><br><span class="line">        <span class="string">&quot;code&quot;</span>: code,</span><br><span class="line">        <span class="string">&quot;msg&quot;</span>: msg,</span><br><span class="line">        <span class="string">&quot;data&quot;</span>: <span class="literal">None</span></span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<h2 id="接口完整请求流程"><a href="#接口完整请求流程" class="headerlink" title="接口完整请求流程"></a>接口完整请求流程</h2><p>前端请求</p>
<p>→ 跨域中间件校验放行</p>
<p>→ main 主程序路由分发</p>
<p>→ api 路由接收请求</p>
<p>→ schemas 参数自动校验</p>
<p>→ Token 登录身份校验</p>
<p>→ 调用 crud 业务逻辑</p>
<p>→ models 操作数据库</p>
<p>→ db 数据库会话交互</p>
<p>→ 数据封装统一返回前端</p>
<p>→ FastAPI 自动生成接口文档</p>
<p>![ChatGPT Image 2026年4月26日 20_57_40](C:\Users\Guailper\Downloads\ChatGPT Image 2026年4月26日 20_57_40.png)</p>
<h2 id="FastAPI-核心基础知识点"><a href="#FastAPI-核心基础知识点" class="headerlink" title="FastAPI 核心基础知识点"></a>FastAPI 核心基础知识点</h2><h3 id="路由请求方式：GET、POST、PUT、DELETE"><a href="#路由请求方式：GET、POST、PUT、DELETE" class="headerlink" title="路由请求方式：GET、POST、PUT、DELETE"></a><strong>路由请求方式</strong>：GET、POST、PUT、DELETE</h3><h3 id="路径参数、查询参数"><a href="#路径参数、查询参数" class="headerlink" title="路径参数、查询参数"></a>路径参数、查询参数</h3><h3 id="请求体-Body（JSON-参数）"><a href="#请求体-Body（JSON-参数）" class="headerlink" title="请求体 Body（JSON 参数）"></a>请求体 Body（JSON 参数）</h3><h3 id="Pydantic-数据模型、数据校验"><a href="#Pydantic-数据模型、数据校验" class="headerlink" title="Pydantic 数据模型、数据校验"></a>Pydantic 数据模型、数据校验</h3><h3 id="请求头-Header、Cookie"><a href="#请求头-Header、Cookie" class="headerlink" title="请求头 Header、Cookie"></a>请求头 Header、Cookie</h3><h3 id="响应数据、自定义返回格式"><a href="#响应数据、自定义返回格式" class="headerlink" title="响应数据、自定义返回格式"></a>响应数据、自定义返回格式</h3><h3 id="跨域-CORS"><a href="#跨域-CORS" class="headerlink" title="跨域 CORS"></a>跨域 CORS</h3><h3 id="JWT-登录认证-Token"><a href="#JWT-登录认证-Token" class="headerlink" title="JWT 登录认证 Token"></a>JWT 登录认证 Token</h3><h3 id="依赖注入-Depends"><a href="#依赖注入-Depends" class="headerlink" title="依赖注入 Depends"></a>依赖注入 Depends</h3><h4 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h4><p>依赖注入（Dependency Injection，简称DI），简单来说，就是将程序中「多个模块&#x2F;接口都需要用到的公共逻辑（如登录校验、数据库连接），抽离成独立的“依赖函数”，在需要使用的地方，通过声明依赖，由框架自动执行该函数、并将执行结果传入，无需手动调用和重复编写的一种编程思想。</p>
<p>在FastAPI中，依赖注入主要通过<code>Depends()</code>方法实现，核心目的是解决代码重复、结构混乱的问题，让接口只专注于核心业务，不用关心公共逻辑的实现细节。</p>
<p>通俗类比：就像写作业时，不用自己准备铅笔、本子（重复杂事），有专门的后勤员（依赖函数）提前准备好，你只要说“我需要学习工具”（声明依赖），后勤员就会把东西递到你手上（自动注入），你直接写作业（核心业务）即可。</p>
<h4 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h4><p>FastAPI的依赖注入核心是「自动解析+自动执行+自动传值」，整体流程分为3步，简单易懂，无需复杂理解：</p>
<ol>
<li>定义依赖函数：将公共逻辑（如登录校验、数据库连接）单独写成一个函数，函数可接收参数（如请求头的Token），并返回处理结果（如当前登录用户、数据库会话）。</li>
<li>声明依赖：在接口函数的参数中，通过<code>参数名 = Depends(依赖函数)</code>的方式，告诉FastAPI“这个接口需要用到这个依赖”。</li>
<li>自动注入：当有请求访问接口时，FastAPI会自动先执行声明的依赖函数，获取其返回结果，再将结果自动传入接口函数的对应参数中，接口直接使用该结果即可，无需手动调用依赖函数。</li>
</ol>
<p>补充：依赖可以多层嵌套（如A依赖B，B依赖C），FastAPI会自动按顺序解析、执行所有依赖，最终将最底层的结果传入接口，全程无需手动干预。同时，依赖函数执行完成后，框架会自动处理资源释放（如数据库连接关闭）。</p>
<h4 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h4><h5 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h5><ol>
<li>减少重复代码（核心优点）：公共逻辑（登录、连库）只写1次，所有需要的接口直接复用，避免重复编写，提升开发效率。</li>
<li>职责分离，结构清晰：依赖函数负责处理杂事（校验、资源管理），接口只负责核心业务（增删改查），代码分层明确，易读易维护。</li>
<li>统一维护，修改便捷：若公共逻辑需要修改（如登录规则、数据库地址变更），只需修改依赖函数1处，所有依赖该函数的接口自动同步更新，减少修改成本和bug。</li>
<li>实现代码解耦：接口无需关心依赖的内部实现，只需声明“需要什么”，依赖和接口互不影响，便于后续扩展和修改。</li>
<li>便于测试和权限控制：可灵活替换依赖（如测试时用模拟依赖），也可通过多层依赖组合（如“登录校验→管理员校验”），实现灵活的权限管控。</li>
<li>自动管理资源：如数据库连接依赖，框架会自动开启连接、请求结束后自动关闭，避免人为忘记释放资源导致的问题。</li>
</ol>
<h5 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h5><ol>
<li>学习成本高：新手难以理解“数据自动传入”的逻辑，阅读代码时需要跳转查看依赖函数，增加阅读成本。</li>
<li>排查问题复杂：若依赖多层嵌套（A→B→C），一旦出现报错，报错链路较长，难以快速定位问题所在。</li>
<li>小型项目易过度设计：若项目只有2-3个接口，无需复杂的公共逻辑，强行使用依赖注入会多写多余代码，小题大做。</li>
<li>存在微量性能损耗：框架需要自动解析依赖、执行依赖函数，比手动调用函数多了一层中间流程，虽损耗极小可忽略，但理论上存在性能开销。</li>
<li>需注意依赖生命周期：不规范使用（如全局依赖未释放资源），可能导致数据库连接泄露、全局变量污染等问题。</li>
</ol>
<h3 id="异步-async-await"><a href="#异步-async-await" class="headerlink" title="异步 async &#x2F;await"></a>异步 async &#x2F;await</h3><h3 id="数据库操作、ORM"><a href="#数据库操作、ORM" class="headerlink" title="数据库操作、ORM"></a>数据库操作、ORM</h3><h3 id="自动接口文档"><a href="#自动接口文档" class="headerlink" title="自动接口文档"></a>自动接口文档</h3>]]></content>
      <categories>
        <category>Python</category>
        <category>后端</category>
        <category>后端</category>
        <category>FastAPI</category>
      </categories>
      <tags>
        <tag>fastapi</tag>
      </tags>
  </entry>
</search>
